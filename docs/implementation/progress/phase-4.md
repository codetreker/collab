# Phase 4+ — 剩余模块 (detail)

> 引自 [PROGRESS.md](../PROGRESS.md) 概览表 Phase 4+ 行 — milestone 翻牌点单源在此. 含 AL/BPP/HB/RT/AP/CM/ADM/DL/CS 9 模块组.

## Phase 4+ — 剩余模块

按需排序。**已知依赖锁紧 (绘制成依赖箭头, 不允许颠倒)**:

```
DL-4 ──→ HB-1  (plugin manifest API)
DL-4 ──→ CS-3  (Web Push gateway)
       (DL-4 必须先于 HB-1/CS-3, 飞马 R2)

BPP-1 ──→ AL-2a ──→ AL-2b ╲
   │                       ╲
   ╰──→ ─────────────→ BPP-3 (AL-2b 与 BPP-3 同 PR 合)

CM-4 ─┬→ CM-5 (agent↔agent, 新增, 依赖 CM-4 + AP-3)
      │
AP-3 ─┘
```

### agent-lifecycle
- [x] **AL-2 wrapper** ⭐ agent lifecycle release gate ✅ 4.1 (PR feat/al-2-wrapper): 4 件套 docs (spec ≤80 行 + stance 70 + acceptance 65) + `docs/release/agent-lifecycle-release-gate.md` ≥10 硬条件清单 (5-state graph reflect + reason 字典锁链 ≥10 处 byte-identical 跨 milestone + 不另起第 7 reason + 不挂 connecting 持久态 BPP-5 立场承袭 + AL-1.4/AL-2a/AL-2b 真测复用 + busy/idle BPP source 锁 + AL/HB 字典分立 + no-bypass + no-admin-godmode-al + AL/HB yml 拆独立) + `.github/workflows/al-release-gate.yml` ≥12 step CI workflow (跟 HB-4 release-gate.yml **拆独立 yml** — host 层 vs runtime 层守门拆死, 跟 HB-3 字典分立同立场承袭); **drift 防御链 release-time 收口** (reason 字典锁链跨 10+ milestone 真守门 + BPP-5 立场承袭 release 前 verify + AL/HB stack audit 字典分立). 4.2 野马签字 ⏸️ deferred 留账 release 前补 3 张截屏 (5-state UI / error→online 反向链 / busy/idle BPP frame 触发).
- [x] **AL-1** 状态四态扩展 ✅ Phase 4 wrapper milestone — 战马D 实施 (一 milestone 一 PR 整闭): schema agent_state_log v=25 + state machine validator (5-state graph + 6 reason 第 8 处单测锁链 byte-identical 跟 AL-1a 同源) + AppendAgentStateTransition single-gate helper (立场 ② state machine 单源) + GET /api/v1/agents/:id/state-log owner-only endpoint + 20 unit tests + 11 acceptance items 全 ✅; AL-1a (#249 三态+6 reason) ✅ + AL-1b (#453/#457/#462 5-state busy/idle) ✅ + BPP-2.2 (#485 task lifecycle frame) ✅ 前置就位; ⏸️ follow-up: dispatcher wire (BPP-2.2 frame → audit 自动写) + presence wire + client UI + e2e
  - [x] **AL-1a** online/offline + error 旁路 + 6 reason codes (PR #249, Phase 2 起步, 蓝图 §2.3 R3 锁)
  - [x] **AL-1b** busy/idle 三段全闭 ✅ (#453 schema v=21 + #457 server 5-state 合并 GET + BPP 单源 PATCH 405 + #462 client SPA PresenceDot 5-state describeAgentState, 跟 BPP-2 #460 同期 stub — task_started/task_finished frame 真接管 BPP-2.2 落地后切)
  - [x] **AL-1.4 wrapper** state machine validator + agent_state_log v=25 + GET /api/v1/agents/:id/state-log owner-only — 本 PR 整闭 ✅
- [x] **AL-2a** config 表 + update API ✅ 三段全闭 (PR #480 merged 7a0c69b): schema v=20 #447 (agent_configs PK 单 agent_id + blob TEXT JSON SSOT) + 4 件套 #264 acceptance / #454 stance+content-lock + AL-2a.2 server PATCH /api/v1/agents/:id/config + AgentConfigPusher interface (nil-safe, 跟 AL-2b 跨 milestone 依赖 schema 锁) + AL-2a.3 client AgentConfigPanel mount + REG-AL2A-001..007 7🟢 + 八处单测锁 sync (#481 ack 加入 7+8); production wiring `Pusher: s.hub` 在 #481 AL-2b merge 时自然落
- [x] **AL-2b** BPP agent_config 双向 frame ✅ 五段全闭 (PR #481 merged 225e739): frame schema 7+7 字段 byte-identical (status CHECK 三态 + direction lock) + hub.PushAgentConfigUpdate (server→plugin) + ack dispatcher (interface seam + 4-step validation + reason 第 8 处链) + api PATCH fanout (idempotency_key deterministic) + spec sync byte-identical; plugin read loop ack ingress ⏸️ deferred → BPP-3 (调研结论: plugin.go RPC envelope vs BPP frame 不同 wire, 需 BPP-3 plugin connection lifecycle 建统一 dispatcher 边界)
- [x] **AL-3** presence 完整版 ✅ (复用 PresenceTracker IsOnline + Sessions 接口, #277 stub → 真实施)
  - [x] **AL-3.1** schema (presence_sessions 表, v=12) (PR #310 merged)
  - [x] **AL-3.2** server hub WS lifecycle hook (PR #317 merged)
  - [x] **AL-3.3** client UI presence dot (PR #324 + #327 follow-up merged)
- [x] **AL-4** runtime registry — 4 件套全 merged + 三段全闭: spec #313 v0/#379 v2 / acceptance #318 / stance #319 / 文案锁 #321; AL-4.1 ✅ #398 (1327c82) schema v=16 / AL-4.2 ✅ #414 (ef7d124) server registry + start/stop API + heartbeat / AL-4.3 ✅ #417 (275dfb8) client SPA agent settings runtime 启停 UI + #427 acceptance §3 + G2.7 demo screenshot follow-up
  - [x] **AL-4.1** schema (agent_runtimes 表, v=16) (PR #398 merged 1327c82)
  - [x] **AL-4.2** server start/stop API + heartbeat hook + admin god-mode (PR #414 merged ef7d124)
  - [x] **AL-4.3** client SPA agent settings 启停 UI (PR #417 merged 275dfb8 + #427 follow-up)

### plugin-protocol (BPP)
- [x] **BPP-1** 协议骨架 + 直连 flag + grep no-runtime + thinking subject 反约束 (PR #304 merged)
- [x] **BPP-2** 抽象语义层 — 4 件套 ✅ (spec `bpp-2-spec.md` + acceptance `bpp-2.md` + 文案锁 `bpp-2-content-lock.md` + stance `bpp-2-stance-checklist.md` 战马E v0 #460 全 land); 实施 PR #485 `feat/bpp-2` 整三段一次合 (新协议 "一 milestone = 一 worktree = 一 PR" #479): BPP-2.1 dispatch 层 + 7 op 白名单 + ActionHandler interface + BPP-2.2 task lifecycle reverse-channel (TaskStarted/TaskFinished + subject 空 reject + outcome 3 态 + reason AL-1a 6 字典 byte-identical 同源) + BPP-2.3 agent_config_update validation + 6 fields 白名单 + ConfigRevTracker 幂等 reload — 三段四件全闭 ⭐ (REG-BPP2-001..017 全 🟢; 26 单测全绿; 7 反约束 grep count==0; bppEnvelopeWhitelist 9→11 扩 BPP-1 #304 reflect lint 自动覆盖); AL-1b busy/idle source 真接管 + AL-2b/BPP-3 SSOT 推送触发留 BPP-3 起步时同期合
- [x] **BPP-3** plugin 上行 BPP frame 统一 dispatcher 边界 ✅ (PR #489 feat/bpp-3): `internal/bpp/plugin_frame_dispatcher.go` 新 `PluginFrameDispatcher` (Register direction-lock + envelope-whitelist 守 / Route 软跳未知 type 前向兼容) + `AckFrameAdapter` 接 AL-2b `AckDispatcher` (deferred from #481, plugin.go RPC envelope vs BPP frame 拆死) + `internal/api/agent_config_ack_handler.go` 真接 `AgentConfigAckHandlerImpl` (3 status × log path) + `AgentOwnerResolver` (跟 anchor #360 owner-only 同源 store.GetAgent) + `internal/ws/hub.go` 加 `pluginFrameRouter` 字段 + `PluginFrameRouter` interface (跟 ws→bpp 反向 import 守, 同 BPP-2.1 ActionHandler 模式) + `internal/ws/plugin.go` read loop default case 路由 + `internal/server/server.go` 启动 wire-up (`pluginFrameRouterAdapter` ws/bpp PluginSessionContext bridge); 15 dispatcher unit + 5 handler unit 全绿; AL-2b ack 入站三态 (applied/rejected/stale) plugin → server 通路开闸闭环
  - [x] **BPP-3.1** `permission_denied` frame (server→plugin) — 战马C 一 milestone 一 PR ✅: PermissionDeniedFrame 8 字段 byte-identical 跟蓝图 auth-permissions.md §4.1 row + AP-1 #493 abac.go 403 body (`required_capability` + `current_scope` 跨 PR drift 守); envelope whitelist 12→13 扩; PushPermissionDenied hub method (跟 PushAgentConfigUpdate 同模式 + cursor 共序 跟 RT-1/AL-2b 共一根 sequence); PermissionDeniedPusher interface seam (api 包不 import ws, AP-1 wiring deferred 1-line follow-up); 6 unit + frame_schemas_test count 自动覆盖. REG-BPP31-001..006 全 🟢; AP-1 留账 ⏸️→ in-progress (wiring 待 AP-1 + BPP-3.1 任一 merge 后接).
  - [x] **BPP-3.2** permission_denied plugin UX 流 — 战马C 一 milestone 一 PR ✅ (4 件套 spec 全闭 c8e37a4 + 77488a0; 三段实施 e61a411 / e47f33d / 本 commit; 蓝图 §1.3 主入口闭环): **3.2.1 server DM dispatch** (`SemanticOpRequestCapabilityGrant` ValidSemanticOps 7→8 + `CapabilityGrantHandler` 调既有 type='system' DM channel + DM body 字面 `"{agent_name} 想 {attempted_action} 但缺权限 {required_capability}"` byte-identical 跟 content-lock §1 + quick_action JSON 5 字段 + capability AP-1 14 const 校验, 5 unit + 反向 grep 守 DM 不另起 channel 类型 + capability hardcode 0 hit) + **3.2.2 owner DM UI 一键 grant** (`POST /api/v1/me/grants` owner-only ACL + scope v1 三层 + action 3-enum {grant, reject, snooze} + reject/snooze v1 audit-only 不持久化 deny list, 5 server unit; `SystemMessageBubble.tsx` 三按钮 DOM 字面 byte-identical 跟 content-lock §3 "授权"/"拒绝"/"稍后" + data-action + data-bpp32-button + 12 同义词反向 grep 禁词 + isBPP32GrantPayload type guard, 10 vitest) + **3.2.3 plugin retry cache** (`RequestRetryCache` in-memory TTL 5min + `MaxPermissionRetries=3` const + `RetryBackoff=30s` 固定退避 + `bpp.retry_exhausted` 错码 byte-identical 跟 content-lock §4, 5 unit + 反向 grep 不复用 BPP-4 watchdog 队列 — 拆三路径) + **server-side full-flow integration test** (agent commit_artifact 无权 → 403 + body BPP routing → owner POST /me/grants → user_permissions 真改 → agent 重试 200, 替代 Playwright e2e 主菜留 plugin SDK 真接入时落). REG-BPP32-001..012 + REG-BPP32-201..205 + REG-BPP32-301 共 17 行 🟢 + REG-BPP32-401 ⏸️ Playwright follow-up. docs/current sync (server/bpp/dispatcher.md §2.1 + server/system-dm.md §4 + client/ui/message.md §4f).
- [x] **BPP-4** 失联检测 + dead-letter audit log ✅ (PR feat/bpp-4): `internal/bpp/heartbeat_watchdog.go` 新 (30s 单源阈值锁 + 10s ticker + AgentErrorSink interface seam 走 agent.Tracker.SetError; 触发 reason=`network_unreachable` 复用 AL-1a 6-dict **第 9 处单测锁链**承袭, 不另起 reason; markedErr 防重复 spammy + 重连自动清) + `internal/bpp/dead_letter.go` 新 (`bpp.frame_dropped_plugin_offline` log key + DeadLetterAuditEntry 5 字段 schema byte-identical 跟 HB-1/HB-2 audit 三处同源, **不入持久队列** RT-1.3 cursor replay 兜底) + `internal/ws/hub.go` 加 `SnapshotPluginLastSeen` (跟 PluginFrameRouter interface seam 同模式) + `internal/ws/plugin.go` 加 `lastSeenAt` mutex-guarded + `touchLastSeen` 每 inbound frame 刷 + `internal/ws/al_2b_2_agent_config_push.go` 改 (plugin offline → LogFrameDroppedPluginOffline) + `internal/server/server.go` 启动 wire-up `hubLivenessAdapter` ws/bpp 桥; **8 watchdog unit + 4 dead-letter unit 全绿** 含 AST 反向断言 `pendingAcks|retryQueue|deadLetterQueue|ackTimeout` 0 hit (best-effort 立场代码层守); 反约束: 不取消 in-flight + 不动 envelope whitelist + admin god-mode 不入
- [x] **BPP-5** plugin reconnect handshake + cursor resume 协议化 ✅ (PR feat/bpp-5): `internal/bpp/envelope.go` 改 (FrameTypeBPPReconnectHandshake + ReconnectHandshakeFrame 6 字段 byte-identical `{type, plugin_id, agent_id, last_known_cursor, disconnect_at, reconnect_at}` direction lock plugin→server, **whitelist 13→14**, BPP-1 #304 reflect lint 自动覆盖) + `internal/bpp/reconnect_handler.go` 新 (PluginFrameDispatcher 复用注册; 调 `bpp.ResolveResume(SessionResumeRequest{Mode: incremental, Since: LastKnownCursor}, …)` **复用 RT-1.3 #296 既有 mechanism**, 不另起 sequence; cross-owner reject + cursor 倒退 trust-but-log warn `bpp.reconnect_cursor_regression` (严格 reject 留 v2); 调 `agent.Tracker.Clear` 触发 AL-1 5-state error→online 反向 valid edge, 不另起 connecting 持久态) + `internal/server/server.go` 启动 wire-up (channelScopeAdapter store→bpp 桥, 复用 ownerResolver + agent.Tracker); **9 unit 全绿** (frame schema 3 项 + handler 5 项 + AST scan 反约束 1 项) — AST scan forbidden tokens 锁链延伸 (BPP-4 dead_letter `pendingAcks|retryQueue|deadLetterQueue|ackTimeout` + BPP-5 加 `pendingReconnects|reconnectQueue|deadLetterReconnect`); reason 字典锁链 BPP-5 = **第 10 处** (BPP-2.2 #485 第 7 + AL-2b #481 第 8 + BPP-4 #499 第 9 + BPP-5 第 10, connecting 中间态 reason-less, 不扩第 7 reason 复用 6-dict)

### host-bridge (Borgee Helper)
- [x] **HB-1** install-butler server endpoint v0 [A] — GET /api/v1/plugin-manifest + ed25519 signed + 7-reason 字典 — 战马D / 飞马 / 烈马 一 milestone 一 PR ✅ (按 hb-1-spec.md v1 [A] scope server endpoint, **0 schema 改**, Rust client install-butler daemon 留 HB-1b 独立 milestone 跟 BPP-2 server-first 模式; HB stack Go 重审决策已对齐 — daemon 改 Go, spec patch 留 follow-up): 4 件套 spec/stance/acceptance/content-lock 全闭 + `internal/api/hb_1_plugin_manifest.go` (HB1PluginManifestHandler + 7-reason const HB1Reason* byte-identical 跟 #491 spec §3.3 + HB1AllReasons 7-tuple 反向 grep 守门 + PluginManifestEntries const slice + ed25519 detached signature + audit 5 字段) + 11 unit PASS (TestHB1_PluginManifest_Returns200_WithShape + Unauthorized_NoToken_401 + PluginEntriesConstNonEmpty + NoSchemaChange + ReasonsByteIdentical + ManifestSignatureVerify + SignPayloadEd25519Roundtrip + NoAdminPluginManifestPath + NoPluginManifestQueue + PluginManifest_Returns200 + NoAL1aDriftIntoHB1) + `internal/server/server.go` 注册紧挨 PWAManifestHandler. 立场 4 项: Bearer api-key 鉴权 + manifest 7-reason 字典 byte-identical + ed25519 signature + admin god-mode 不挂 (ADM-0 §1.3) + AST 锁链延伸第 23 处 forbidden 3 token (`pendingPluginManifest/pluginManifestQueue/deadLetterPluginManifest`) 0 hit. 跨 milestone byte-identical 锁链: AL-1a #249 reason 锁链停在 HB-6 第 19 处 (HB-1 7-dict 跟 runtime 6-dict 字典分立) + audit 5 字段 (HB-1+HB-2+BPP-4+HB-3+HB-4 跨五 milestone) + DL-4 #485 PWA manifest 命名拆死锚 (GET /api/v1/plugin-manifest 跟 PWA manifest 不同 endpoint). REG-HB1-001..006 共 **6 行 🟢**.
- [ ] **HB-2** host-bridge daemon (仅读)
- [x] **HB-3** 情境化授权 4 类 ✅ (PR feat/hb-3 9b97809+95f3dfa+本 commit): 4 件套 docs (spec ≤80 行 / stance 68 行 / acceptance 64 行 / content-lock 53 行) + `internal/migrations/hb_3_1_host_grants.go` v=27 (host_grants 9 列 / grant_type 4-enum CHECK 跟蓝图 §1.3 字面 byte-identical / ttl_kind 2-enum 跟弹窗 UX 双向锁 / forward-only revoke 不真删行) + `internal/api/host_grants.go` POST/GET/DELETE owner-only ACL (anchor #360) + audit log 5 字段 byte-identical 跟 HB-1/HB-2/BPP-4 dead-letter **跨四 milestone 同源** (改 = 改四处单测锁) + `packages/client/src/components/HostGrantsPanel.tsx` 弹窗 DOM 三按钮 byte-identical (data-action="deny|grant_one_shot|grant_always" + data-hb3-button="danger|primary"); **7 migration unit + 8 server REST unit + 5 client vitest 全绿** 含 AST scan `pendingGrants/grantQueue/deadLetterGrants` 0 hit (best-effort 立场代码层守, BPP-4/5 锁链延伸第 3 处) + AST scan user_permissions reference 0 hit (字典分立反约束); 反约束: 撤销 → revoked_at + daemon 不缓存 (HB-4 §1.5 release gate 第 5 行 < 100ms v1 实现) + admin god-mode 不入 grant (用户主权 蓝图 §1.3 字面). HB-3.2 daemon Rust crate 跟 HB-2 同 PR 真接入 (本 PR 仅锁 contract).
- [x] **HB-4** ⭐ 4.1 行为不变量 ✅ + 4.2 demo 签字 ⏸️ deferred (PR feat/hb-4): 4 件套 docs (spec a02f1d6 ≤80 行 + stance 67 + acceptance 63) + `docs/release/host-bridge-release-gate.md` ≥10 硬条件清单 (蓝图 §1.5 6 行 byte-identical + 跨 milestone 反约束 4 项 + 反约束守门 4 项 + 信任五支柱 UI 5 项) + `.github/workflows/release-gate.yml` (≥10 step CI workflow, 任一 fail → release block; 含 audit-schema-cross-milestone reflect lint + AST scan 锁链跨三 milestone BPP-4/BPP-5/HB-3 + revoke-latency 真测 + dict-isolation host vs runtime + numeric-singletons BPP-4 30s heartbeat 单源 + no-bypass 反向 grep + no-admin-godmode + startup/dogfood/signature 三 placeholder 留账); audit schema 锁链 HB-4 = **第 5 处** (HB-1+HB-2+BPP-4+HB-3+HB-4 跨五 milestone byte-identical, 改 = 改五处单测锁); ⭐ drift 防御链至 release 收口. 4.2 野马签字 ⏸️ deferred 留账 release 前补 3 张截屏 (五支柱状态页 / 情境授权弹窗 / 撤销后行为).

### realtime
- [x] **RT-2** 离线回放人/agent 拆 (取消 ⭐ cancelled)
- [ ] **RT-3** ⭐ 多端全推 + 活物感 + thinking subject 反约束 (升 ⭐, 取代 RT-2)

### auth-permissions (剩余, AP-0 在 Phase 1)
- [x] **AP-1** ABAC 单 SSOT + capability 白名单 + 严格 403 — 战马C 一 milestone 一 PR ✅ (按 `docs/implementation/modules/ap-1-spec.md` 飞马 v0 spec 三立场对齐): ① **REG-CHN1-007 ⏸️→🟢** `internal/api/channels.go::handleGetChannel` 404→403 flip (非 member 严格 403, 不再 404 隐藏存在性, 跟 GitHub repo 私有路径同模式) + 3 处 e2e flip; ② `internal/auth/abac.go::HasCapability(ctx, perm, scope) bool` 单 SSOT helper (agent 不享 (*,*) 短路 / human 享 wildcard / 跨 scope 严格); ③ `internal/auth/capabilities.go` const 白名单 14 项 byte-identical 跟 spec §1 ③; 真路由 wired POST /api/v1/artifacts/{id}/commits 用 `auth.HasCapability(ctx, auth.CommitArtifact, ...)` (POC); 反约束 grep 单测守 spec §2 #1 (`HasCapability("..."` hardcode 0 hit). 不在范围 (spec §5): expires_at runtime check (schema slot 留 v=24 migration, 业务化 v2+) / bundle UI / cross-org AP-3 / permission_denied BPP frame. REG-CHN1-007 + REG-AP1-001..007 + REG-AP1-101..104 共 11 行 🟢 + 1 flip ⏸️→🟢.
- [ ] **AP-2** UI bundle (无角色名)
- [x] **AP-3** 跨 org owner-only 强制 (PR #521 merged)
- [x] **AP-4-enum** capability 清单 enum 化 + reflect-lint 单源 — 战马 / 飞马 / 烈马 一 milestone 一 PR ✅ (按 `docs/implementation/modules/ap-4-enum-spec.md` v0 三立场对齐, wrapper milestone 复用 AP-1 #493 capabilities.go 14 const byte-identical 不动, **0 schema 改 + 0 endpoint 加**): 4 件套 spec/stance/acceptance/content-lock 全闭 + AP-4-enum.1 `var ALL = []string{...}` ordered slice + `func init()` 自动 rebuild Capabilities map + `IsValidCapability(name)` helper 单源 (reasons.IsValid #496 SSOT 包同精神, 7 unit `capabilities_lint_test.go` PASS — TestAP4E1_ALL_OrderedByteIdentical + Capabilities_AutoBuildFromAll + ALL_Length14 + reflect_lint_NoOrphanConst + reflect_lint_NoExtraInMap + NoAdminGodModeInALL + IsValidCapability_TruthTable) + AP-4-enum.2 handler 路径替换 (capability_grant.go + me_grants.go 既有 `auth.Capabilities[name]` → `auth.IsValidCapability(name)` 走 helper 单源) + `.github/workflows/release-gate.yml` 加 step `ap4enum-no-hardcode-capability` (跟 BPP-4 AST scan / HB-3 dict-isolation 同模式, 4 反向 grep CI 守门: HasCapability hardcode 0 hit + auth.Capabilities[ direct 0 hit + Capabilities map literal mutate 0 hit + admin god-mode 字面禁) + 4 unit `ap_4_enum_reverse_grep_test.go` PASS — TestAP4E2_HandlerHelperOnly + ReverseGrep_HardcodeCapability + ReverseGrep_DirectMapAccess + CIWorkflowStepExists. 立场 3 项: ① ALL slice + init 自动 rebuild SSOT (改 capability = 改 ALL 一处, reflect-lint 单测立刻 fail) + ② 反向 grep CI 守门 hardcode 0 hit (production code 必走 const, _test.go 白名单) + ③ IsValidCapability helper 单源 (handler 不准直查 map). 命名澄清: 跟 AP-4 #551 reactions ACL 共用 AP-4 标号双义解锁, 文件名 ap-4-enum-*.md 避漂. 不在范围: enum schema migration (string SSOT 不入 DB enum) / capability bundle UI (留 AP-N) / generic auth analyzer (v3+) / admin god-mode capability (永久不挂 ADM-0 §1.3 红线) / ABAC condition time/ip (v2+). 跨 milestone byte-identical 锁链: AP-1 #493 14 const 字面 unchanged + reasons.IsValid #496 SSOT 包同模式 + BPP-4 / HB-3 release-gate AST scan + ADM-0 §1.3 红线. REG-AP4ENUM-001..006 共 **6 行 🟢**. docs/current sync (server/auth.md §capabilities-enum + ALL slice 字面引用).

### concept-model 补
- [x] **CM-5** agent 间独立协作 (新增, X2 冲突裁决) — Phase 4 第二个 milestone, 三段全闭 ✅: spec #463 (5 立场 + 3 拆段 + 4 行黑名单) + CM-5.1 反约束 grep #473 (cm5stance/cm_5_1_anti_constraints_test.go 5 cases AST walk + go/parser) + CM-5.2 server 端到端 #476 (cm_5_2_agent_to_agent_test.go 5 cases) + CM-5.3 client SPA stacked into #476 (lib/cm5-toast.ts X2 toast 字面锁 byte-identical `正在被 agent {name} 处理` + `data-cm5-collab-link` DOM hover anchor + 6 cm-5-content-lock.test.ts cases + e2e cm-5-x2-collab.spec.ts + screenshot). 立场关键: **0 行 server 实施代码新增** (走人协作 path 不裂表). REG-CM5-001..005 全 🟢.

### admin-model
- [x] **ADM-1** 用户隐私承诺页 ✅ Phase 4 启动 milestone — 战马B 实施 #455 + 战马D e2e/截屏 #459 (3 cases PASS, `g4.1-adm1-{privacy-promise,privacy-table}.png` 入 git, REG-ADM1-001..006 6 🟢; 联签 ADM-2 留账)
- [x] **ADM-2** 分层透明 audit + impersonate ✅ (取消 ⭐, 野马 R2 — 普通用户无感; 内部 milestone 烈马代签) — 战马D 实施 #484 (一 milestone 一 PR 整闭): admin_actions schema v=22 + impersonation_grants schema v=23 + 5 REST endpoints (双 GET + 业主授权 CRUD) + 4/5 admin handler audit hook (start_impersonation 留 follow-up) + system DM 5 模板字面 byte-identical + client UI (BannerImpersonate.tsx + AdminActionsList.tsx + ImpersonateGrantSection.tsx) + 71 unit tests PASS + coverage 85.1%; REG-ADM2-001..009 9 🟢; ADM-1 #464 deferred 2 行 (admin 写动作 system DM admin_name 非 raw UUID) ⏸️→✅ 兑现; ⏸️ follow-up: REG-ADM2-010 (grant 校验 wire) + REG-ADM2-011 (admin SPA audit-log 页 + e2e + G4.2 双截屏)
- [ ] **ADM-3** 来源 C 混合

### data-layer (剩余, INFRA-1 在 Phase 0)
- [x] **DL-1** 接口抽象 (A 必修) ✅ 整 milestone 一 PR (4 件套 + 4 interface + factory + DI seam + 5 sample handler 注入 + CI 守门链第 6 处): spec `dl-1-spec.md` v0 (3 立场 + 5 反向 grep + 3 段) + stance-checklist (野马) + acceptance template (zhanma-d 代翻 据 CI evidence, liema 回归 audit) + content-lock 不需 (server-only). DL-1.1 4 interface (`internal/datalayer/{storage,presence,eventbus,repository,v1_sqlite,factory}.go`, 599 行) + 12 unit (4 interface × 3 case PASS) + DL-1.2 server.go wire (Server struct +`dl *datalayer.DataLayer`, NewDataLayer 单源 wire) + 5 sample handler 注入 (UserHandler / RemoteHandler / CommandHandler / AgentHandler / AL5Handler 加 DataLayer 字段 nil-safe; al_5_recover.go agent ACL 走 UserRepo.GetByID + agents.go agent 创建走 UserRepo.Create 真迁移 2 path) + release-gate.yml `dl1-no-direct-store` step (baseline=108 锁 internal/api/ 直 import internal/store 文件数 ≤ baseline) + DL-1.3 closure (REG-DL1-001..006 6 🟢 + docs/current/server/data-layer.md 63 行同步). 立场 3 项: ① 4 interface byte-identical 跟蓝图 §4 B + ② factory + DI seam 单源 (handler 不直 import store, CI 守门链第 6 处) + ③ 0 schema / 0 endpoint / 既有实施 byte-identical 不破. 跨 milestone 锁链: BPP-3 #489 PluginFrameDispatcher 模式 (factory + DI 同精神) + reasons.IsValid #496 SSOT 包模式 + AL-3 #324 PresenceTracker 不破 + CV-1 artifacts.go Storage 不破 + CI 守门链第 6 处 (跟 BPP-4 / HB-3 / AP-4-enum / HB-4 / INFRA-3 同模式). ArtifactRepo 留 v1.5 follow-up (store.Artifact model 没抽出). 不在范围: DL-2 events 双流 / DL-3 阈值哨 / SQLite → PG 真切 / EventBus → NATS / 全 handler 一次切 / generic ORM abstraction.
- [ ] **DL-2** events 双流 + retention
- [ ] **DL-3** 阈值哨
- [x] **DL-4** server-side services (PWA Web App Manifest + Web Push subscriptions + VAPID gateway + mention fan-out hook) — must-fix 收口 (PR #490, 一 milestone 一 PR; 7 段 6/7 实施 + 1 ⏸️ AgentTaskNotifier RT-3.2 派生 hook 待 BPP-2.2 plugin 上行落地)

### client-shape
- [x] **CS-1** 三栏 + Artifact 分级 ✅ Phase 4+ client wrapper milestone — 整 milestone 一 PR (4 件套 + 三段全闭): spec `cs-1-spec.md` v0 (3 立场 + 5 反向 grep + 3 段) + stance + acceptance + content-lock. CS-1.1 `lib/use_artifact_panel.ts` (4 态 state machine: closed/drawer/split/fullscreen, open/promoteToSplit/demoteToDrawer/close/setFullscreen API) + `components/AppShell.tsx` (CSS grid 三栏 240px/1fr/380px byte-identical 蓝图 §1.2 + 768px breakpoint mobile 降级 + computeGridColumns helper). CS-1.2 `components/ArtifactDrawer.tsx` (drawer/split/fullscreen 渲染 + close/promote button + drag-handle mouseUp 触发 split 升级). 22 新 vitest PASS (useArtifactPanel 9 + AppShell 7 + ArtifactDrawer 5 + 1 字面常量) + 既有 558 vitest 全 PASS (Wrapper 不破) + typecheck clean. 立场 5 项: ① 三栏 byte-identical 蓝图 §1.2 (240px/1fr/380px + 768px breakpoint) + ② Artifact 4 态 state machine 单源 (useArtifactPanel hook) + ③ closed → split 直接 reject (蓝图"避免自动劈开屏幕"立场, promoteToSplit closed 状态返回 false 反向断言单测) + ④ 0 server / 0 schema / 0 endpoint (Wrapper milestone, 0-server-prod 第 13 处) + ⑤ 复用既有 ChannelView/ArtifactPanel byte-identical. 跨 milestone byte-identical 锁链: CV-1 #347 ArtifactPanel 复用 + CHN-3/13 ChannelList 复用 + RT-4 #562 留 CS-2 + 0-server-prod 系列 13 处承袭 (CV-9..14/DM-5..6/DM-9/CHN-11..12/CS-1). 不在范围: CS-2 顶部团队栏故障 UX (#595) / CS-3 PWA install + Web Push (#598) / HB-2 Tauri / CS-4 IndexedDB (v3+) / Artifact split 比例自定义 (v3+) / 多 artifact 并存 split (v3+). REG-CS1-001..006 全 🟢 active.
- [ ] **CS-2** 故障三态 + 乐观缓存
- [x] **CS-3** PWA install + Web Push UI — 战马D / 飞马 / 烈马 / 野马 一 milestone 一 PR ✅ (按 `docs/implementation/modules/cs-3-spec.md` v0 三立场对齐, wrapper milestone client-only **0 server prod + 0 schema 改 + 0 新 endpoint** 跟 CS-1/CS-2 / CV-9..14 / DM-5..6 / DM-9 / CHN-11..12 同模式承袭): 4 件套 spec/stance/acceptance/content-lock 全闭 + CS-3.1 install prompt SSOT (`lib/cs3-install-prompt.ts::useInstallPrompt()` hook + `BeforeInstallPromptEvent` 拦截 + `appinstalled` event listener + 三态 enum `installable/installed/unavailable` + `display-mode: standalone` matchMedia 检测) + plain language 4-dict (`lib/cs3-permission-labels.ts::PUSH_PERMISSION_LABELS` byte-identical 跟 DL-4 #485 PushPermissionState 4-enum + `INSTALL_BUTTON_LABEL` 字面 byte-identical 跟蓝图 §1.1 `安装 Borgee 桌面应用`) + CS-3.2 UI 组件 (`InstallPromptButton.tsx` installable 时渲染 button + click → useInstallPrompt.prompt() user-gesture only + installed/unavailable 时 return null; `PushSubscribeToggle.tsx` 三态文案 byte-identical (`已开启通知` / `通知已被浏览器拒绝, 请到浏览器设置开启` / `开启通知`) + click → DL-4 `subscribeToPush()` / `unsubscribeFromPush()` byte-identical 复用 + unsupported 时 return null + denied 时 disabled) + 17 vitest PASS (cs3-install-prompt.test.tsx 4 + cs3-permission-labels.test.ts 4 + InstallPromptButton.test.tsx 3 + PushSubscribeToggle.test.tsx 6) + 全套 client 78 files / 545 tests 全 PASS (既有 DL-4 pushSubscribe.ts / manifest.json / sw.js byte-identical 不动). 立场 3 项: ① PWA install user-gesture only (Chrome/Edge 防滥用红线 — auto-prompt 0 hit) + ② Web Push 三态 byte-identical 跟 DL-4 + click-only 触发 (mount-time auto requestPermission 0 hit) + ③ 0-server-prod + 0 schema + 文案 byte-identical 跟蓝图字面 + admin god-mode 不挂 (ADM-0 §1.3 红线). 不在范围: Tauri 桌面壳 (HB stack Go 重审决策放弃) / IndexedDB 乐观缓存 (留 CS-4) / Web Notifications API 自定义渲染 (走 sw.js DL-4) / background sync (蓝图 §1.1 字面承袭) / iOS Safari beforeinstallprompt (留 v2) / per-device 多端管理 (留 v1). 跨 milestone byte-identical 锁链: DL-4 #485 pushSubscribe.ts byte-identical (改 0 行) + manifest.json + sw.js + PushPermissionState 4-enum + 蓝图 client-shape.md §1.1+§1.4 字面 + ADM-0 §1.3 红线. REG-CS3-001..006 共 **6 行 🟢**. docs/current sync (`client/pwa-install.md`).
- [ ] **CS-1** 三栏 + Artifact 分级
- [x] **CS-2** 故障三态 + 4 层 UX 呈现 — 战马D 一 milestone 一 PR ✅ #595 (wrapper client-only, 0 server prod + 0 schema). 4 件套全闭 + CS-2.1 三态 SSOT (`FAILURE_TRI_STATE = ['online', 'error', 'offline']` byte-identical 跟 PresenceDot data-presence + AL-3) + plain language 6-dict (跟 reasons.IsValid #496 + AL-4 #321 byte-identical) + CS-2.2 4 层 UX (`PresenceDot` failure-badge / `FailurePopover` 3 inline button `重连`/`重填 API key`/`查日志` / `FailureBanner` 阈值 ≥2 全 failed OR 核心 > 5min / `FailureCenter` ≥2 团队栏聚合) + repair stub 3 action + 25 vitest + 全套 80 files / 553 tests PASS. 立场: ① 三态拆死 ② 4 层 byte-identical ③ 0-server + 文案 byte-identical. REG-CS2-001..006 6 行 🟢.
- [ ] **CS-3** Mobile PWA (依赖 DL-4)

### infra (元 milestone, Phase 4+ wrapper)
- [x] **INFRA-3** PROGRESS.md 拆分 ✅ 整 milestone 一 PR (4 件套 + 5 子文件实拆 + CI line-budget 守门): spec `infra-3-spec.md` v1 (3 立场 + 5 反向 grep + 3 段) + stance + acceptance + content-lock 不适用 (非 UI). INFRA-3.1 主 PROGRESS.md 359→73 行 (≤100) + 5 phase 子文件实拆 (`progress/phase-{0,1,2,3,4}.md` 27/29/78/63/148 行 含 phase-4.md §更新日志归档 56 行历史 changelog 全量迁) + INFRA-3.2 .gitattributes merge=union 扩 5 子文件 (跟 regression-registry #560 + PROGRESS.md 既有同精神扩) + release-gate.yml 加 step `progress-line-budget` 5 sub-check (单行 ≤200 字符 / 总行 ≤100 / 5 子文件存在 / 主文件 0 checkbox / 跳转锚 ≥5) + INFRA-3.3 closure (REG-INFRA3-001..005 5 🟢). 立场 5 项: ① 主 ≤100 行 + 行 ≤200 字符 (反战马 1500+ 字符 commit 塞主) + ② 5 子文件每 Phase 一文件 (用户拍板 byte-identical, 不裂 changelog/ 子目录) + ③ 翻牌机制不变 (单源切路径, 主文件 0 checkbox) + ④ .gitattributes merge=union 扩 (并发翻牌不撞 conflict) + ⑤ CI 守门链第 5 处 (跟 BPP-4 AST scan / HB-3 dict-isolation / AP-4-enum / HB-4 audit-schema 同模式). 跨 milestone byte-identical 锁链: regression-registry merge=union (#560) + 既有 PROGRESS.md merge=union 扩 5 行 + release-gate.yml step 链第 5 处. 不在范围: changelog/<milestone>.md 外迁 (用户拍板) / 翻牌机制改 / regression-registry 拆 / 模块组目录 (用户拒绝). 0 server / 0 schema / 0 endpoint, **真 infra refactor** (5 子文件实拆 + CI step 加锁 + .gitattributes 5 行扩 — 非 docs-only).
- [x] **TEST-FIX-2** server.New ctx 入参 + 3 处 goroutine leak 修 ✅ 整 milestone 一 PR (test infra refactor, 真 production code 改 — server package signature 改 + ctx-aware shutdown). 起因: 7 PR (#584/#595/#597/#598/#600/#601/#605) `go-test-race` 持续 fail, subagent 诊断真因 = goroutine + DB 连接 leak (RetentionSweeper / HeartbeatRetentionSweeper / rateLimiter cleanup 三处用永不 cancel `context.Background()`, 累积 N 个泄出 ticker 命中 closed DB 写 GORM error log 累积到 120s timeout panic in `TestClosedStoreInternalErrorBranches`). 修法: ① server.New 加 `ctx context.Context` 首参 + Server struct 加 ctx 字段 + ② SetupRoutes 两处 sweeper `.Start(s.ctx)` + ③ Handler `newRateLimiter(s.ctx)` + middleware.cleanup `select ctx.Done()` + ④ 调用方升级 (cmd/collab/main.go production 走 `context.WithCancel(context.Background())`, testutil/server.go 2 处 + server_test.go::testServer 走 `t.Context()` Go 1.25+ 自动 cancel + server_test.go 5 处 newRateLimiter() → newRateLimiter(t.Context())). 实测: TestClosedStoreInternalErrorBranches race PASS 31s, full api 包 race 73.5s ≤120s budget, full server-go race ./... 全 26 packages PASS. 立场 3 项: ① 不 skip 任何 test (11 sub-test 全保留) + ② 不降覆盖度阈值 (cov ≥84%) + ③ 不加 retry/sleep (真修 leak, 反 mask). 跨 PR 解锁: 7 卡住 PR rebase 后 race 自然过. REG-TESTFIX2-001..003 全 🟢 active.
- [x] **TEST-FIX-1** TestClosedStoreInternalErrorBranches race budget fix ✅ 整 milestone 一 PR (test refactor scope 小, spec brief ≤40 行无 4 件套): error_branches_test.go 819-820 行加 `tc := tc` capture + `t.Parallel()` 内 sub-test (跟 outer t.Parallel 同精神). 11 sub-test 各启独立 in-memory store + httptest, 互不依赖, 走 parallel 把 race 总耗时从 28s+ 串行 (CI 4x 慢 = >120s timeout) 降到 ~3s 本地. 实测: 单测 31s→2.56s (12x), 全 api 包 race 68.9s→53s. 立场 3 项: ① 不降覆盖 11 sub-test 全保留 + ② 不 bypass CI race timeout (真修测试结构) + ③ 0 production code 改 (仅 *_test.go 2 行). 起因: CV-15 #592 race 4 跑 fail, 诊断 = #555 AP-5 测试串行+race CI runner 慢累积爆 budget; AP-5 author (zhanma) 死, 由 zhanma-c 修. REG-TESTFIX1-001..003 🟢. **同 PR 嵌入** INFRA-3 follow-up (yema/liema review 抓的 12 patches): yema Phase 4/5/6 编造字样删 + liema batch7 11 patches (REG-AL4-010 ⚪→🟢 + phase-2/3 readiness 4 处 + acceptance-templates ADM-1/2 + CHN-1 + CV-1 + DM-2 7 处 + ADM-2 烈马 row append). PR title 双义跟 progress-split-spec §6.3 同模式.
- [x] **INFRA-4** phase-4.md 历史 changelog 按 milestone 归位 + 单文件 ≤200 行硬限 ✅ 整 milestone 一 PR (INFRA-3 #594 续作 wrapper milestone, **0 server / 0 schema / 0 endpoint** 真 infra refactor — byte-identical 历史迁移 + CI sub-check ⑥): INFRA-4.1 phase-4.md line 100-151 历史 changelog 51 行 (含 init) 按 milestone 归位 phase-{0,1,2,3}.md §更新日志归档 段 (Phase 0 烈马闭环 → phase-0 / Phase 1 烈马收口 → phase-1 / Phase 2 飞马 D1-D9 → phase-2 / Phase 3 多条 CHN-1/CV-1/RT-1/CHN-3/G3.* → phase-3 / cross-Phase 单条留最晚 Phase / 其他 Phase 4+ 模块组留 phase-4) + INFRA-4.2 phase-4.md 151→143 行 (≤200) + INFRA-4.3 release-gate.yml `progress-line-budget` step 加 sub-check ⑥ phase-4.md 单文件 ≤200 行硬限 + INFRA-4.4 closure (REG-INFRA4-001..005 5 🟢). 立场 5 项: ① phase-4.md ≤200 行 (反 wrapper 全堆撑爆) + ② byte-identical 迁移 (不改字面只移位置, 反向 grep 守 0 missing) + ③ phase-{0,1,2,3}.md 各加 §更新日志归档 段 (跟 phase-4.md 同模式) + ④ 跨 Phase 单条 log 选最晚 Phase, 不复制 + ⑤ 不破既有外锚 (phase-2-exit-announcement / acceptance-templates / G3.x signoff). 跨 milestone byte-identical 锁链: INFRA-3 #594 progress-line-budget step 共扩 sub-check ⑥ (CI 守门链第 6 处) + .gitattributes merge=union 5 子文件 (#594 不变) + 历史 changelog 字面 0 改. 不在范围: changelog/ 子目录 / 翻牌机制改 / 主 PROGRESS.md 改 (仅加 INFRA-4 entry 1 行). 0 server / 0 schema / 0 endpoint, **真 infra refactor** (byte-identical 迁移 + CI sub-check ⑥ + REG 5 行).

**G4.audit (滚动)**: 每个模块完成时更新 v0 代码债 audit 行; 全部完成时总表无 TODO — 飞马

---

---

## 更新日志归档 (历史 changelog 全量迁移自 PROGRESS.md §更新日志)

## 更新日志 (本文件)

| 日期 | 更新人 | 变化 |
|------|--------|------|
| (init) | team-lead | 初版打勾 skeleton 建立 |
| 2026-04-27 | team-lead | 4 人 review 后改: 加 CM-5 / AL-2 拆 a/b / RT-1 移 Phase 3 / RT-3 升 ⭐ / DL-4 收口 / 每 Phase audit gate / 签字回滚条款 / 4.1+4.2 双挂规则 |
| 2026-04-27 | team-lead | R2 review 落 20 项: Phase 0 工期 2 周 / G2.5 触发点 stub / 闸 5 覆盖率收紧 / 跨模块 PR 拆契约+实现 / CM-4.4 PR 与签字解耦 / Sessions 多端压测留 AL-3 / thinking subject 挪 BPP-2 / DL-4 头部排序锁 / ADM-2 取消 ⭐ / G3.4 加 chat+artifact 双 tab / G2.4 加 subject 文案 + agent↔agent 口播 / HB-4 测量基准锁 / CV-4 timer 单测 + 5s 不刷新 / blueprint §1.3 加协作语义边界 / presence 路径 internal/presence/contract.go / 签字回滚仅 reopen milestone |
| 2026-04-29 | 战马A (audit follow-up) | PROGRESS Phase 概览 stale flip — line 24 Phase 3 行 vs 真状态 drift (战马D #436 修详细段后, 概览行还说 "8 milestone 4 件套全闭 + CHN-4 wiring 进行中", 跟 #428 closure / #442 G3 evidence / #443 G3.audit skeleton / #403 G3.3 SIGNED 全脱节). 修 2 处:<br>(1) line 24 Phase 3 概览: 状态 `🔄 IN PROGRESS` → `🔄 收口中 (G3 退出闸三签 pending)`; 备注重写为 11 milestone 全闭 + G3.1/3.2/3.3/3.4/audit 各闸状态 (G3.3 ✅ SIGNED #403, G3.1/3.2/3.4 ✅ READY, G3.audit DRAFT #443) + G3 evidence #442 锚.<br>(2) line 25 Phase 4+ 备注: `等 Phase 3` → 起步路径预计 (AL-1b / AL-2a/2b / BPP-* / DL-4 → HB-1/CS-3 / CM-4/CM-5 + AP-3 / RT-3 ⭐), 给 PM 起步预演路径锚.<br>跟 #438 docs PR ≤30 行净改 + #436 audit 模式同根, 概览维度 audit (line 24-25), 详细 milestone 段不动 (#436 已修). |
| 2026-04-29 | 飞马 | AL-1b/AL-2a milestone status flip — line 211 AL-1b ⚪→✅ (三段 #453 schema v=21 + #457 server 5-state+PATCH 405 + #462 client SPA PresenceDot 全闭, 跟 BPP-2 同期 stub) / line 212 AL-2a "三段全闭" stale → ✅ schema+4 件套部分闭 (.1 #447 schema v=20 + #264 acceptance + #454 stance/content-lock 闭, .2/.3 server REST + client SPA AgentConfigPanel 待战马接续, 跟 AL-2b/BPP-2.3 同期合). 偏差 audit 抓出 line 212 stale 比真状态超前 (说 .2/.3 全闭但实际未起), 反 line 211 stale 比真状态滞后 (说 ⚪ 但 #462 已 merged). docs only PR ≤10 行 diff, 不动 §5 totals (留烈马 closure flip 翻牌). 跟 #436/#438 同模式 docs-only Phase 概览 stale flip. |
| 2026-04-29 | 战马D | REFACTOR-REASONS — AL-1a 6 错误原因码 SSOT (一 PR, 飞马 #492 review flag 3 follow-up): 新建 `internal/agent/reasons/reasons.go` SSOT 包 (6 const + `ALL` 切片顺序锁 + `IsValid()` + `All()` 防御性 copy) + 5 单测 (字面顺序 byte-identical / 字典外 9 case reject 含大小写漂移 / state 名混入 / CV-4 stub `runtime_not_registered` 反向) + 4 production 源 import 替换: `internal/agent/state.go` 6 const → re-export `reasons.*` (既有 import-site 不破) / `internal/bpp/task_lifecycle.go::validTaskReasons` map → `validTaskReason()` func 调 `reasons.IsValid` / `internal/bpp/agent_config_ack_dispatcher.go::validAL1aReasons` map → `validAL1aReason()` func 调 `reasons.IsValid`. test 字面**不动** (立场 ⑤ — 跨 milestone byte-identical 锁链 #249/#305/#321/#380/#454/#458/#481/#492 是多源独立断言, dedupe 后才能反向校验 SSOT 漂移). spec brief `docs/implementation/modules/refactor-reasons-spec.md` 62 行 (≤80 严守); REG-RR-001..005 5🟢 全 active; `go test ./...` 全 PASS (无行为级 regression). AL-1 #492 `internal/store/agent_state_log.go::validReasons` 留 follow-up commit (本 PR baseline 是 main, AL-1 在 feat/al-1 stacked, merge 顺序 #492 → 本 PR follow-up). |
| 2026-04-29 | 战马D | DM-3 整 milestone 一 PR 落 (Phase 5 候选 — agent-DM 多端同步): spec brief `dm-3-spec.md` 80 行 (≤80) + stance `dm-3-stance-checklist.md` 63 行 (≤80) + acceptance `dm-3.md` 7 项 ✅ + DM-3.1 server 0 行新增 (3 反约束 grep test PASS — `TestDM31_NoBypassEndpoint` / `_BackfillIncludesDMChannel` / `_NoBypassFrame`) + DM-3.2 client `useDMSync.ts` hook + 5 vitest PASS (cold-start / monotonic / page-reload / corrupt-clamp / multi-device) + DM-3.3 e2e `dm-3-multi-device-sync.spec.ts` REST-driven dual-tab cursor reuse + thinking 5-pattern 反向断言. 立场 5 项: ① cursor 复用 RT-1.3 (不开 /dm/sync 旁路) + ② 多端走 RT-3 fan-out (不开 dm-only frame) + ③ thinking 5-pattern 延伸 (RT-3 #488 byte-identical) + ④ useDMSync hook 复用 useArtifactUpdated/lastSeenCursor 模式 + ⑤ server 0 行新增. 跨 milestone byte-identical 锁链: cursor 跟 RT-1 #290 + AL-2b #481 + CV-* + BPP-3.1 #494 共一根 sequence; useDMSync 跟 CV-1.3 #346 同模式; ≤3s 跟 RT-1.2 #292 同源. 不在范围: e2ee / 跨 org (AP-3) / CHN-3 layout / offline DM (DL-4 已盖). REG-DM3-001..005 占号 (待烈马 PR merge 后翻 🟢). client vitest 36 files / 303 tests 全 PASS, server-go ./internal/api/ DM-3.1 3 tests PASS. |
| 2026-04-29 | 战马D | BPP-6 整 milestone 一 PR (Phase 5 plugin-protocol 真收尾 — plugin cold-start handshake + state re-derive): spec brief `bpp-6-spec.md` 50 行 (≤80) + stance `bpp-6-stance-checklist.md` 69 行 (≤80) + acceptance `bpp-6.md` 40 行 (≤50) + content-lock 不需 (server-only). BPP-6.1 frame schema (envelope 14→15, ColdStartHandshakeFrame 5 字段 byte-identical 字段集**与 ReconnectHandshakeFrame 互斥** — 不含 LastKnownCursor/DisconnectAt/ReconnectAt; spec §0.1 立场守门, BPP-1 #304 reflect lint 自动覆盖) + BPP-6.2 server handler (`internal/bpp/cold_start_handler.go` — agent.Tracker.Clear + AL-1 #492 single-gate AppendAgentStateTransition any→online + reason 复用 `runtime_crashed` 6-dict byte-identical, 不重放历史 frame 反向 BPP-5; AgentStateAppender interface seam, OwnerResolver/AgentErrorClearer 复用 BPP-5 同模式; 6 unit PASS — 字段顺序锁 / direction lock / 字段互斥反断 / 3 valid edges initial/error/offline → online / cross-owner reject + nil-safe ctor / restart count 反向 derive + AST scan ResolveResume 反向 + AST scan cold-start-queue 6 forbidden tokens 0 hit 锁链延伸 BPP-4+BPP-5 第 3 处) + BPP-6.3 closure (REG-BPP6-001..006 6 🟢; 总计 334→340 / active 308→314 / pending 26 不变). 立场 3 项: ① cold-start ≠ reconnect (字段集互斥) + ② state 重新 derive (Tracker.Clear + single-gate + 不重放) + ③ restart count 仅 audit (复用 runtime_crashed, COUNT 反向 derive 不另开列). 跨 milestone byte-identical 锁链: BPP-1 #304 envelope reflect lint + BPP-3 #489 PluginFrameDispatcher 复用 + BPP-5 #503 反向帧 + AL-1 #492 single-gate + REFACTOR-REASONS #496 6-dict 不扩 + AL-1 reason 锁链第 11 处 + ADM-0 §1.3 红线. 不在范围: cross-plugin restart takeover (v2) / SDK backoff / restart rate limit / state graph 不加 cold_starting transient 中间态 (跟 BPP-5 connecting deferred 同精神). server-go internal/bpp + internal/server tests 全 PASS. |
| 2026-04-30 | 战马D | CV-4 v2 整 milestone 一 PR (Phase 5+ — canvas iteration history list + timeline UI 续, **0 schema 改**): spec brief `cv-4-v2-spec.md` 49 行 (≤80) + stance `cv-4-v2-stance-checklist.md` 64 行 (≤80) + acceptance `cv-4-v2.md` 38 行 (≤50) + content-lock 不需 (server 仅加 limit query + state badge label 跟 CV-4 v1 #380 已锁). CV-4.1 v2 server (`internal/api/iterations.go::handleListIterations` — CV-4 v1 #414 endpoint 复用 + 加 `?limit=N` query clamp default 50/max 200; cv4v2ClampLimit helper + ClampCV4V2LimitForTest test-only export; 0 schema 改) + CV-4.2 client (`packages/client/src/components/IterationTimeline.tsx` — DESC list + 4-state badge byte-identical 跟 CV-4 v1 #380 + intent_text + thumbnail 复用 artifact_versions.preview_url CV-3 v2 #517 字段 + onJump callback; 4 vitest PASS — 4 态 badges + thumbnail 复用 + 空状态 + DoesNotWriteOwnCursor 反向断言) + CV-4.3 closure (REG-CV4V2-001..005 5 🟢; 总计 353→358 / active 327→332 / pending 26 不变). 立场 3 项: ① iteration history 复用既有 path (反向 grep `iteration_history_event\|artifact_iteration_log\|iteration_history_table` 0 hit) + ② thumbnail history 不存 (复用 CV-3 v2 #517 preview_url, 反向 grep `thumbnail_history\|preview_history\|version_thumbnail_snapshot` 0 hit) + ③ cursor 复用 RT-1.1 useArtifactUpdated + admin god-mode 不挂 (反向 grep `admin/iterations\|AdminListIterations\|AdminCV4` 在 admin*.go 0 hit + 反向断言 client borgee.cv4.cursor:* sessionStorage 0 hit). 跨 milestone byte-identical 锁链: CV-4 v1 #398/#414/#417 artifact_iterations 表 + CV-1 #348 commits endpoint + RT-1 #290 ArtifactUpdated frame + CV-3 v2 #517 preview_url + AL-2a/BPP-3.2/AL-1/AL-5/DM-4 owner-only 6 处 + ADM-0 §1.3 红线. 不在范围: iteration retry server queue (留 v3) / iteration-level comment (留 v3) / thumbnail 历史快照表 (永久不挂) / schema 改 (CV-4 v1 表已盖) / admin god-mode iteration history / 跨 owner cross-org (留 AP-3) / 独立 cursor 字典. server-go ./internal/api/ TestCV4V2_LimitClamp + NoSchemaChange + AdminGodModeNotMounted PASS + client vitest 4 PASS. |
| 2026-04-30 | 战马D | BPP-8 整 milestone 一 PR (PR #532) (Phase 6 — plugin lifecycle audit log, **0 新表** 复用 admin_actions): spec brief `bpp-8-spec.md` 50 行 (≤80) + stance `bpp-8-stance-checklist.md` 62 行 (≤80) + acceptance `bpp-8.md` 39 行 (≤50) + content-lock 不需 (内部 audit log 无 DOM/UI). BPP-8.1 schema migration v=31 (`internal/migrations/bpp_8_1_admin_actions_plugin_actions.go` admin_actions CHECK 12-step rebuild 6 → 11 项加 5 条 plugin_* 字面: connect/disconnect/reconnect/cold_start/heartbeat_timeout; 跟 CV-3.1/CV-2 v2/AP-2 同模式) + BPP-8.2 server (`internal/bpp/lifecycle_audit.go` LifecycleAuditor interface + AdminActionsLifecycleAuditor 5 method, recordEvent 走 Store.InsertAdminAction with actor='system' + 复用 reasons.NetworkUnreachable/RuntimeCrashed 6-dict byte-identical, AL-1a 锁链第 13 处; `internal/api/bpp_8_lifecycle_list.go` GET /api/v1/agents/{agentId}/lifecycle owner-only ACL + limit clamp default 100/max 500 + non-plugin actions 排除; server.go 加 bpp8Handler register) + BPP-8.3 closure (REG-BPP8-001..006 6 🟢; 总计 364→370 / active 338→344 / pending 26 不变). 立场 3 项: ① 5 事件复用 admin_actions 表 (反向 grep plugin_lifecycle_events 等 0 hit, audit forward-only 锁链第 5 处) + ② reason 复用 6-dict (heartbeat_timeout=NetworkUnreachable/cold_start=RuntimeCrashed, AL-1a 锁链第 13 处) + ③ owner-only 视图 (跟 AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7 7 处同模式; admin god-mode 不挂 ADM-0 §1.3). 跨 milestone byte-identical 锁链: BPP-3 #489 dispatcher + BPP-4 #499 watchdog + BPP-5 #503 reconnect + BPP-6 #522 cold_start + BPP-7 SDK + AL-1 #492 + REFACTOR-REASONS #496 6-dict (锁链第 13 处) + ADM-2.1 #484 admin_actions audit forward-only 锁链第 5 处 + AP-2 #525 sweeper + ADM-0 §1.3 红线 + AST 锁链延伸第 5 处 (BPP-4/5/6/7/8 forbidden tokens 全 0 hit). 不在范围: metrics exporter (v3) / WS push (v3) / cross-org agg (v3) / retention policy (v3) / admin god-mode SDK / 6-dict 扩第 7 / plugin_lifecycle_events 表 / retry queue. 23 unit PASS (migrations 5 + bpp 11 + api 7). |
| 2026-04-30 | 战马D | BPP-7 整 milestone 一 PR (spec PR #529) (Phase 6 起步 — plugin SDK 真接入 in `packages/server-go/sdk/bpp/` 同 module 共享 envelope 0 drift): spec brief `bpp-7-spec.md` 50 行 (≤80) + stance `bpp-7-stance-checklist.md` 69 行 (≤80) + acceptance `bpp-7.md` 43 行 (≤50) + content-lock 不需 (SDK 内部无 DOM/UI). BPP-7.1 SDK Client + Connect (`sdk/bpp/client.go` — Client struct {PluginID, AgentID, conn, lastKnownCursor} + NewClient nil-safe ctor + Connect ws.Dial + ConnectFrame 5 字段 byte-identical 复用 srvbpp.ConnectFrame + AdvanceCursor monotonic 立场承袭 RT-1.3 + SendHeartbeat HeartbeatFrame 6 字段 + Close) + BPP-7.2 Reconnect/ColdStart/HeartbeatLoop/GrantRetry (`sdk/bpp/reconnect.go` — Reconnect 走 BPP-5 ReconnectHandshakeFrame 携 last_known_cursor; ColdStart 走 BPP-6 ColdStartHandshakeFrame **不**携 cursor 字段集互斥反断 + RestartReason=reasons.RuntimeCrashed byte-identical AL-1a 锁链第 12 处; HeartbeatLoop 30s ticker; GrantRetry MaxPermissionRetries=3 + RetryBackoff=30s const re-export from srvbpp byte-identical 跟 BPP-3.2.3 RequestRetryCache 同行为) + BPP-7.3 closure (REG-BPP7-001..006 6 🟢; 总计 358→364 / active 332→338 / pending 26 不变). 立场 3 项: ① SDK frame schema 跟 server byte-identical (SDK 不重定义 frame, AST + reflect 双锁) + ② SDK 同 module 复用 envelope + ws 库 `github.com/coder/websocket` 同源 (best-effort 锁链延伸第 4 处, AST scan forbidden 8 token 0 hit) + ③ BPP-3.2.3 retry + BPP-4 watchdog + BPP-5/6 reconnect/cold-start SDK side 真实施 + reason 复用 6-dict (AL-1a 锁链第 12 处). 跨 milestone byte-identical 锁链: BPP-1 #304 envelope reflect lint + BPP-3 #489 dispatcher (server-only 边界) + BPP-3.2 #498 retry cache (const 复用) + BPP-4 #499 watchdog (30s 周期同源) + BPP-5 #503 reconnect 帧 + BPP-6 #522 cold-start 帧 + AL-1 #492 + REFACTOR-REASONS #496 6-dict (锁链第 12 处) + ADM-0 §1.3 红线. 不在范围: JS/TS port (v2) / auth rotation (v2) / metrics (v3) / structured logger (v2) / plugin marketplace (Phase 7) / SDK 独立定义 frame / admin god-mode SDK / 6-dict 扩第 7 / e2e 真启 server 整链 (留 follow-up). server-go ./sdk/bpp/ 12 unit PASS. |
| 2026-04-29 | 战马D | DM-4 整 milestone 一 PR (spec PR #523) (Phase 5 — agent message edit 多端同步): spec brief `dm-4-spec.md` 49 行 (≤80) + stance `dm-4-stance-checklist.md` 67 行 (≤80) + acceptance `dm-4.md` 38 行 (≤50) + content-lock 不需 (server-only PATCH + client hook). DM-4.1 server PATCH `/api/v1/channels/{channelId}/messages/{messageId}` (`internal/api/dm_4_message_edit.go` — DM-only ACL channel.Type != "dm" → 403 `dm.edit_only_in_dm` + owner-only ACL sender == user → 403 `dm.edit_non_owner_reject` + REG-INV-002 cross-org fail-closed + body schema {content} + events INSERT kind="message_edited" + payload op="edit" 复用 RT-3 fan-out 路径 + Hub.BroadcastEventToChannel 多端覆盖; 6 unit PASS — HappyPath / NonOwnerRejected / NonDMReject / Unauthorized401 / NotFound404 / NoThinkingPatternInBody 反向 grep 5 字面 0 hit) + DM-4.2 client `useDMEdit.ts` hook (`packages/client/src/hooks/useDMEdit.ts` PATCH wrapper via `patchDMMessage` API + optimistic update + isEditing/error 状态; **反向不写** borgee.dm4.cursor:* sessionStorage — cursor 进展全归 useDMSync DM-3 #508; 4 vitest PASS — HappyPath patchDMMessage 真调 + 空 content reject + DoesNotWriteOwnCursor + multi-device useDMSync cursor 不被破坏) + DM-4.3 closure (REG-DM4-001..005 5 🟢; 总计 340→345 post BPP-6 / active 314→319 / pending 26 不变). 立场 3 项: ① RT-3 既有 fan-out 复用 (events INSERT op="edit", 不另起 channel/frame/sequence) + ② edit 是 cursor 子集 (RT-3 fan-out 已建 + useDMSync 已订阅, 不另起 sequence) + ③ thinking 5-pattern 反约束延伸第 3 处 (RT-3 第 1 + DM-3 第 2 + DM-4 第 3, agent edit 是机械修订不暴露 reasoning). 跨 milestone byte-identical 锁链: RT-3 #488 fan-out + DM-3 #508 cursor 复用 RT-1.3 + AL-2a/BPP-3.2/AL-1/AL-5 owner-only 5 处同模式 + ADM-0 §1.3 红线 + REG-INV-002 cross-org fail-closed + RT-1.2 #292 ≤3s latency. 不在范围: edit history audit (v2) / OT-CRDT conflict (v2) / edit window 时间限制 (v2) / edit push 通知 (RT-3.2 follow-up). server-go ./internal/api/ TestDM41 5 unit PASS + client vitest 4 PASS + server.go register dm4EditHandler 真挂. |
| 2026-04-30 | 战马D | AL-7 整 milestone 一 PR (PR #536) (Phase 6 — audit log retention + archive, audit 5 字段链终结收尾 + retention 14d default + admin override; **0 新表** 复用 admin_actions.archived_at): spec brief `al-7-spec.md` 51 行 (≤80) + stance `al-7-stance-checklist.md` 66 行 (≤80) + acceptance `al-7.md` 41 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3). AL-7.1 schema migration v=33 (`internal/migrations/al_7_1_admin_actions_archived_at.go` ALTER admin_actions ADD COLUMN archived_at INTEGER NULL 跟 AP-1.1 expires_at + AP-3.1 org_id + AP-2.1 revoked_at 跨四 milestone 同模式 + sparse idx WHERE archived_at IS NOT NULL byte-identical 跟 AP-2.1 + admin_actions CHECK 12-step rebuild 11→12 项加 'audit_retention_override' 字面跟 CV-3.1/CV-2 v2/AP-2/BPP-8 同模式; v=33 sequencing post-CV-6 v=32) + AL-7.2 server (`internal/auth/audit_retention_sweeper.go` RetentionSweeper 1h ticker + ctx-aware shutdown + RunOnce 同步入口跟 AP-2 ExpiresSweeper #525 同模式 nil-safe; UPDATE archived_at = now WHERE created_at < cutoff AND archived_at IS NULL **不真删** forward-only soft-archive; RetentionDays=14 / RetentionMinDays=1 / RetentionMaxDays=365 const 单源; SweeperReason=reasons.Unknown byte-identical AL-1a 锁链第 15 处 + `internal/api/al_7_audit_retention_override.go` POST /admin-api/v1/audit-retention/override admin-rail only 写 admin_actions audit row action='audit_retention_override' ADM-0 §1.3 红线; server.go 加 al7RetentionHandler.RegisterAdminRoutes + RetentionSweeper.Start) + AL-7.3 closure (REG-AL7-001..006 6 🟢; 总计 370→376 / active 344→350 / pending 26 不变). 立场 3 项: ① retention 走 admin_actions.archived_at 列 + sweeper 不裂表/不真删 (反向 grep `audit_archive_table\|audit_history_log\|al7_archive_log` 0 hit + `DELETE FROM admin_actions` 0 hit, audit forward-only 锁链第 6 处) + ② admin override 复用 audit + reason 复用 6-dict (AL-1a 锁链第 15 处, HB-3 v2 #14 承袭不漂) + ③ admin-rail only ADM-0 §1.3 红线. 跨 milestone byte-identical 锁链: AP-2 #525 ExpiresSweeper time.Ticker 同模式 + AP-1.1/AP-3.1/AP-2.1 ALTER ADD COLUMN nullable 跨四 milestone + ADM-2.1 #484 admin_actions audit forward-only 锁链第 6 处 + BPP-4/5/6/7/8 + HB-3 v2 audit/best-effort 锁链 + AL-1 #492 + REFACTOR-REASONS #496 6-dict (AL-1a reason 锁链第 15 处) + ADM-0 §1.3 红线 + AST 锁链延伸第 7 处 (BPP-4+5+6+7+8 + HB-3 v2 + AL-7 forbidden tokens `pendingRetentionQueue / retentionRetryQueue / deadLetterRetention` 全 0 hit) + cron 框架反向 grep `robfig/cron / gocron` 0 hit 跟 AP-2 立场承袭. 不在范围: admin dashboard UI (v3) / runtime hot-mutate retention 窗口 (v0 仅留痕) / Prometheus metrics exporter (v3) / archive 数据搬移到 cold storage (v3) / 跨 org retention policy (v3) / admin god-mode override 历史回看 (admin-api/v1/audit-log 已盖) / 另开 audit_archive 表 / retention queue. 18 unit PASS (migrations 7 + auth 8 含 AST scan + api 3 含 ClampsRetention 6 sub-case). |
| 2026-04-30 | 战马D | AL-8 整 milestone 一 PR (PR #538) (Phase 6 audit retention 互补 — admin 看 active vs archived audit; 既有 GET /admin-api/v1/audit-log filter 加 since/until/archived/actions, **0 schema / 0 新 endpoint** 复用 AL-7 #533 archived_at + ADM-2.2 既有 path): spec brief `al-8-spec.md` 79 行 (≤80) + stance `al-8-stance-checklist.md` 69 行 (≤80) + acceptance `al-8.md` 38 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3). AL-8.1 schema 0 行 (复用 AL-7.1 archived_at + sparse idx) + AL-8.2 server filter 扩 (`internal/store/admin_actions.go::AdminActionListFilters` 加 Since/Until/ArchivedView/Actions 顺位 append 既有 3 字段 byte-identical 不动 + ListAdminActionsForAdmin Where 链按 nil-safe 加 since/until BETWEEN + archived 三态 switch + actions IN slice; `internal/api/adm_2_2_endpoints.go::handleAdminAuditLog` 加 query parse since/until int64 ms epoch + archivedView 三态 + actions 多值 r.URL.Query()["action"] Slice + 错码字面单源 audit_log.{time_range_invalid/time_range_inverted/archived_view_invalid}) + AL-8.3 closure (REG-AL8-001..006 6 🟢; 总计 376→382 / active 350→356 / pending 26 不变). 立场 3 项: ① 不另起 endpoint (additive filter 既有 GET 路径单源) + ② admin-rail only ADM-0 §1.3 + ③ archived 三态 (active 默认走 archived_at IS NULL 跟 AL-7.1 sparse idx 反向同源; archived 走 idx_admin_actions_archived_at 现网零开销; all 无 WHERE). 跨 milestone byte-identical 锁链: ADM-2.2 #484 既有 GET path + 3-filter byte-identical 不动 + AL-7 #533 archived_at 列 + sparse idx 反向同源 + ADM-0 §1.3 红线 admin-rail only + AL-1a reason 锁链 AL-8 = 第 16 处 (AL-7 #15 承袭, 复用 reasons.Unknown 跟 AL-7 SweeperReason 同源) + audit 5 字段链第 8 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8) + AST 锁链延伸第 8 处 (BPP-4+5+6+7+8 + HB-3 v2 + AL-7 + AL-8 forbidden tokens `pendingAuditQuery / auditQueryRetryQueue / deadLetterAuditQuery` 全 0 hit) + parseLimit helper default 100/max 500 复用 ADM-2.2 同源. 不在范围: admin SPA dashboard UI (留 v3) / audit export CSV/NDJSON (留 v3) / audit metadata JSON path search (留 v4) / Prometheus exporter (留 v3) / audit cross-org filter (留 v3 跟 AP-3 同期) / user-rail audit search (永久不挂 ADM-0 §1.3) / audit row write/update/delete (forward-only AL-7 锁) / audit retention runtime hot-mutate (留 v3). 14 unit PASS — TestAL81_* 3 (NoSchemaChange / NoNewEndpoint / NoUserRailAuditLog) + TestAL82_* 10 (ArchivedView 4 sub-case + TimeRange 3 + Actions 2 + RejectsUserRail + ReasonChain_NotExpanded) + TestAL83_NoAuditQueryQueue 1; ADM-2.2 既有 全套 不破. server-go ./... 全 25 packages 全绿. |
| 2026-04-30 | 战马E | CV-13 整 milestone 一 PR (Phase 5+ — artifact comment quote / reference 视觉块, **0 server production code** 复用 reply_to_id CV-8 #441; 跟 CV-9..12 / DM-5 / AP-4 / AP-5 0-server client-only 同模式): spec brief `cv-13-spec.md` 67 行 (≤80) + stance `cv-13-stance-checklist.md` 70 行 + acceptance `cv-13.md` 38 行 + content-lock `cv-13-content-lock.md` (文案 + DOM SSOT 单源). CV-13.1 server 反向断言 (git diff origin/main -- packages/server-go/ 0 production lines + 反向 grep `quote_event\|comment_quote_table\|cv13.*server` 在 internal/ 0 hit; 复用 messages.reply_to_id CV-8 #441 既有列 + 既有 fetchMessages list 返字段) + CV-13.2 client (`packages/client/src/components/QuotedCommentBlock.tsx` 新组件 props-driven 渲染 — quotedMessage Message|null + collapse toggle + 200-char truncate + missing fallback "(原消息已删除)" byte-identical; ArtifactCommentItem.tsx 加 props quotedMessage wire ≤10 行 — 父组件从 messages list 内存 cache lookup parent reply_to_id; 7 vitest PASS — HappyPath + MissingFallback + DeletedFallback + CollapseToggle + Truncate200 + DOMAttrs + NoStorageWrite) + CV-13.3 closure (REG-CV13-001..005 5 🟢; 总计 382→387 / active 356→361 / pending 26 不变). 立场 5 项: ① 0 server production code (反向断言 git diff 0 行) + ② quote 块从内存 cache 渲染 (props-driven 不 import api/fetch*) + ③ thinking 5-pattern 反约束锁链第 9 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13) + ④ 文案 byte-identical 6 字面 (> quote prefix + @ author + 展开/收起 collapse + (原消息已删除) fallback + … ellipsis + 200 chars truncate length; content-lock SSOT) + ⑤ DOM 4 data-attr (data-cv13-quoted-block + -author + -id + -collapsed). 跨 milestone byte-identical 锁链: CV-7 #535 ArtifactCommentItem 续 + CV-8 #441 reply_to_id 列 SSOT + CV-9 #539 mention preview 内存 cache 同精神 + CV-11 #543 markdown 不入 quote (纯文本截断) + thinking 5-pattern 锁链第 9 处 + ADM-0 §1.3 红线 + DM-5/AP-4/AP-5 0-server client-only 同模式. 不在范围: quote chain 递归 (留 v2) / markdown 富文本 quote (CV-11 markdown 仅对 body) / admin god-mode quote audit (永久不挂) / schema 改 / 新 endpoint / quote 跨 channel / quote 编辑历史 (forward-only 同精神) / e2e (deferred 复用 CV-7 e2e harness, 留 follow-up). vitest 47 files / 397 tests 全 PASS + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行验证. |
| 2026-04-30 | 战马D | DM-6 整 milestone 一 PR (Phase 6 DM thread reply UI; **0 schema 改 / 0 server production code** — messages.reply_to_id 既有列 + POST /channels/{id}/messages 既有接受 reply_to_id byte-identical 不动. 跟 DM-5 #549 / CV-7 #535 / CV-9..12 一系列 0-server 模式承袭): spec brief `dm-6-spec.md` 75 行 (≤80) + stance `dm-6-stance-checklist.md` 59 行 (≤80) + acceptance `dm-6.md` 45 行 (≤50) + content-lock `dm-6-content-lock.md` 94 行. DM-6.1 server **0 production code** (`internal/api/dm_6_thread_test.go` _test.go ONLY: NoSchemaChange + NoServerProductionCode 双反向 grep + ReplyToIDColumnExists PRAGMA 反断 + DMThreadReply_HappyPath 走 既有 POST path + NoThinkingPatternInProduction 5-pattern 锁链第 9 处 + NoDMThreadQueue AST 锁链 #15) + DM-6.2 client (`packages/client/src/components/DMThread.tsx` 折叠 toggle + reply input + submit DOM byte-identical 跟 content-lock §1; toggle 文案 `▼ 隐藏 N 条回复` / `▶ 显示 N 条回复` byte-identical + N 计数动态; placeholder `回复...` 2 字 + submit `发送` 2 字; 空 thread null 不渲染; thread depth 1 层强制 — reply 行内不渲染 sub-thread toggle) + DM-6.3 closure (REG-DM6-001..006 6 🟢; §5 dup 总计 fix → 单一 405/379/26). 立场 3 项 + 3 边界: ① 0 schema / 0 server prod (跟 DM-5/CV-7/CV-9..12 同模式) + ② owner-only ACL 锁链第 18 处 (CHN-9 #17 承袭) + admin god-mode 不挂 + ③ thinking 5-pattern 锁链第 9 处 (DM-5 第 8 处承袭, RT-3+DM-3+DM-4+DM-5 反向断言延伸). 跨 milestone byte-identical 锁链: CHN-1 既有 messages.reply_to_id 列复用 (0 schema 改) + DM-3 #508 既有 channel.member ACL byte-identical 不动 + DM-5 #549 + CV-7 #535 + CV-9..12 0-server 模式承袭 + RT-3 #488 + DM-3/4/5 thinking 5-pattern 反向断言承袭 (锁链第 9 处) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 18 处一致 + audit 5 字段链第 15 处 + AST 锁链延伸第 15 处. 不在范围: thread depth >1 (永久不挂) / thread reply push (留 v3) / admin god-mode thread override (永久不挂) / thread reply audit (复用既有 messages) / cross-channel thread (永久不挂). 13 unit PASS — server-go 6 (TestDM61_NoSchemaChange + TestDM61_NoServerProductionCode + TestDM61_ReplyToIDColumnExists + TestDM61_DMThreadReply_HappyPath + TestDM61_NoThinkingPatternInProduction + TestDM63_NoDMThreadQueue) + client vitest 7 (DMThread.test.tsx: 折叠态/展开态文案 byte-identical + N 计数动态 + reply input + submit DOM byte-identical + 空 thread null + 同义词反向 + thread depth 1 层强制 + submit callback). server-go ./... 25 packages 全绿 + client ./... 47 files / 397 tests 全绿 (DM-3/DM-5/CV-7/CV-9..12 既有 unit 不破). |
| 2026-04-30 | 战马D | HB-3 v2 整 milestone 一 PR (Phase 6 — heartbeat decay 三档 derive + cross-bucket audit + owner-only 视图, **0 schema 改**; 命名拆死跟 #507 host_grants 不交): spec brief `hb-3-v2-spec.md` 53 行 (≤80) + stance `hb-3-v2-stance-checklist.md` 66 行 (≤80) + acceptance `hb-3-v2.md` 42 行 (≤50) + content-lock 不需 (owner-only API 无 DOM/UI). HB-3 v2.1 `internal/bpp/heartbeat_decay.go` (DecayState enum 3 字面 fresh/stale/dead + DeriveDecayState 纯 fn + StaleThreshold=30s/DeadThreshold=60s const byte-identical 跟 BPP-4 watchdog + BPP-7 SDK HeartbeatInterval 同源 + IsCrossBucketTransition helper) + HB-3 v2.2 BucketAuditTrigger (HeartbeatTimeoutAuditor interface 解耦 BPP-8 PR 顺序; cross-bucket 调 RecordHeartbeatTimeout, 同档 no-op) + `internal/api/hb_3_v2_decay_list.go` GET /api/v1/agents/{agentId}/heartbeat-decay owner-only ACL (响应 `{agent_id, state, age_ms}`, 不漏 raw last_heartbeat_at; 复用 agent_runtimes.last_heartbeat_at 0 schema 改); server.go 加 hb3v2Handler register + HB-3 v2.3 closure (REG-HB3V2-001..005 5 🟢; 总计 364→369 / active 338→343 / pending 26 不变). 立场 3 项: ① decay 三档 derive 0 schema 改 (反向 grep heartbeat_decay_table 等 0 hit) + ② 复用 BPP-8 RecordHeartbeatTimeout 不另开 RecordHeartbeatStale 旁路 (AL-1a 锁链第 14 处, reasons.NetworkUnreachable 复用) + ③ owner-only 视图 (锁链第 9 处; admin god-mode 不挂 ADM-0 §1.3). 跨 milestone byte-identical 锁链: BPP-4 #499 watchdog (30s threshold) + BPP-7 SDK (HeartbeatInterval) + BPP-8 #532 (RecordHeartbeatTimeout) + AL-1 #492 + REFACTOR-REASONS #496 6-dict (锁链第 14 处) + ADM-2.1/AP-2 audit forward-only + ADM-0 §1.3 红线 + AST 锁链延伸第 6 处 (BPP-4+5+6+7+8 + HB-3 v2 forbidden tokens 全 0 hit). 不在范围: decay UI 时间轴 (v3) / metrics (v3) / owner override 阈值 (v3) / cross-org agg (v3 跟 AP-3) / admin god-mode / 6-dict 扩第 7 / 另开 heartbeat_decay 表 / RecordHeartbeatStale 旁路. 16 unit PASS (bpp 8 + api 7 + AdminGodMode 1). |
| 2026-04-30 | 战马E | AP-5 整 milestone 一 PR (Phase 5+ messages endpoints ACL audit, post-removal fail-closed gap 闭合, 跟 AP-4 #551 reactions ACL 同模式扩展; **0 schema 改 + 0 新错码 + 0 新 endpoint**): spec brief `ap-5-spec.md` 47 行 (≤80) + stance + acceptance (4件套 content-lock 不需 server-only). AP-5.1 server (`internal/api/messages.go::handleUpdateMessage` PUT + `handleDeleteMessage` DELETE + `internal/api/dm_4_message_edit.go::handleEdit` PATCH 各加 6 行 channel-member ACL gate `Store.IsChannelMember(existing.ChannelID, user.ID) && Store.CanAccessChannel(existing.ChannelID, user.ID)` 跟 AP-4 reactions 同 helper 复用 + cross-org check 仍先于 channel-member 404 守 TestCrossOrgRead403 lock byte-identical) + AP-5.2 unit (`internal/api/messages_acl_audit_test.go` 5 unit PASS — TestAP5_PutMessage_PostRemovalReject + TestAP5_DeleteMessage_PostRemovalReject + TestAP5_PatchDM_PostRemovalReject + TestAP5_NonSenderMember_403 反向 sanity + TestAP5_Member_PutDelete_OK 反向 sanity) + e2e (`packages/e2e/tests/ap-5-messages-acl-matrix.spec.ts` 4 case — PUT/DELETE/PATCH post-removal + cross-org sanity) + AP-5.3 closure (REG-AP5-001..005 5 🟢 + REG-DM5-005 / REG-AP4-005 cross-link gap 闭合三轨成对). 立场 4 项: ① 3 handler IsChannelMember + CanAccessChannel gate 复用 AP-4 同 helper + ② cross-org 403 仍先于 channel-member 404 (REG-INV-002 fail-closed) + ③ owner-only ACL 既有不破 (sender_id 403 跟 AL-2a/BPP-3.2/AL-1/AL-5 owner-only 5 处同模式) + ④ 0 schema 改 + 0 新错码 (复用 messages.go::handleCreateMessage 既有 "Channel not found" byte-identical 字符 fail-closed) + 反约束: admin god-mode 不挂 ADM-0 §1.3 红线 + audit 不另起 (复用既有 events 表 + soft-delete 路径). 跨 milestone byte-identical 锁链: AP-4 #551 reactions ACL gap 闭合 (双轨 ACL 收紧成对) + DM-5 #549 §3.3 反向断 REG-DM5-005 gap 锚 + messages.go 既有 fail-closed 字符 + AL-2a/BPP-3.2/AL-1/AL-5 owner-only 5 处 + ADM-0 §1.3 红线 + REG-INV-002 fail-closed. 不在范围: 抗 enumeration message_id (留 v2) / admin god-mode reactions visibility (永久不挂 ADM-0 §1.3) / rate-limit (留 spam Phase) / 跨 org 反向断 (复用 store.CrossOrg 既有). 副作用 — `error_branches_test.go::TestChannelsMessagesWorkspaceAdditionalBranches` update/delete-message-forbidden 翻 403 → 404 因 post-leave-public 状态 channel-member gate 先 fire (gap fix 真生效, 不是 regression — 注释解释). server-go ./... 全 25 packages 全绿 + AP-5_* 5 unit PASS. |
| 2026-04-30 | 战马D | HB-5 整 milestone 一 PR (Phase 6 — heartbeat history retention 收尾, agent_state_log 长期累积无界增长复用 AL-7 #533 archived_at 模式; **0 新表 / 0 新 admin_actions enum** 复用 AL-7 既有 'audit_retention_override' action + metadata.target='heartbeat' 字面区分; **命名说明**: HB-4 #524 已被 release-gate 占, 顺位 HB-5): spec brief `hb-5-spec.md` 79 行 (≤80) + stance `hb-5-stance-checklist.md` 66 行 (≤80) + acceptance `hb-5.md` 41 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3). HB-5.1 schema migration v=34 (`internal/migrations/hb_5_1_agent_state_log_archived_at.go` ALTER agent_state_log ADD COLUMN archived_at INTEGER NULL 跨五 milestone 同模式 + sparse idx 跟 AL-7.1 byte-identical; 不挂 admin_actions CHECK enum 改 — AL-7.1 12 项 byte-identical 不动) + HB-5.2 server (`internal/auth/heartbeat_retention_sweeper.go` HeartbeatRetentionSweeper 跟 AL-7 RetentionSweeper #533 + AP-2 ExpiresSweeper #525 同模式 nil-safe; UPDATE agent_state_log SET archived_at = now WHERE ts < cutoff AND archived_at IS NULL 不真删; HeartbeatRetentionDays=30 / HeartbeatTargetLabel='heartbeat' / HeartbeatSweeperReason=reasons.Unknown const 单源; `internal/api/hb_5_heartbeat_retention_override.go` POST /admin-api/v1/heartbeat-retention/override admin-rail; body retention_days int 1..365 clamp 复用 auth.RetentionMinDays/MaxDays 跟 AL-7 同源; 写 admin_actions row action='audit_retention_override' 复用 AL-7 既有 ActionAuditRetentionOverride const + metadata.target='heartbeat' 字面区分; server.go 加 hb5HeartbeatRetentionHandler.RegisterAdminRoutes + HeartbeatRetentionSweeper.Start) + HB-5.3 closure (REG-HB5-001..006 6 🟢; 总计 382→388 / active 356→362 / pending 26 不变). 立场 3 项: ① retention 走 agent_state_log.archived_at + sweeper 不裂表/不真删 + ② admin override 复用 AL-7 既有 action 不挂 admin_actions CHECK 第 13 项 enum (反向 grep `'heartbeat_retention_override'` 0 hit) + ③ admin-rail only ADM-0 §1.3 红线 + reason 复用 reasons.Unknown 跟 AL-7 SweeperReason 同源 (AL-1a 锁链第 17 处). 跨 milestone byte-identical 锁链: AL-7 #533 archived_at + sparse idx 反向同源 + ALTER ADD COLUMN nullable 跨五 milestone (AP-1.1+AP-3.1+AP-2.1+AL-7.1+HB-5.1) + AL-1 #492 agent_state_log forward-only + AL-7 RetentionSweeper #533 (time.Ticker 同模式 nil-safe) + ADM-2.1 #484 admin_actions audit (5 字段链第 9 处) + AL-1a reason 锁链 HB-5 = 第 17 处 (AL-7 #15 + AL-8 #16 承袭) + ADM-0 §1.3 红线 + AST 锁链延伸第 9 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7 + AL-8 + HB-5 forbidden tokens 全 0 hit) + admin_actions CHECK 12 项 byte-identical 跟 AL-7.1 不动 + auth.RetentionMinDays/MaxDays / ActionAuditRetentionOverride const 复用 AL-7 同源. 不在范围: admin SPA dashboard heartbeat retention list 视图 (留 v3) / agent_state_log archived 行搬移到 cold storage (留 v3) / 跨 org heartbeat retention 策略 (留 v3 跟 AP-3 同期) / user-rail heartbeat retention 调控 (永久不挂 ADM-0 §1.3) / agent_state_log row write/update/delete (forward-only AL-1 锁) / heartbeat history export CSV/NDJSON (留 v3). 17 unit PASS — migrations 5 (HB51_AddsArchivedAtColumn / _HasSparseIdx / _VersionIs34 / _Idempotent / _NoAdminActionsEnumDrift) + auth 9 (HB52_RunOnceArchivesExpired / _RunOnceSoftArchiveNotRealDelete / _RunOnceIdempotent / _StartCtxShutdown / _NilSafeCtor / _SweeperReason_ByteIdentical / _NoCronFrameworkImport + HB53_NoHeartbeatRetentionQueue) + api 3 (HB52_OverrideEndpointWritesAudit / _OverrideRejectsUserRail / _OverrideClampsRetention 含 6 sub-case). server-go ./... 25 packages 全绿. |
| 2026-04-30 | 战马D | CHN-7 整 milestone 一 PR (Phase 6 channel mute/notification 静默 — per-user mute; **0 schema 改 / 0 新 admin_actions enum** — user_channel_layout.collapsed INTEGER 列 bitmap 编码: bit 0 (=1) = 折叠 (CHN-3 既有), bit 1 (=2) = 静音 (CHN-7 新增) + MuteBit=2 const 双向锁 server+client byte-identical 跟 CHN-6 #544 PinThreshold 同模式): spec brief `chn-7-spec.md` 80 行 (≤80) + stance `chn-7-stance-checklist.md` 65 行 (≤80) + acceptance `chn-7.md` 48 行 (≤50) + content-lock `chn-7-content-lock.md` 78 行 (UI 必锁 — mute button + `已静音` indicator + 同义词反向 + MuteBit 双向锁). CHN-7.1 server (`internal/store/queries.go::SetMuteBit(userID, channelID, bitMask, set)` UPSERT collapsed bitmap toggle 不动 position; `IsMutedForUser(userID, channelID, muteBit)` 谓词单源走 collapsed & MuteBit; `internal/api/chn_7_mute.go` POST + DELETE /api/v1/channels/{channelId}/mute + MuteBit=2 const + IsMuted 谓词单源 + DM 反 layout.dm_not_grouped byte-identical + non-member 403 + 401; server.go 加 RegisterCHN7Routes user-rail authMw) + CHN-7.2 client (`packages/client/src/lib/mute.ts` MUTE_BIT=2 字面 byte-identical 跟 server + isMuted 谓词单源; `lib/api.ts::muteChannel(id, muted)` 单源 method=POST/DELETE; `components/MuteButton.tsx` toggle 文案 byte-identical `静音`/`取消静音` + data-action='mute'/'unmute' + 同义词反向 reject; `components/MutedChannelIndicator.tsx` `<span>` + `data-testid='muted-channel-indicator'` + `🔕 已静音` 文案 byte-identical + muted=false return null) + CHN-7.3 closure (REG-CHN7-001..006 6 🟢; 总计 §5 fix → 405/379/26 sum=actual rows, CHN-7 +6 净). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 collapsed INTEGER bitmap + MuteBit=2 双向锁) + ② owner-only ACL 锁链第 15 处 + admin god-mode 不挂 PATCH/POST/DELETE ADM-0 §1.3 红线 + ③ mute **不 drop messages** best-effort — CreateMessage/RT-3 fan-out/WS frame 全 byte-identical 不动, mute 仅 DL-4 push notifier skip (反向 grep `mute.*skip.*broadcast\|mute.*drop.*message` 在 internal/ws+messages*.go 0 hit). 跨 milestone byte-identical 锁链: CHN-3.1 #410 user_channel_layout 列复用 (0 schema 改 + bitmap bit 0/1 拆死) + CHN-3.2 #412 既有 PUT /me/layout endpoint byte-identical 不动 + CHN-6 #544 PinThreshold 双向锁模式承袭 (MuteBit=2 同源) + DL-4 push gateway 立场承袭 (best-effort skip) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 15 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5/CHN-6/CHN-7) + audit 5 字段链第 12 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8+HB-5+CHN-5+CHN-6+CHN-7) + AST 锁链延伸第 12 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8+HB-5+CHN-5+CHN-6+CHN-7 forbidden tokens 全 0 hit) + MuteBit=2 双向锁 (server const + client const byte-identical). 不在范围: 自动 mute 时长 muted_until 过期解锁 (留 v3 — 永久 mute v1) / 跨设备 mute 状态推送 RT-3 frame (留 v3) / admin god-mode mute override (永久不挂 ADM-0 §1.3) / DM mute (永久不挂 — DM 走 CHN-4 dm-only) / 全局静默 DnD 模式 (留 user-settings v3, 跟 mute 不同维度). 18 unit PASS — server-go 9 (TestCHN71_NoSchemaChange + TestCHN71_MuteChannel_HappyPath/NonMemberRejected/Unauthorized + TestCHN71_UnmuteChannel_HappyPath/Idempotent/PreservesCollapsedBit + TestCHN71_MuteBit_ByteIdentical + TestCHN71_NoAdminMutePath + TestCHN71_MuteDoesNotDropMessages + TestCHN73_NoChannelMuteQueue) + client vitest 9 (MuteButton.test.tsx 5: 文案 mute/unmute / click→API+onChange / 同义词反向 user-visible + MutedChannelIndicator.test.tsx 4: 已静音 文案 / muted=false null / MuteBit 双向锁 / 同义词反向). server-go ./... 25 packages 全绿 + client ./... 46 files / 389 tests 全绿 (CHN-3.2/CHN-5/CHN-6 既有 unit 不破). |
| 2026-04-30 | 战马D | DM-7 整 milestone 一 PR (Phase 6 — DM message edit history audit, DM-4 #553 既有 PATCH 续作 — 给 edit 一份历史回放; **复用 messages 表 + ALTER ADD nullable 跨七 milestone**): spec brief `dm-7-spec.md` 80 行 (≤80) + stance `dm-7-stance-checklist.md` 64 行 (≤80) + acceptance `dm-7.md` 45 行 (≤50) + content-lock `dm-7-content-lock.md` 85 行 (EditHistoryModal `编辑历史` 4 字 + `共 N 次编辑` + RFC3339 + JSON shape `{old_content, ts, reason}` byte-identical). DM-7.1 schema migration v=34 (`internal/migrations/dm_7_1_messages_edit_history.go` ALTER messages ADD COLUMN edit_history TEXT NULL — 跟 AP-1.1+AP-3.1+AP-2.1+AL-7.1+HB-5.1+CHN-5.1 跨七 milestone ALTER ADD nullable 同模式; NULL = 无历史 / 老消息 byte-identical / 现网行为零变 + idempotent guard hasColumn pre-check) + DM-7.2 server (`internal/store/queries.go::UpdateMessage` SSOT wrap — 改 content 前 SELECT old content + edit_history + JSON marshal append `[{old_content, ts, reason='unknown'}]` + idempotent same-content skip; reason 复用 AL-1a `reasons.Unknown='unknown'` byte-identical 跟 AL-7 SweeperReason / HB-5 HeartbeatSweeperReason 同源 — AL-1a 锁链第 18 处; `internal/api/dm_7_edit_history.go` GET /api/v1/channels/{channelId}/messages/{messageId}/edit-history user-rail sender-only ACL + GET /admin-api/v1/messages/{messageId}/edit-history admin readonly admin-rail — owner-only 锁链第 19 处; admin god-mode 不挂 PATCH/DELETE/PUT ADM-0 §1.3 红线; DM-4 既有 dm_4_message_edit.go production byte-identical 不变, history append 走 store layer wrap UpdateMessage 单源调用方 unchanged) + DM-7.3 client (`packages/client/src/lib/api.ts::getEditHistory` thin wrapper + `packages/client/src/components/EditHistoryModal.tsx` — `编辑历史` 4 字 title + `共 N 次编辑` + RFC3339 时间戳 byte-identical + 同义词反向 reject `changes/revisions/revs/版本/修订/变更/回退` + 空 history null + 加载失败 `加载编辑历史失败`; 5 vitest PASS title+count + RFC3339 + 空 null + 加载失败 + 同义词反向 grep) + DM-7.4 closure (REG-DM7-001..006 6 🟢; 总计 394→400 / active 368→374 / pending 26 不变). 立场 3 项: ① schema ALTER ADD nullable 跨七 milestone 同模式 (NULL = 无历史 / 老消息 byte-identical) + ② UpdateMessage SSOT history append (反向 grep `inline.*UPDATE.*messages.*content` 0 hit production, DM-4 既有 PATCH path 不变) + ③ owner-only sender + admin readonly (admin god-mode 不挂 PATCH/DELETE/PUT 反向 grep 0 hit ADM-0 §1.3 红线). 跨 milestone byte-identical 锁链: AP-1.1+AP-3.1+AP-2.1+AL-7.1+HB-5.1+CHN-5.1+DM-7.1 ALTER ADD nullable 跨七 milestone + AL-1a reason 锁链第 18 处 (HB-5 第 17 + AL-8 第 16 + AL-7 第 15 承袭) + AL-7/HB-5 reasons.Unknown byte-identical + owner-only ACL 锁链第 19 处 + AST 锁链延伸第 16 处 forbidden 3 token (`pendingEditHistory/editHistoryQueue/deadLetterEditHistory`) 0 hit + DM-4 #553 既有 PATCH path byte-identical + ADM-0 §1.3 红线. 不在范围: edit history 全文搜 (留 v3 跟 CV-6 FTS 同期) / edit history retention sweeper (留 v3 跟 AL-7 retention 同精神) / admin god-mode edit override (永久不挂 ADM-0 §1.3) / diff syntax highlight (留 v3 client only) / delete history (永久不挂 forward-only 跟 AL-1 同精神). 13 server unit PASS (migrations 3 + api 10) + 全 server suite 25 packages 全绿 + 5 client EditHistoryModal vitest PASS + 全 client 47 file/395 case 全绿. |
| 2026-04-30 | 战马D | CHN-9 整 milestone 一 PR (Phase 6 channel privacy 三态 — `🔒 仅创建者` / `👥 成员可见` / `🌐 组织内可见`; **0 schema 改 / 0 新 admin_actions enum** — channels.visibility TEXT 列复用 CHN-1.1 #267 既有, 加第 3 enum `creator_only` 跟既有 `private`/`public` 共三态. Visibility 三向锁 server const + client const + DB 字面 byte-identical 跟 CHN-7 #550 MuteBit 双向锁 / CHN-8 #552 NotifPref 三向锁同模式扩展): spec brief `chn-9-spec.md` 79 行 (≤80) + stance `chn-9-stance-checklist.md` 64 行 (≤80) + acceptance `chn-9.md` 48 行 (≤50) + content-lock `chn-9-content-lock.md` 80 行 (UI 必锁 — VisibilityBadge 三态 DOM + 文案 + 同义词反向 + Visibility 三向锁). CHN-9.1 server (`internal/api/chn_9_visibility.go` const VisibilityCreatorOnly/Members/OrgPublic + IsValidVisibility 谓词 + VisibilityRejectMessage 单源; `internal/api/channels.go::handleCreateChannel + handleUpdateChannel` validation 扩 IsValidVisibility 谓词调用 byte-identical 反向 reject 外值 → 400 报错文案 byte-identical) + CHN-9.2 client (`packages/client/src/lib/visibility.ts` VISIBILITY_* 字面 byte-identical 跟 server + isValidVisibility 谓词 + VISIBILITY_LABELS 三态 emoji + text 单源; `lib/api.ts::setChannelVisibility 走 updateChannel 单源 + visibility type 扩 3-tuple; `components/VisibilityBadge.tsx` `<span>` + `data-visibility` 三态 + 文案 byte-identical) + CHN-9.3 closure (REG-CHN9-001..006 6 🟢; §5 dup 总计 fix → 单一 405/379/26). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 visibility 列 + 加 creator_only enum; CHECK 在 app 层) + ② Visibility 三向锁 (server const + client const + DB byte-identical, 改一处=改三处) + ③ owner-only ACL 锁链第 17 处 + admin god-mode 不挂 PATCH visibility + creator_only **不 leak** (反向断言非 creator user GET /api/v1/channels 列表 0 行 leak; ListChannelsWithUnread `visibility = 'public'` filter byte-identical 不动). 跨 milestone byte-identical 锁链: CHN-1.1 #267 channels.visibility 列复用 (0 schema 改) + CHN-1.2 既有 PATCH + channel.manage_visibility ACL byte-identical 不动 + ListChannelsWithUnread SQL filter byte-identical (creator_only 不入 org-public preview) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 17 处一致 + audit 5 字段链第 14 处 + AST 锁链延伸第 14 处 + Visibility 三向锁 (server + client + DB byte-identical) + CHN-7 MuteBit 双向锁 + CHN-8 NotifPref 三向锁模式承袭. 不在范围: per-message visibility (永久不挂) / visibility 切换审计 (audit 5 字段链复用既有 PATCH path) / admin god-mode visibility override (永久不挂 ADM-0 §1.3) / DM visibility (永久不挂 DM 走 CHN-4 dm-only) / visibility-based 跨 org 邀请 (留 v3 跟 AP-3 同期). 13 unit PASS — server-go 8 (TestCHN91_NoSchemaChange + TestCHN91_VisibilityConsts_ByteIdentical + TestCHN91_PatchVisibility_CreatorOnly_HappyPath + TestCHN91_PatchVisibility_BackcompatPublicPrivate + TestCHN91_PatchVisibility_RejectsInvalidValue + TestCHN91_CreatorOnlyChannel_NotLeakedToOrgPeers + TestCHN91_ListChannelsFilter_ByteIdentical + TestCHN91_NoAdminVisibilityPath + TestCHN93_NoVisibilityQueue) + client vitest 5 (VisibilityBadge.test.tsx: creator_only/private/public 三态文案 byte-identical + 三向锁 + 同义词反向). server-go ./... 25 packages 全绿 + client ./... 46 files / 390 tests 全绿 (CHN-1.2/CHN-3.2/CHN-5/CHN-6/CHN-7/CHN-8 既有 unit 不破). |
| 2026-04-30 | 战马D | HB-6 整 milestone 一 PR (Phase 6 — heartbeat lag percentile 监控, 复用 BPP-4 watchdog + HB-3 v2 decay 同源; **0 schema / 0 sweeper goroutine / 0 client UI v1**): spec brief `hb-6-spec.md` 80 行 (≤80) + stance `hb-6-stance-checklist.md` 65 行 (≤80) + acceptance `hb-6.md` 39 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3 跟 AL-7/AL-8 同精神). HB-6.1 server lag aggregator (`internal/api/hb_6_lag.go::AggregateLag` + `SampleLagFromStore` — 30s 滚窗 SELECT lag_ms FROM agent_runtimes WHERE status='running' AND last_heartbeat_at IS NOT NULL AND last_heartbeat_at >= cutoff; 内存 sort + linear interpolation P50/P95/P99 + count + at_risk + reason_if_at_risk; WindowSeconds=30 const byte-identical 跟 BPP-4 BPP_HEARTBEAT_TIMEOUT_SECONDS 同源 + LagThresholdMs=15000 const = watchdog 周期一半) + HB-6.2 admin readonly endpoint (GET /admin-api/v1/heartbeat-lag 跟 ADM-2.2 + AL-7.2 + AL-8 admin-rail 同模式; admin readonly 不挂 PATCH/POST/DELETE ADM-0 §1.3 红线; server.go 加 hb6LagHandler.RegisterAdminRoutes) + HB-6.3 closure (REG-HB6-001..006 6 🟢; 总计 405→411 / active 379→385 / pending 26 不变). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 agent_runtimes.last_heartbeat_at AL-4.1 #398 v=16 既有列, 反向 grep migrations/hb_6_ 0 hit) + ② lag derived metric 不写表 / 不另起 admin_actions enum (反向 grep `lag_audit\|hb_lag_log\|heartbeat_lag_table` 0 hit; AST 锁链延伸第 16 处 forbidden 3 token `pendingLagSample / lagSampleQueue / deadLetterLag` 0 hit) + ③ admin-rail only (反向 grep PATCH/POST/PUT/DELETE 在 admin-api/v1/heartbeat-lag 0 hit + user-rail /api/v1/heartbeat-lag 0 hit; AL-1a reason 锁链第 19 处 — at-risk reason 字面=reasons.NetworkUnreachable byte-identical 跟 BPP-4 watchdog 同源) + ④ watchdog 节奏不动 (HB-6 是同步 GET handler 即时聚合 NOT scheduled job, 反向 grep `HeartbeatLagSweeper\|hb6.*Ticker\|lag_ticker` 0 hit) + ⑤ WindowSeconds=30 双向锁 (server const + BPP-4 BPP_HEARTBEAT_TIMEOUT_SECONDS 反向锁 — 改一处=改两处) + ⑥ 不挂 client UI v1 (反向 grep `useHeartbeatLag\|HeartbeatLagPanel` 在 client/src/ 0 hit). 跨 milestone byte-identical 锁链: AL-4.1 #398 agent_runtimes.last_heartbeat_at 列复用 (0 schema 改) + BPP-4 #499 watchdog BPP_HEARTBEAT_TIMEOUT_SECONDS=30 双向锁 (改一处 = 改两处) + HB-3 v2 #14 decay 三档承袭立场 (HB-6 read-side 不改 watchdog 节奏 / 不改 decay 阈值) + AL-1a reason 锁链 HB-6 = 第 19 处 (DM-7 #18 + HB-5 #17 + AL-8 #16 + AL-7 #15 承袭, reasons.NetworkUnreachable byte-identical 跟 BPP-4 watchdog timeout 同源) + ADM-0 §1.3 admin god-mode 不挂 PATCH/POST/DELETE + admin-rail only 跟 ADM-2.2 / AL-7.2 / AL-8 同模式 + AST 锁链延伸第 16 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 forbidden tokens 全 0 hit). 不在范围: lag 历史持久化表 (留 v3 跟 AL-7 retention 同精神) / admin SPA dashboard UI (留 v3 跟 AL-7/AL-8 同精神) / Prometheus metrics exporter (留 v3) / 主动告警 (Slack/Email留 v4) / 跨 org lag 隔离 (留 AP-3 同期) / per-agent lag 历史时序 (留 v3, v0 仅 30s 滚窗 percentile) / runtime hot-mutate WindowSeconds (留 v3, const 字面单源). 11 server unit PASS — TestHB61_NoSchemaChange + WindowSecondsByteIdentical + AggregateLag_PercentileCorrect + AggregateLag_EmptyNoRisk + WindowCutoffExcludesStale + TestHB62_AdminHappyPath + NonAdmin401 + NoUserRailPath + AtRiskReasonByteIdentical + TestHB63_NoAdminWritePath + NoLagSampleQueue + NoClientUIv1; server-go ./... 25 packages 全绿 (BPP-4 watchdog + AL-4 agent_runtimes + ADM-2.2 既有 unit 全套不破). |
| 2026-04-30 | 战马D | CHN-10 整 milestone 一 PR (Phase 6 — channel description owner-only PUT + ChannelHeader 描述行 + DescriptionEditor modal; **0 schema 改** — 复用 channels.topic 既有列 cm-onboarding-welcome ALTER TEXT NOT NULL DEFAULT '' + GORM size:500): spec brief `chn-10-spec.md` 65 行 (≤80) + stance `chn-10-stance-checklist.md` 60 行 (≤80) + acceptance `chn-10.md` 38 行 (≤50) + content-lock `chn-10-content-lock.md` 70 行 (DescriptionEditor + ChannelHeader 文案 byte-identical + 同义词反向 reject). CHN-10.1 schema 0 行 (复用 channels.topic 既有列) + CHN-10.2 server (`internal/api/chn_10_description.go::CHN10DescriptionHandler` PUT /api/v1/channels/{channelId}/description owner-only — channel.CreatedBy == user.ID 反向断 member-level reject 403; DescriptionMaxLength=500 const byte-identical 跟 channels.topic GORM size:500 + client DESCRIPTION_MAX_LENGTH 同源 — 双向锁守门; 写入相同 channels.topic 列 store.UpdateChannel 单源; server.go 加 chn10DescHandler.RegisterUserRoutes; admin god-mode 不挂 RegisterAdminRoutes 不存在 ADM-0 §1.3 红线; 既有 PUT /topic CHN-2 #406 member-level + 250 字符 path byte-identical 不变) + CHN-10.3 client (`packages/client/src/lib/api.ts::setChannelDescription` thin wrapper + DESCRIPTION_MAX_LENGTH=500 const byte-identical; `components/DescriptionEditor.tsx` modal title `频道说明` 4 字 + save `保存` 2 字 + cancel `取消` 2 字 + counter `{n}/500` + textarea maxLength=500 + error `频道说明不能超过 500 字符` byte-identical; `components/ChannelHeader.tsx` 描述行 + edit trigger `编辑` 2 字 + 空 description return null) + CHN-10.4 closure (REG-CHN10-001..006 6 🟢; 总计 405→411 / active 379→385 / pending 26 不变). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 channels.topic 既有列, 反向 grep migrations/chn_10_ + ALTER channels.*description 0 hit) + ② owner-only ACL 锁链第 20 处 (DM-7 #19 + CHN-9 #14 承袭, channel.CreatedBy == user.ID 反向断 member-level reject 403; admin god-mode 不挂 PATCH/PUT/POST/DELETE 在 admin-api/v1/.../description ADM-0 §1.3 红线) + ③ 文案 byte-identical 锁 (`频道说明` + `保存`/`取消` + `{n}/500` counter + `编辑` trigger + 同义词反向 reject `topic/about/intro/简介/主题/关于/介绍`) + ④ 既有 /topic byte-identical 不变 (CHN-2 #406 既有 PUT /topic member-level + 250 字符 path 不动, 反向 grep `chn_10/DescriptionMaxLength//description` 在 handleSetTopic block 0 hit) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19 不延伸, 反向 grep `chn10.*reason\|description_reason` 0 hit) + ⑥ AST 锁链延伸第 17 处 forbidden 3 token 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-2 #406 既有 PUT /topic member-level + 250 字符 path byte-identical 不动 + cm-onboarding-welcome channels.topic ALTER TEXT NOT NULL DEFAULT '' + GORM size:500 byte-identical 双向锁 (server const + client const + GORM 三向锁) + DM-7 #19 owner-only ACL 锁链承袭 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 17 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7/AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 + CHN-10 forbidden tokens 全 0 hit). 不在范围: description Markdown 渲染 (留 v3 — 文本 only v0) / description 历史回放 (留 v3 — DM-7 edit history 不延伸) / description push 通知 (留 v3 跟 RT-3 fan-out 不联动) / per-language i18n description (留 v4) / description 全文搜 (留 v3 FTS 同期) / admin god-mode override (永久不挂 ADM-0 §1.3) / description retention sweeper (留 v3 forward-only). 13 unit PASS — server-go ./internal/api/ TestCHN101_NoSchemaChange + TestCHN102_PutDescription_OwnerHappyPath/NonOwnerRejected/Unauthorized401/LengthCap500 + TestCHN102_TopicPathByteIdentical + TestCHN103_NoAdminDescriptionPath/NoDescriptionQueue 8 unit + client packages/client vitest DescriptionEditor.test.tsx 5 PASS = 总 13 unit PASS. server-go ./... 25 packages 全绿 (CHN-2 既有 + cm-onboarding-welcome + 全套既有 unit 不破) + client ./... 51 files / 415 tests 全绿. |
| 2026-04-30 | 战马D | CHN-5 整 milestone 一 PR (Phase 6 channel archive UI 闭环 — CHN-1.2/.3 #265/#288 既有 archive PATCH + 列表过滤 + badge UI 已落; 本 milestone 收尾: GET 列表 endpoint + unarchive system DM fanout 互补二式 + 客户端 ArchivedChannelsPanel 折叠区 + admin-rail readonly GET; **0 schema 改 / 0 新 admin_actions enum** — 复用 CHN-1.1 #267 既有 channels.archived_at 列): spec brief `chn-5-spec.md` 77 行 (≤80) + stance `chn-5-stance-checklist.md` 67 行 (≤80) + acceptance `chn-5.md` 44 行 (≤50) + content-lock `chn-5-content-lock.md` 60 行 (UI 必锁 — 互补二式 archive/unarchive system DM + ArchivedChannelsPanel DOM + 同义词反向). CHN-5.1 schema 0 行 (复用 CHN-1.1 既有 channels.archived_at) + CHN-5.2 server (`internal/store/queries.go::ListArchivedChannelsForUser` (cm.user_id 过滤 + cross-org 承袭 CM-3) + `ListAllArchivedChannelsForAdmin` admin 全 org readonly; `internal/api/chn_5_archived.go` handleListMyArchivedChannels owner-only + handleAdminListArchivedChannels admin readonly + fanoutUnarchiveSystemMessage 跟 fanoutArchiveSystemMessage 同模式 + 文案 byte-identical 跟 content-lock §1; `channels.go` handleUpdateChannel 加 unarchive 分支调 fanoutUnarchiveSystemMessage; server.go 加 RegisterCHN5Routes user-rail + RegisterCHN5AdminRoutes admin-rail) + CHN-5.3 client (`packages/client/src/components/ArchivedChannelsPanel.tsx` `<details>` 折叠区 + `<summary>已归档频道</summary>` + 行 data-archived="true" + badge `已归档` 跟 CHN-1.3 #288 同源 + button data-action="restore" `恢复` 1 字 + empty `暂无已归档频道`; `lib/api.ts::listArchivedChannels` 单源新增) + CHN-5.4 closure (REG-CHN5-001..006 6 🟢; 总计 381→387 / active 355→361 / pending 26 不变). 立场 3 项: ① 0 schema 改 (复用 CHN-1.1 既有列) + ② owner-only ACL 锁链 13 处一致 + admin god-mode 不挂 PATCH ADM-0 §1.3 红线 + ③ unarchive system DM 互补二式 byte-identical 跟 content-lock 同源. 跨 milestone byte-identical 锁链: CHN-1.1 #267 channels.archived_at 列复用 + CHN-1.2 #265 fanoutArchiveSystemMessage 同模式 + CHN-1.3 #288 SortableChannelItem badge byte-identical + ListChannelsWithUnread archived_at IS NULL filter byte-identical 不动 + ADM-0 §1.3 admin god-mode 不挂 PATCH + CM-3 #208 cross-org 过滤承袭 + owner-only ACL 锁链 13 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5) + audit 5 字段链第 10 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8+HB-5+CHN-5) + AST 锁链延伸第 10 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7 + AL-8 + HB-5 + CHN-5 forbidden tokens 全 0 hit). 不在范围: 自动 archive 策略 (留 v3) / age-based auto-archive cron (留 v3) / archived hard-delete (永久不挂 forward-only) / cross-org 同步 (留 v3 AP-3 同期) / admin god-mode PATCH archive (永久不挂 ADM-0 §1.3) / 历史 cursor 重置 (留 v3). 13 unit PASS — server-go internal/api TestCHN51_NoSchemaChange + TestCHN52_ListMyArchived_HappyPath/EmptyList/Unauthorized + TestCHN52_AdminListArchived_HappyPath/RejectsUserRail + TestCHN52_UnarchiveFanoutsSystemMessage + TestCHN52_NoAdminPatchPath + TestCHN53_NoChannelArchiveQueue 9 + client packages/client vitest ArchivedChannelsPanel.test.tsx 4 PASS. server-go ./... 25 packages 全绿 + client ./... 44 files / 380 tests 全绿 (含 ADM-2.2 + CHN-1.2 + 全套既有 unit 不破). |
| 2026-04-30 | 战马D | CHN-6 整 milestone 一 PR (Phase 6 channel pin/unpin 顶置 — 用户顶置 favorite channels; **0 schema 改 / 0 新 admin_actions enum** — user_channel_layout 列复用 CHN-3.1 #410 既有, pin 状态走 `position < 0` 字面约定 + PinThreshold=0 const 双向锁 server+client byte-identical): spec brief `chn-6-spec.md` 80 行 (≤80) + stance `chn-6-stance-checklist.md` 66 行 (≤80) + acceptance `chn-6.md` 46 行 (≤50) + content-lock `chn-6-content-lock.md` 69 行 (UI 必锁 — pin button DOM + `已置顶频道` section + 同义词反向 + PinThreshold 双向锁). CHN-6.1 server (`internal/store/queries.go::PinChannelLayout` UPSERT user_channel_layout SET position = -(nowMs) ON CONFLICT DO UPDATE 跟 CHN-3.2 既有 upsert 同模式 + `UnpinChannelLayout` UPSERT position = max(positive)+1.0 跟 CHN-3.3 #415 client MIN-1.0 单调小数模式互补; `internal/api/chn_6_pin.go` POST + DELETE /api/v1/channels/{channelId}/pin endpoints + PinThreshold=0.0 const + IsPinned(position) 谓词单源 + DM 反 layout.dm_not_grouped byte-identical 跟 CHN-3.2 + non-member 403 + Unauthorized 401; server.go 加 RegisterCHN6Routes user-rail authMw 必经) + CHN-6.2 client (`packages/client/src/lib/pin.ts` POSITION_PIN_THRESHOLD=0 字面 byte-identical 跟 server PinThreshold + isPinned 谓词单源; `lib/api.ts::pinChannel(id, pinned)` 单源 method=POST/DELETE; `components/PinButton.tsx` toggle 文案 byte-identical `置顶`/`取消置顶` + data-action='pin'/'unpin' + 同义词反向 reject; `components/PinnedChannelsSection.tsx` `<section>` + `<header>已置顶频道</header>` + `data-testid='pinned-channels-section'` + 行 `data-pinned='true'` + filter `channel.position < POSITION_PIN_THRESHOLD` byte-identical + empty state 不渲染 return null) + CHN-6.3 closure (REG-CHN6-001..006 6 🟢; 总计 393→399 / active 367→373 / pending 26 不变 — 注: 修了 main §5 dup 总计行 `382/356/382/356/381/355` → 单一 405/379 总计 sum=actual rows). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 CHN-3.1 既有列 + position<0 字面约定 + PinThreshold const) + ② owner-only ACL 锁链第 14 处 + admin god-mode 不挂 PATCH/POST/DELETE ADM-0 §1.3 红线 + ③ pin 状态 client/server 双源不漂 (PinThreshold=0 双向锁 改一处=改两处). 跨 milestone byte-identical 锁链: CHN-3.1 #410 user_channel_layout 列复用 + CHN-3.2 #412 既有 layout endpoint byte-identical 不动 + CHN-3.3 #415 client MIN-1.0 单调小数模式承袭 + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 14 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5/CHN-6) + audit 5 字段链第 11 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8+HB-5+CHN-5+CHN-6) + AST 锁链延伸第 11 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8+HB-5+CHN-5+CHN-6 forbidden tokens 全 0 hit) + PinThreshold=0 双向锁 (server PinThreshold + client POSITION_PIN_THRESHOLD byte-identical). 不在范围: pin 数量上限 / pin 排序拖拽 (留 v3 现网 ASC 自然按时间排) / pin 跨设备同步 RT-3 push (留 v3) / admin god-mode pin override (永久不挂 ADM-0 §1.3) / DM pin (永久不挂 DM 走 CHN-4 dm-only) / pin folder nested grouping (留 v3 跟 CHN-3.3 follow-up). 18 unit PASS — server-go 9 (TestCHN61_NoSchemaChange + TestCHN61_PinChannel_HappyPath/NonMemberRejected/Unauthorized + TestCHN61_UnpinChannel_HappyPath/Idempotent + TestCHN61_PinThreshold_ByteIdentical + TestCHN61_NoAdminPinPath + TestCHN63_NoChannelPinQueue) + client vitest 9 (PinButton.test.tsx 5: 文案 pin/unpin / click→API+onChange / 同义词反向 + PinnedChannelsSection.test.tsx 4: 列表 DOM / empty state null / PinThreshold 双向锁 / 同义词反向). server-go ./... 25 packages 全绿 + client ./... 45 files / 385 tests 全绿 (CHN-3.2 既有 unit 不破). |
| 2026-04-30 | 战马D | CHN-11 整 milestone 一 PR (Phase 6 — channel member admin UI; **0 schema / 0 server production code** — POST/DELETE/GET /channels/:id/members CHN-1 #276 既有 path byte-identical 不动; 跟 DM-6 #556 / DM-5 #549 / CV-7 #535 / CV-9..12 一系列 0-server 模式承袭): spec brief `chn-11-spec.md` 70 行 (≤80) + stance `chn-11-stance-checklist.md` 50 行 (≤80) + acceptance `chn-11.md` 32 行 (≤50) + content-lock `chn-11-content-lock.md` 80 行 (MemberList + AddMemberModal + KickConfirmModal 三组件 文案 byte-identical + 同义词反向 reject). CHN-11.1 schema 0 行 (复用 channel_members) + CHN-11.2 server **0 行 production** (CHN-1 #276 既有 ABAC manage_members + agent cross-owner reject + agent_join system DM 文案锁全套 byte-identical 不动) + CHN-11.3 client (`lib/api.ts::addChannelMember/removeChannelMember/listChannelMembers` 既有 thin wrapper; `components/MemberList.tsx` title `成员` + add `添加成员` + row remove `移除` byte-identical + canManage 闸 + 空 null + data-member-user-id 行级锚; `components/AddMemberModal.tsx` title `添加成员` + placeholder `用户邮箱或 ID` + submit `添加` + cancel `取消`; `components/KickConfirmModal.tsx` title `确认移除 {user.display_name}?` byte-identical (user 占位) + confirm `确认` + cancel `取消`) + CHN-11.4 closure (REG-CHN11-001..006 6 🟢; CHN-11 +6 行). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 channel_members) + ② 0 server production code (反向 grep `chn_11/chn11/CHN11` 在 internal/api/*.go 非 _test.go 0 hit) + ③ 文案 byte-identical (`成员/添加成员/移除/确认移除 {user}?`) + 同义词反向 reject (`invite/kick/remove/expel/逐出/踢出/邀请` 在 user-visible Chinese text 0 hit) + ④ 既有 handleAddMember+handleRemoveMember block byte-identical (反向 grep chn_11 在 2 handler 2500 字符 block 0 hit) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19, 不引入新 reason; audit 5 字段链复用既有 user_joined/user_left events) + ⑥ AST 锁链延伸第 19 处 forbidden 3 token (`pendingMemberAdmin / memberAdminQueue / deadLetterMemberAdmin`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-1 #276 既有 channel_members 表 + manage_members ABAC + agent cross-owner guard + agent_join system DM 文案锁 全套不动 + DM-6 #556 / DM-5 #549 / CV-7 #535 / CV-9..12 0-server 模式承袭 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 19 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 + CHN-10 + RT-4 + CHN-11 forbidden tokens 全 0 hit). 不在范围: 角色管理 admin/moderator (留 v3) / bulk add/remove (留 v3) / member 历史回放 (留 v3) / pending invitations (留 v3 跟 INV-* 同期) / admin god-mode kick override (永久不挂 ADM-0 §1.3) / cross-org member add (CM-3 既有禁) / audit log per add/remove (留 v3 — events user_joined/user_left 既有 audit path 承袭). 10 unit PASS — server-go ./internal/api/ TestCHN111_NoSchemaChange + NoServerProductionCode + HandlersByteIdentical + TestCHN113_NoMemberAdminQueue + NoAdminMembersPath 5 unit + client packages/client vitest MemberList.test.tsx 5 PASS = 总 10 unit PASS. server-go ./... 25 packages 全绿 (CHN-1 既有 unit 全套不破) + client ./... 53 files / 423 tests 全绿. |
| 2026-04-30 | 战马D | CHN-8 整 milestone 一 PR (Phase 6 channel notification preferences 三态 — `所有消息` / `仅@提及` / `不打扰`; **0 schema 改** — collapsed bitmap bits 2-3 跟 CHN-3 bit 0 + CHN-7 bit 1 拆死. NotifPref **三向锁** server const + client const + bitmap byte-identical 跟 CHN-7 MuteBit 双向锁 / CHN-6 PinThreshold 双向锁同模式扩展): spec brief `chn-8-spec.md` 74 行 (≤80) + stance `chn-8-stance-checklist.md` 66 行 (≤80) + acceptance `chn-8.md` 47 行 (≤50) + content-lock `chn-8-content-lock.md` 68 行 (UI 必锁 — pref dropdown DOM + 三选一文案 + 同义词反向 + NotifPref 三向锁). CHN-8.1 server (`internal/store/queries.go::SetNotifPrefBits/GetNotifPrefForUser/GetCollapsedForUser` UPSERT 不动 position; `internal/api/chn_8_notif_pref.go` PUT /api/v1/channels/{channelId}/notification-pref + NotifPrefShift=2/Mask=3/All=0/Mention=1/None=2 const + GetNotifPref 谓词单源 + DM 反 layout.dm_not_grouped + non-member 403 + 401 + spec 外值 reject 400 notification_pref.invalid_value; server.go 加 RegisterCHN8Routes user-rail authMw) + CHN-8.2 client (`packages/client/src/lib/notif_pref.ts` NOTIF_PREF_* 字面 byte-identical 跟 server + getNotifPref 谓词单源 + NOTIF_PREF_STRING_TO_INT/INT_TO_STRING 双向 map; `lib/api.ts::setNotificationPref(id, pref)` 单源 method=PUT body={pref}; `components/NotificationPrefDropdown.tsx` `<select>` + 三选一 option `所有消息`/`仅@提及`/`不打扰` 文案 byte-identical + `data-testid='notification-pref-dropdown'`) + CHN-8.3 closure (REG-CHN8-001..006 6 🟢; §5 dup 总计 fix → 单一 405/379/26 sum=actual rows). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 collapsed bitmap + NotifPref 三向锁) + ② owner-only ACL 锁链第 16 处 + admin god-mode 不挂 PUT/POST/DELETE ADM-0 §1.3 红线 + ③ 不 drop messages — CreateMessage/RT-3 fan-out/WS frame 全 byte-identical, 仅 DL-4 push notifier 三态 skip best-effort. 跨 milestone byte-identical 锁链: CHN-3.1 #410 user_channel_layout 列复用 (0 schema 改 + bitmap bit 0/1/2-3 拆死) + CHN-3.2 #412 既有 PUT /me/layout endpoint byte-identical 不动 + CHN-6 #544 PinThreshold + CHN-7 #550 MuteBit 双向锁模式承袭 (NotifPref 三向锁延伸) + DL-4 push gateway 立场承袭 (三态 skip) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 16 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5/CHN-6/CHN-7/CHN-8) + audit 5 字段链第 13 处 + AST 锁链延伸第 13 处. 不在范围: 自动 quiet hours / 时段静默 (留 v3 全局 user-settings) / per-keyword 通知 (留 v3) / admin god-mode notif pref override (永久不挂) / DM notif pref (永久不挂 DM 走 CHN-4 dm-only) / 跨设备 pref 推送 RT-3 frame (留 v3). 12 unit PASS — server-go 8 (TestCHN81_NoSchemaChange + TestCHN81_SetPref_All/Mention/None + TestCHN81_SetPref_RejectsInvalidValue + TestCHN81_SetPref_NonMemberRejected + TestCHN81_NotifPrefConsts_ByteIdentical + TestCHN81_NoAdminNotifPrefPath + TestCHN81_BitmapIsolation_PreservesOtherBits + TestCHN81_NotifPrefDoesNotDropMessages + TestCHN83_NoNotifPrefQueue) + client vitest 4 (NotificationPrefDropdown.test.tsx: 三选一 DOM byte-identical / change → API + onChange / 同义词反向 / NotifPref 三向锁含 bitmap 不互扰). server-go ./... 25 packages 全绿 + client ./... 45 files / 384 tests 全绿 (CHN-3.2/CHN-5/CHN-6/CHN-7 既有 unit 不破). |
| 2026-04-30 | 战马D | RT-4 整 milestone 一 PR (Phase 6 — channel presence indicator (online users + 既有 typing 复用); **0 schema 改 / 0 新 WS frame** — 复用 AL-3.1 #277 presence_sessions 既有表 + IsOnline 谓词单源; 既有 RT-2 typing path byte-identical 不变): spec brief `rt-4-spec.md` 65 行 (≤80) + stance `rt-4-stance-checklist.md` 48 行 (≤80) + acceptance `rt-4.md` 33 行 (≤50) + content-lock `rt-4-content-lock.md` 50 行 (ChannelPresenceList DOM `当前在线 N 人` byte-identical + ≤5 显示 + `+M` overflow 同义词反向). RT-4.1 schema 0 行 (复用 AL-3.1 presence_sessions) + RT-4.2 server (`internal/api/rt_4_presence.go::RT4PresenceHandler` GET /api/v1/channels/{channelId}/presence — channel.members ∩ presence.IsOnline 同步取交集 即时聚合; member-only ACL IsChannelMember 反向断 non-member 403; PresenceSnapshot response shape `{online_user_ids: [...], counted_at: nowMs}`; server.go 加 rt4PresenceHandler.RegisterUserRoutes + presence.NewSessionsTracker 复用 AL-3.2 #310 既有 SessionsTracker; admin god-mode 不挂 RegisterAdminRoutes 不存在 ADM-0 §1.3 红线; 既有 RT-2 typing WS path ws/client.go::handleTyping byte-identical 不变 反向 grep `rt_4` 在 typing block 0 hit) + RT-4.3 client (`packages/client/src/lib/api.ts::getChannelPresence` thin wrapper + `components/ChannelPresenceList.tsx` `当前在线 N 人` byte-identical + ≤5 显示头像 + N>5 显示前 5 + `+M` overflow chip + `data-presence-user-id` 行级锚 + 空 onlineUserIds → return null; 既有 TypingIndicator.tsx RT-2 path byte-identical 不变) + RT-4.4 closure (REG-RT4-001..006 6 🟢; 总计 405→411 / active 379→385 / pending 26 不变). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 AL-3.1 presence_sessions, 反向 grep migrations/rt_4_*+ALTER presence_sessions 0 hit) + ② 0 新 WS frame (presence-change push 留 v3, 反向 grep `presence_changed/presenceChanged/user_online_pushed` 在 ws+api 0 hit) + ③ member-only ACL + admin god-mode 不挂 PATCH/POST/PUT/DELETE/GET 在 admin-api/v1/.../presence (ADM-0 §1.3 红线) + ④ 既有 RT-2 typing path byte-identical 不变 (反向 grep rt_4/RT4/rt4 在 ws/client.go::handleTyping block 0 hit) + ⑤ PresenceTracker.IsOnline 单源 (复用 AL-3.2 SessionsTracker, 不裂出独立 tracker) + ⑥ AST 锁链延伸第 18 处 forbidden 3 token (`pendingPresenceQuery / presenceQueueRetry / deadLetterPresence`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: AL-3.1 #277 presence_sessions 既有表 + idx_presence_sessions_user_id 复用 (0 schema 改) + AL-3.2 #310 SessionsTracker IsOnline 谓词单源 + RT-2 既有 typing WS path (ws/client.go::handleTyping + multi_device_test.go) byte-identical 不变 + 既有 client TypingIndicator.tsx (5s timeout 自动消失) byte-identical 不动 + ADM-0 §1.3 admin god-mode 不挂 + AL-1a reason 锁链不漂 (停在 HB-6 #19 — RT-4 read-only 不引入新 reason) + AST 锁链延伸第 18 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 + CHN-10 + RT-4 forbidden tokens 全 0 hit). 不在范围: presence-change WS push frame (留 v3 — RT-3.2 fan-out 同期) / per-device presence (留 v3 — multi-device 算一个 user 已 online) / typing indicator 改 (既有 RT-2 path byte-identical 不动) / presence 历史回放 (留 v3 — DM-7 edit history 不延伸) / admin god-mode override (永久不挂 ADM-0 §1.3) / cross-org presence isolation (留 AP-3 同期) / last-seen-at 列展示 (留 v3 — 当前仅 online 二态). 13 unit PASS — server-go ./internal/api/ TestRT41_NoSchemaChange + TestRT41_TypingPathByteIdentical + TestRT42_GetPresence_MemberHappyPath/NonMemberRejected/Unauthorized401 + TestRT43_NoAdminPresencePath + TestRT43_NoNewPresenceFrame + TestRT43_NoPresenceQueue 8 unit PASS + client packages/client vitest ChannelPresenceList.test.tsx 5 PASS = 总 13 unit PASS. server-go ./... 25 packages 全绿 (AL-3.1/RT-2/既有 typing path 全套不破) + client ./... 53 files / 423 tests 全绿. |
| 2026-04-30 | 战马D | CHN-12 整 milestone 一 PR (Phase 6 — channel sort manual reorder; **0 schema / 0 server production code** — PUT /api/v1/me/layout CHN-3.2 #357 既有 batch upsert path byte-identical 不动 + 既有 SortableChannelItem 拖拽 handle byte-identical 跟 CHN-3.3 #415 同源; 跟 CHN-11 #563 / DM-6 #556 / CV-12 #545 / CV-9..12 系列 0-server 模式承袭): spec brief `chn-12-spec.md` 80 行 (≤80) + stance `chn-12-stance-checklist.md` 50 行 (≤80) + acceptance `chn-12.md` 30 行 (≤50) + content-lock `chn-12-content-lock.md` 90 行 (ChannelDragHandle DOM + ChannelList dnd-kit wiring + computeReorderPosition 单调小数算法 byte-identical 跟 CHN-3.3 #415 同源 + 同义词反向 reject + DM 反挂锁). CHN-12.1 schema 0 行 (复用 user_channel_layout.position REAL) + CHN-12.2 server **0 行 production** (CHN-3.2 #357 既有 PUT /me/layout + layout.invalid_payload + layout.dm_not_grouped + non-member 403 + layoutSaveErrorMsg 5 字面反向锚 byte-identical 不动) + CHN-12.3 client (`lib/dnd_position.ts::computeReorderPosition(prev, next)` pure fn 单调小数算法 byte-identical 跟 CHN-3.3 #415 acceptance §2.4 同源: 都 null → 1.0 / prev=null → next-1.0 / next=null → prev+1.0 / 中间 → 中点 (prev+next)/2.0; 既有 SortableChannelItem aria-label `拖拽调整顺序` + ⋮⋮ + data-sortable-handle byte-identical 不动 (CHN-3.3 #415 锚)) + CHN-12.4 closure (REG-CHN12-001..006 6 🟢; CHN-12 +6 行). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 user_channel_layout.position) + ② 0 server production code (反向 grep `chn_12/chn12/CHN12` 在 internal/api/*.go 非 _test.go 0 hit) + ③ 文案 byte-identical (跟 CHN-3.3 既有 `拖拽调整顺序` aria-label byte-identical, 不漂同义词 `移动/排序/拖动/抓握`) + ④ 既有 handlePutMyLayout block byte-identical (反向 grep chn_12 在 layout.go 3500 字符 block 0 hit + 3 既有锚 must-contain) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19, reorder per-user UI preference 不 audit, 跟 CHN-7 mute / CHN-8 notif-pref 立场 ⑥ 同精神) + ⑥ AST 锁链延伸第 20 处 forbidden 3 token (`pendingReorder / reorderQueue / deadLetterReorder`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-3.2 #357 既有 PUT /me/layout + 5 字面反向锚 (DM reject + non-member 403 + invalid_payload + 文案锁 + 字段反约束) byte-identical 不动 + CHN-3.3 #415 既有 SortableChannelItem 拖拽 handle 全套保留 + CHN-11 #563 / DM-6 #556 / CV-12 #545 0-server 模式承袭 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 20 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..11 + DM-6 + DM-7 + HB-6 + RT-4 + CHN-12 forbidden tokens 全 0 hit). 不在范围: group reorder (CHN-9 group ordering 已分别 milestone) / bulk reorder API (留 v3 — server PUT /me/layout 已 batch 单源) / pinned section drag-into-pinned (留 v3 — pinned 仅 CHN-6 toggle path) / DM reorder (永不 — DM 不参与分组立场 ④ byte-identical 跟 CHN-3.2 #357) / 跨用户共享 sort (永不 — per-user preference 立场 ②) / admin god-mode reorder override (永远不挂 ADM-0 §1.3) / audit log per reorder (留 v3). 10 unit PASS — server-go ./internal/api/ TestCHN121_NoSchemaChange + NoServerProductionCode + HandlerByteIdentical + TestCHN123_NoReorderQueue + NoAdminLayoutPath 5 unit + client packages/client vitest dnd_position.test.ts 5 PASS (5 case: 都 null fallback + prev=null + next=null + 中间中点 + 负数 position) = 总 10 unit PASS. server-go ./... 25 packages 全绿 (CHN-3.2 / CHN-3.3 既有 unit 全套不破) + client ./... 全绿. |
| 2026-04-30 | 战马D | CHN-13 整 milestone 一 PR (Phase 6 — channel list search/filter; **0 schema / ≤15 行 server prod** — `handleListChannels` 加 `?q=` 子串 filter + `ListChannelsWithUnread(userID, q)` sig 加 q 参数; q="" 走既有 SQL byte-identical, q!="" 加 LIKE COLLATE NOCASE 大小写不敏感子串; 跟 CHN-12 #582 / CHN-11 #563 / DM-6 #556 系列 minimal-server 模式承袭): spec brief `chn-13-spec.md` 79 行 (≤80) + stance `chn-13-stance-checklist.md` 50 行 (≤80) + acceptance `chn-13.md` 35 行 (≤50) + content-lock `chn-13-content-lock.md` 95 行 (ChannelSearchInput DOM + ChannelList 空态/count + 同义词反向 reject + debounce 200ms 跟 CHN-3.3 useUserLayout PUT_DEBOUNCE_MS 同源 + server q 透传 byte-identical). CHN-13.1 schema 0 行 (复用 channels 既有表 + name 既有 index) + CHN-13.2 server (handleListChannels 加 `q := strings.TrimSpace(r.URL.Query().Get("q"))` + ListChannelsWithUnread(userID, q) sig 加 q + 空 q 路径 byte-identical 跟既有 SQL 同源, q!="" 加 `AND c.name LIKE '%' || ? || '%' COLLATE NOCASE` 子句 + 既有 callsites store_coverage_test.go + welcome_test.go 全填 q="" byte-identical) + CHN-13.3 client (`lib/api.ts::fetchChannels(q?)` thin wrapper 加 optional q + encodeURIComponent + 空 q URL byte-identical 不漂 `?q=`; `components/ChannelSearchInput.tsx` controlled input + debounce 200ms + placeholder/aria-label `搜索频道` byte-identical + 反向 grep同义词 reject) + CHN-13.4 closure (REG-CHN13-001..006 6 🟢; CHN-13 +6 行). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 channels 既有表) + ② server 最小补丁 ≤15 行 production (handleListChannels 加 q 解析 + ListChannelsWithUnread sig 加 q) + ③ 文案 `搜索频道` placeholder/aria-label + `未找到匹配` 空态 + `共 N 个频道` count byte-identical + 同义词反向 reject (`find/lookup/locate/查找/检索/查询`) + ④ 既有 ListChannelsWithUnread 空 q 路径 byte-identical (反向 grep 锚: q="" 走原 SQL 不破; 既有 admin-rail /admin-api/v1/channels 不动) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19, search 是 read-only filter 不 audit, 跟 CHN-7 mute / CHN-8 notif-pref 立场 ⑥ 同精神) + ⑥ AST 锁链延伸第 21 处 forbidden 3 token (`pendingSearch / searchQueue / deadLetterSearch`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-1.2 既有 ListChannelsWithUnread membership-scoped + cross-org public discovery + visibility='public' filter (CHN-9 #553 锚) byte-identical 不动 + CHN-3.3 useUserLayout debounce 200ms 同源 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 21 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..12 + DM-6 + DM-7 + HB-6 + RT-4 forbidden tokens 全 0 hit). 不在范围: 全文搜索 (message body / artifact content) 留 v3 / 模糊匹配/pinyin 留 v3 / 跨 org 搜索 永不 (CM-3 既有禁) / admin-rail 搜索 留 v3 / 搜索历史 推荐 热门 留 v3+ / 搜索结果排序定制 v0 既有 ordering / audit log per search 永不. 10 unit PASS — server-go ./internal/api/ TestCHN131_NoSchemaChange + TestCHN132_ListChannelsWithQuery + QueryCaseInsensitive + QuerySubstringMatch + TestCHN133_NoSearchQueue + NoAdminSearchPath 5 unit (其中 ListChannelsWithQuery seed 3 channel + alpha/beta/gamma + q="alpha" 返回 1 channel + empty q 返回 ≥3) + client packages/client vitest ChannelSearchInput.test.tsx 5 PASS (placeholder + aria-label + debounce 200ms 真测 + 空 q 触发 + 同义词反向 grep) = 总 10 unit PASS. server-go ./... 25 packages 全绿 (既有 ListChannelsWithUnread callsites 全填 q="" byte-identical 不破) + client ./... 全绿. |
| 2026-04-30 | 战马E | CV-14 整 milestone 一 PR (Phase 5+ — artifact comment unread count badge, **0 server production code** 复用 useArtifactCommentAdded CV-5 #530 既有 WS hook; 跟 CV-9..13 / DM-5..6 / AP-4 / AP-5 0-server client-only 同模式): spec brief `cv-14-spec.md` 60 行 (≤80) + stance `cv-14-stance-checklist.md` 60 行 + acceptance `cv-14.md` 41 行 + content-lock `cv-14-content-lock.md` (文案 + DOM SSOT 单源). CV-14.1 server 反向断言 0 production diff (复用 ws.ArtifactCommentAddedFrame CV-5 #530 + useArtifactCommentAdded WS hook) + CV-14.2 client (`packages/client/src/components/CommentUnreadBadge.tsx` 新组件 — 订阅 useArtifactCommentAdded + filter `sender_id !== currentUserId` 不计自己 + count > 99 → "99+" overflow + click reset; 7 vitest PASS — ZeroNotRendered + OtherSenderIncrements + SelfSenderNotCounted + Overflow99Plus + ClickResets + DOMAttrs + NoStorageWrite) + CV-14.3 closure (REG-CV14-001..005 5 🟢; 总计 387→392 / active 361→366 / pending 26 不变). 立场 5 项: ① 0 server production code (反向断言 git diff 0 行) + ② 跟 CV-9 mention badge 共存 (mention 走 CV-9 既有, mention=更强 signal, mention comment 同时计入两 badge 是预期行为) + ③ thinking 5-pattern 锁链第 10 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13+CV-14) + ④ 文案 byte-identical 3 字面 (${N} 条新评论 中文模板 + 99+ overflow + zero-state null) + ⑤ DOM 2 data-attr (data-cv14-comment-unread-badge + data-cv14-unread-count). 跨 milestone byte-identical 锁链: CV-5 #530 ArtifactCommentAddedFrame WS hook + CV-9 #539 ArtifactCommentsMentionBadge 共存 + CV-13 #557 props-driven 同模式 + Sidebar.tsx unread-badge 99+ overflow 同精神 + thinking 锁链第 10 处 + ADM-0 §1.3 红线. 不在范围: persistent unread 跨 reload (留 v2 复用 channel.unread_count) / admin god-mode unread (永久不挂) / desktop notification (DL-4 路径) / schema 改 / 新 endpoint / unread per-comment highlight (留 v3). vitest 64 files / 477 tests 全 PASS + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行验证. |
| 2026-04-30 | 战马E | DM-9 整 milestone 一 PR (Phase 5+ — DM message emoji picker, **0 server production code + 0 schema 改 反 v=39 硬锁** 复用 CV-7 #535 PUT /api/v1/messages/{id}/reactions + AP-4 #551 ACL gate; 跟 CV-9..14 / DM-5..6 / AP-4..5 同模式): spec brief `dm-9-spec.md` 70 行 + stance + acceptance + content-lock (5 emoji preset + 文案 + DOM SSOT 三轨字面). DM-9.1 server 反向断言 0 行 + 反 v=39 schema 硬锁 + DM-9.2 client (`packages/client/src/components/EmojiPickerPopover.tsx` 新组件 — 5 emoji preset 顺序 byte-identical 👍 ❤️ 😄 🎉 🚀 + toggle + Escape/outside-click 关 popover + click → addReaction + onChanged callback; 8 vitest PASS — ToggleByteIdentical + DefaultClosed + OpenAndPresetOrder + EmojiClickTriggersAddReaction + EscapeCloses + OutsideClickCloses + DOMAttrs + NoStorageWrite) + DM-9.3 closure (REG-DM9-001..005 5 🟢; 总计 392→397). 立场 5 项: ① 0 server production code (反 v=39 schema 硬锁, 既有 reactions 表 + endpoint 已盖) + ② 5-emoji preset byte-identical 顺序 + ③ thinking 5-pattern 锁链第 11 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13+CV-14+DM-9) + ④ DOM 4 data-attr (toggle + popover + 5 option + open 状态) + ⑤ 跟 DM-5 ReactionSummary 互斥共存 (picker=加新, ReactionSummary=显示+toggle, click 后 onChanged callback). 跨 milestone byte-identical 锁链: CV-7 #535 PUT reactions endpoint + AP-4 #551 ACL gate + DM-5 #549 ReactionSummary 视觉互补 + CV-7 ArtifactCommentItem 不另起 picker 反约束 (DM-9 今天补) + thinking 锁链第 11 处 + ADM-0 §1.3 红线. 不在范围: custom emoji upload (v3) / search filter / admin god-mode (永久) / schema 改 / v=39 migration (反硬锁) / skin tone (v2). vitest 70 files / 510 tests 全 PASS + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行 + 反 v=39 grep 0 hit 验证. |
| 2026-04-30 | 战马E | ADM-3 整 milestone 一 PR (Phase 4 admin-model 收尾, BPP-8 #532 名实不符 follow-up, **元数据 RENAME 0 数据迁移 + view backward compat**): spec brief `adm-3-spec.md` 80 行 (飞马 v0 #570 起草) + stance `adm-3-stance-checklist.md` 60 行 + acceptance `adm-3.md` 38 行 + content-lock 不需 (server-only schema). ADM-3.1 schema migration v=43 (`internal/migrations/adm_3_1_audit_events_rename.go` ALTER admin_actions RENAME TO audit_events 元数据 0 数据迁移 + CREATE VIEW admin_actions AS SELECT * FROM audit_events alias backward compat + INSTEAD OF INSERT/UPDATE triggers 路由 view → table; v=43 sequencing post-cv-6 v=36 跟 team-lead 占号 reservation 一致) + ADM-3.2 backward compat 验证 (既有 ADM-2 + BPP-8 + AL-7 + AL-8 unit tests 全 PASS via alias view; AuditEventsTableExists + AdminActionsViewExists + ViewSelectRoundtrip + ViewInsertRoutedToTable + VersionIs43 + Idempotent 6 unit PASS) + ADM-3.3 closure (REG-ADM3-001..005 5 🟢; ADM-0 §1.3 红线扩展段落 ≤5 行 audit-forward-only 单表跨全 actor type 立场写进 docs/blueprint/admin-model.md 实施 PR 顺手). 立场 3 项: ① 表名改 audit_events 单表跨全 actor type (admin + plugin_system + system) + ② alias view backward compat 战马原代码 0 改 (gorm AdminAction.TableName == admin_actions 写通过 INSTEAD OF trigger 路由 audit_events table) + ③ ADM-0 §1.3 红线扩展 audit-forward-only 不可 DELETE/UPDATE + admin 仍走 /admin-api/*. 跨 milestone byte-identical 锁链: ADM-2.1 #484 起源 + BPP-8 #532 plugin lifecycle 名实不符今修 + AL-7.1 archived_at 跟随 RENAME 自动 + AL-7.2 sweeper UPDATE archived_at 走 audit_events 直接 + audit-forward-only 锁链跨 11 处. 不在范围: 字段重设计 (留 v3+) / 函数名 RENAME (留 Phase 5+) / view 删除 (留 Phase 5+ deprecation). server-go TestADM31_* 6 PASS + 全 25 packages 全绿 (+sqlite_fts5 tag) 含 ADM-2/BPP-8/AL-7 backward compat 验证. |
| 2026-04-30 | 战马D | CS-4 整 milestone 一 PR (Phase 4+ — Client Shape 第四段 IndexedDB 乐观缓存 wrapper, **0 server prod + 0 schema 改 + 0 新 endpoint** 跟 CS-1/2/3 / CV-9..14 / DM-5..6 / DM-9 / CHN-11..12 client-only 0-server 同模式承袭): 4 件套 spec/stance/acceptance/content-lock 全闭 (蓝图 client-shape.md §1.4 IndexedDB 乐观缓存 B 路径) + CS-4.1 IDB wrapper SSOT (`lib/cs4-idb.ts` 单源 — `openCS4DB()` IDBOpenDBRequest + onupgradeneeded 建 3 store byte-identical 跟蓝图 §1.4 表 `messages` keyPath=id index channel_id + `last_read_at` keyPath=channel_id + `agent_state` keyPath=agent_id; `cs4Get/Put/Delete` typed wrappers + `clearStaleEntries(maxAgeMs)` cleanup helper, DB version=1 schema 改 = bump version + onupgradeneeded migration 跟 server schema_migrations 同精神) + sync-state SSOT (`lib/cs4-sync-state.ts` 4-enum byte-identical 跟蓝图字面 — `offline_cache_hit`→`离线模式` / `synced`→`已同步` / `syncing`→`同步中…` / `cache_miss`→null 不渲染; `SYNCING_LABEL_DELAY_MS = 3000` 沉默胜于假 loading 阈值 跟 RT-1 §1.1 字面承袭) + CS-4.2 hook + UI (`lib/use_first_paint_cache.ts` mount 时 IDB.get 返 cached + 同时 caller-supplied `cursorBackfillFn(sinceCursor)` server fetch confirm 后 IDB.put 覆盖 + offline `navigator.onLine=false` skip server fetch 走 cache hit + cache miss 不阻塞 UI; `components/SyncStatusIndicator.tsx` DOM `data-cs4-sync-state` 4 态 + syncing ≥3s 才显示 label) + 20 vitest PASS (cs4-idb.test.ts 6 + cs4-sync-state.test.ts 5 + use_first_paint_cache.test.tsx 4 + SyncStatusIndicator.test.tsx 5) + 全套 client 80 files / 556 tests 全 PASS (既有 RT-1 cursor / DM-3 useDMSync / CV-10 localStorage 草稿路径 byte-identical 不破). 立场 3 项: ① 3 store 拆死 byte-identical 跟蓝图 §1.4 表 (typing/presence-realtime 不入 IDB — 蓝图字面 "必从 server 实时拉"; artifact 内容 / DM body / 草稿 走 CV-10 localStorage 既有 拆死) + ② 乐观缓存非权威 + cursor sync 触发 + ≤3s 沉默 (mount 时 IDB.get 返 cached + 同时触发 server cursor backfill confirm 后 IDB.put 覆盖; cache miss 时不阻塞 UI; offline 时 skip server fetch 走 cache hit; syncing ≥3s 才显示 label 沉默胜于假 loading 字面承袭 RT-1 §1.1) + ③ 0-server-prod + 0 schema + 文案 byte-identical 跟蓝图字面 + admin god-mode 不挂 (ADM-0 §1.3 红线). 不在范围: background sync (蓝图 §1.1 字面承袭) / artifact 内容 + DM body + 草稿入 IDB (草稿走 CV-10 既有) / typing + presence-realtime 入 IDB (蓝图 §1.4 字面) / Service Worker offline page (留 CS-3 PWA + sw.js DL-4 既有) / 跨设备同步 (server cursor 是真相, IDB 只 first paint 加速) / admin god-mode IDB inspect (永久不挂 ADM-0 §1.3) / IDB cleanup goroutine + scheduled job (留 v1 用户主动 logout 时清, 不做 background sweep). 跨 milestone byte-identical 锁链: RT-1 #290 cursor opaque 协议 byte-identical (CS-4 IDB.put cursor key 跟 server `?cursor=` 同源, 走 caller-supplied cursorBackfillFn 不绑定具体 lib import) + DM-3 useDMSync 既有 client cursor 同步模式承袭 + CS-10 草稿 localStorage 拆死 (CS-4 不入草稿域) + CS-2 #595 故障三态联动 (failed 时 IDB cache hit + offline label graceful fallback) + ADM-0 §1.3 admin god-mode 不挂 + 0-server-prod 系列模式承袭 (CS-1/2/3 / CV-9..14 / DM-5..6 / DM-9 / CHN-11..12 / CS-4 第 16 处). REG-CS4-001..006 共 **6 行 🟢**. docs/current sync (`client/idb-cache.md`). 新增 dev-dep `fake-indexeddb@^6.2.5` (vitest 跑 IDB 必需, jsdom 不带原生 IDB). |
| 2026-04-30 | 战马E | HB-2.0 prerequisite 整 milestone 一 PR (Phase 4+ host-bridge stack 第 2 步 — HB-2 v0(C) Go daemon 实施前置, **真 infra PR 跟 INFRA-2 #195 Playwright scaffold 同模式**, 不开 docs-only): spec brief `hb-2-0-spec.md` 45 行 + stance `hb-2-0-stance-checklist.md` 46 行 + acceptance `hb-2-0.md` 56 行 + content-lock 不需 (CI infra + helper package 无 UI). HB-2.0.1 helper package single source `packages/server-go/internal/helper/ipc.go` 30 行 production (IPCPlatform 三态 const linux-uds/darwin-uds/windows-named-pipe + IPCEndpointDefault switch 函数) + HB-2.0.2 3 IPC unit per platform build-tag-per-platform (`ipc_linux_test.go` build tag linux + `ipc_darwin_test.go` build tag darwin + `ipc_windows_test.go` build tag windows; 各字面对比 endpoint default + label byte-identical 反向断 IPC primitive drift) + HB-2.0.3 CI matrix `.github/workflows/ci.yml` 加 `hb20-ipc-prereq` job 18 行 (strategy.matrix.os = [ubuntu-latest, macos-latest, windows-latest] + fail-fast: false + runs-on: ${{ matrix.os }} + go test ./internal/helper/...) + HB-2.0.4 closure REG-HB20-001..003 3 🟢. 立场 3 项: ① CI matrix 加 macOS + Windows runner (反 HB-2 v0(C) 实施时 Linux-only CI 漏 macOS sandbox-exec / Windows Named Pipe 边界) + ② IPC primitive 选择器单源 helper package (反双源 drift, 反向 grep IPCPlatform/IPCEndpointDefault 在 internal/ 除 helper/ 0 hit + 3 字面 endpoint 在 internal/ 除 helper/ + 除 _test 0 hit) + ③ 3 IPC unit per platform build tag (反 HB-2 v0(C) 实施时改 const 漏跨 OS unit). 跨 milestone byte-identical 锁链: INFRA-2 #195 Playwright scaffold 真 infra PR 同模式 + HB-1 #491 install-butler audit log schema 跨 OS 同精神 + HB-2 spec §1.3 红线 admin god-mode 不挂 host IPC + HB-3 host_grants schema (HB-2 v0(C) read-only consumer 接入点) + HB stack 4 步路径 (HB stack Go spec patch #599 → 本 PR HB-2.0 → HB-2 v0(C) → HB-3) + AL-1a 6-dict + HB-1 7-dict + HB-2 8-dict 三字典分立 (HB-2.0 仅 IPC primitive label, 不入 reason 字典). 不在范围: HB-2 v0(C) Go daemon binary ~400-500 行 packages/borgee-helper/cmd/ (留 follow-up PR 跟 HB-3 host_grants schema 同期接) / UDS server / Named Pipe server 真启 (留 v0(C)) / grants_consumer trait + mock impl (留 v0(C)) / cross-agent ACL 真测 (留 v0(C)) / systemd unit + launchd unit + sandbox-exec profile (留 v0(C)) / HB-3 host_grants schema (留 HB-3 spec brief) / admin god-mode IPC (永久不挂 ADM-0 §1.3 跟 HB-2 spec §1.3 红线). server-go ./internal/helper/ Linux runner 本地 TestHB20_IPC_UDSConnect_Linux PASS (macOS + Windows runner 真验留 CI matrix run); 反向 grep 单源闸 0 hit 验证. |
| 2026-04-30 | 战马E | HB-2 v0(C) host-bridge daemon 整 milestone 一 PR (Phase 4+ host-bridge stack 第 3 步, HB stack Go redesign 真启 — `packages/borgee-helper` 独立 Go module Go daemon binary, UDS POSIX + handshake + ACL gate + audit + sandbox build-tag stub; 跟 HB-2.0 #605 prereq + #599 HB stack Go spec patch 同源链): spec brief `hb-2-v0c-spec.md` 70 行 (≤80) + stance `hb-2-v0c-stance-checklist.md` 51 行 + acceptance `hb-2-v0c.md` 80 行 + content-lock 不需 (CI infra + helper package 无 UI). HB-2 v0(C).1 module separate `packages/borgee-helper/go.mod` (反 server-go binary bloat, hb-2-spec §5.5 拆死) + HB-2 v0(C).2 reasons 8-dict (`internal/reasons/reasons.go` 27 行 — 8 字面 byte-identical 跟 hb-2-spec §3.3, 反向断 HB-1 7-dict + AL-1a 6-dict 字面 0 hit 三字典分立) + HB-2 v0(C).3 audit (`internal/audit/audit.go` 48 行 — 5-field SSOT JSON-line writer byte-identical 跟 HB-1 install-butler audit log schema, reject 也写) + HB-2 v0(C).4 grants (`internal/grants/grants.go` 94 行 — Consumer interface + MemoryConsumer mock; HB-3 落地后 SQLite consumer 直接实现同 interface 不改 daemon 一行; 不缓存 — 反向 grep `grantsCache|cachedGrants` 0 hit; 撤销 < 100ms 生效 HB-4 release gate 第 5 行守; grant_expired vs grant_not_found 字面区分 LookupRaw 双 bool) + HB-2 v0(C).5 acl (`internal/acl/acl.go` 128 行 — path normalize anti-traversal + cross-agent ACL handshake!=request reject + 写类 100| 2026-04-30 | 战马E | HB-2 v0(C) host-bridge daemon 整 milestone 一 PR (Phase 4+ host-bridge stack 第 3 步, HB stack Go redesign 真启 — `packages/borgee-helper` 独立 Go module Go daemon binary, UDS POSIX + handshake + ACL gate + audit + sandbox build-tag stub; 跟 HB-2.0 #605 prereq + #599 HB stack Go spec patch 同源链): spec brief `hb-2-v0c-spec.md` 70 行 + stance 51 行 + acceptance 80 行 + content-lock 不需 (CI infra + helper package 无 UI). HB-2 v0(C).1 module separate `packages/borgee-helper/go.mod` (反 server-go binary bloat, hb-2-spec §5.5 拆死) + HB-2 v0(C).2 reasons 8-dict (`internal/reasons/reasons.go` 27 行 — 8 字面 byte-identical 跟 hb-2-spec §3.3, 反向断 HB-1 7-dict + AL-1a 6-dict 字面 0 hit 三字典分立) + HB-2 v0(C).3 audit (`internal/audit/audit.go` 48 行 — 5-field SSOT JSON-line writer byte-identical 跟 HB-1 install-butler audit log schema, reject 也写) + HB-2 v0(C).4 grants (`internal/grants/grants.go` 94 行 — Consumer interface + MemoryConsumer mock; HB-3 落地后 SQLite consumer 直接实现同 interface 不改 daemon 一行; 不缓存 — 反向 grep `grantsCache|cachedGrants` 0 hit; 撤销 < 100ms 生效 HB-4 release gate 第 5 行守; grant_expired vs grant_not_found 字面区分 LookupRaw 双 bool) + HB-2 v0(C).5 acl (`internal/acl/acl.go` 128 行 — path normalize anti-traversal + cross-agent ACL handshake!=request reject + 写类 100%% reject 反向枚举 12 写类 write_file/delete_file/chmod/chown/mkdir/rmdir/mv/cp/exec/shell/rename/truncate, 反约束 #2+#4+#7) + HB-2 v0(C).6 ipc (`internal/ipc/ipc.go` 151 行 — handshake first message + JSON-line protocol + request_id 多路复用单连接 + reject 走 audit) + HB-2 v0(C).7 sandbox build tag 拆死 (`sandbox_{linux,darwin,other}.go` 61 行 — Platform 字面单一, v0(C) Apply no-op stub, 留 v0(D) 真 landlock LSM/sandbox-exec) + HB-2 v0(C).8 main (`cmd/borgee-helper/main.go` 98 行 + main_other.go 16 行 — UDS POSIX listener default `/run/borgee-helper/borgee-helper.sock` byte-identical 跟 HB-2.0 #605 IPCEndpointDefault, signal SIGINT/SIGTERM clean shutdown; main_other.go build !linux && !darwin 提示 v0(D)) + HB-2 v0(C).9 closure REG-HB2-001..006 6 🟢. 立场 4 项: ① 0 server-go diff (独立 Go module, 反向 grep package server 在 packages/borgee-helper/ 0 hit) + ② 8-dict reason 字典分立 + ③ read-only Consumer 接口锚 (HB-3 接入点) + ④ sandbox build tag 拆死 (v0(C) stub, v0(D) 真接). 跨 milestone byte-identical 锁链: HB-2.0 #605 IPCEndpointDefault 字面承袭 + HB-1 #491 install-butler audit log schema 5-field SSOT + HB-2 spec §1.3 红线 admin god-mode 不挂 host IPC + HB-3 host_grants schema (Consumer 是接入点) + HB-4 release gate 第 5 行 (< 100ms 撤销 TestHB23_RevocationImmediate 守) + 第 6 行 (写类 100%% reject TestHB24_WriteActions100PercentRejected 12 写类反向枚举守) + HB stack 4 步路径 #599 → #605 → 本 PR → HB-3 + AL-1a 6-dict + HB-1 7-dict + HB-2 8-dict 三字典分立 + ADM-0 §1.3 红线 + REG-INV-002 fail-closed cross-agent + anchor #360 owner-only 立场延伸到 host 层. 不在范围 (留 v0(D) / HB-3): Windows Named Pipe 真启 (留 v0(D) go-winio) / 真 landlock LSM 调用 (留 v0(D) go-landlock, HB-1 #589 binary 后接) / 真 sandbox-exec profile 生成 (留 v0(D)) / systemd + launchd unit (留 v0(D) install-butler 拉起) / HB-3 SQLite consumer (留 HB-3) / 文件 read_file+list_files 真 IO (v0(C) 仅 ACL 决策) / 网络出站 outbound proxy 真接 (留 v0(D)). 总计 production: ~623 行 / 总计 test: ~461 行 27 unit. cd packages/borgee-helper && go test -timeout=60s -race ./... 全 6 packages PASS (reasons 2 + audit 3 + grants 4 + acl 8 + ipc 5 + sandbox 2) + go build ./... PASS. |
| 2026-04-30 | 战马E | DM-12 整 milestone 一 PR (Phase 5+ — DM message reaction picker composite, **0 server production code** 复用 CV-7 #535 + AP-4 #551 endpoint + DM-9 #585 picker + DM-5 #549 chip composite; 跟 CV-9..14 / DM-5/6/9/10/11 client-only 同模式): spec brief `dm-12-spec.md` 70 行 + stance 60 行 + acceptance 50 行 + content-lock §1+§2+§3 (DOM SSOT + delegate DM-9/DM-5 SSOT). DM-12.1 server 反向断言 0 行 + DM-12.2 client (`packages/client/src/components/DMMessageReactionPicker.tsx` 新 composite — 复用 EmojiPickerPopover + ReactionSummary + auto-fetch on mount when initialReactions undefined + refetch on add/toggle; 7 vitest PASS — root-render + empty-no-summary + non-empty-chips + auto-fetch + addReaction-refetch-chain + DOMAttrs + NoStorageWrite) + DM-12.3 closure (REG-DM12-001..006 6 🟢). 立场 5 项: ① 0 server production code (复用 CV-7 endpoint + AP-4 ACL gate) + ② DM-only mounting path (composite 不感知 channel context, 父组件 follow-up wire-up PR 决定 channel.type === 'dm' 时挂) + ③ 复用 DM-9 + DM-5 不另起组件 (反向 grep `EMOJI_PRESET` + `👍❤️😄🎉🚀` 在 DMMessageReactionPicker.tsx 0 hit) + ④ thinking 5-pattern 锁链第 12 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13+CV-14+DM-9+DM-12) + ⑤ DOM 2 data-attr (data-dm12-reaction-picker + data-dm12-loading) + delegate DM-9/DM-5 锚. 跨 milestone byte-identical 锁链: DM-9 #585 + DM-5 #549 + CV-7 #535 + AP-4 #551 + thinking 锁链第 12 处 + ADM-0 §1.3 红线 + DM-10 #597 + DM-11 #600 client-only 同模式. 不在范围: MessageItem.tsx 真挂集成 (留 follow-up wire-up PR — channel.type 守 + 替换既有 ReactionBar 重 emoji-mart 包) / admin god-mode reaction visibility (永久不挂) / custom emoji preset (留 v3 跟 DM-9 同期) / optimistic UI (留 v2) / schema 改. vitest 77 files / 543 tests 全 PASS (含 DMMessageReactionPicker 7 case) + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行验证. |
| 2026-04-29 | 战马D | DM-3 整 milestone 一 PR 落 (Phase 4+ 候选 — agent-DM 多端同步): spec brief `dm-3-spec.md` 80 行 (≤80) + stance `dm-3-stance-checklist.md` 63 行 (≤80) + acceptance `dm-3.md` 7 项 ✅ + DM-3.1 server 0 行新增 (3 反约束 grep test PASS — `TestDM31_NoBypassEndpoint` / `_BackfillIncludesDMChannel` / `_NoBypassFrame`) + DM-3.2 client `useDMSync.ts` hook + 5 vitest PASS (cold-start / monotonic / page-reload / corrupt-clamp / multi-device) + DM-3.3 e2e `dm-3-multi-device-sync.spec.ts` REST-driven dual-tab cursor reuse + thinking 5-pattern 反向断言. 立场 5 项: ① cursor 复用 RT-1.3 (不开 /dm/sync 旁路) + ② 多端走 RT-3 fan-out (不开 dm-only frame) + ③ thinking 5-pattern 延伸 (RT-3 #488 byte-identical) + ④ useDMSync hook 复用 useArtifactUpdated/lastSeenCursor 模式 + ⑤ server 0 行新增. 跨 milestone byte-identical 锁链: cursor 跟 RT-1 #290 + AL-2b #481 + CV-* + BPP-3.1 #494 共一根 sequence; useDMSync 跟 CV-1.3 #346 同模式; ≤3s 跟 RT-1.2 #292 同源. 不在范围: e2ee / 跨 org (AP-3) / CHN-3 layout / offline DM (DL-4 已盖). REG-DM3-001..005 占号 (待烈马 PR merge 后翻 🟢). client vitest 36 files / 303 tests 全 PASS, server-go ./internal/api/ DM-3.1 3 tests PASS. |
| 2026-04-29 | 战马D | BPP-6 整 milestone 一 PR (Phase 4+ plugin-protocol 真收尾 — plugin cold-start handshake + state re-derive): spec brief `bpp-6-spec.md` 50 行 (≤80) + stance `bpp-6-stance-checklist.md` 69 行 (≤80) + acceptance `bpp-6.md` 40 行 (≤50) + content-lock 不需 (server-only). BPP-6.1 frame schema (envelope 14→15, ColdStartHandshakeFrame 5 字段 byte-identical 字段集**与 ReconnectHandshakeFrame 互斥** — 不含 LastKnownCursor/DisconnectAt/ReconnectAt; spec §0.1 立场守门, BPP-1 #304 reflect lint 自动覆盖) + BPP-6.2 server handler (`internal/bpp/cold_start_handler.go` — agent.Tracker.Clear + AL-1 #492 single-gate AppendAgentStateTransition any→online + reason 复用 `runtime_crashed` 6-dict byte-identical, 不重放历史 frame 反向 BPP-5; AgentStateAppender interface seam, OwnerResolver/AgentErrorClearer 复用 BPP-5 同模式; 6 unit PASS — 字段顺序锁 / direction lock / 字段互斥反断 / 3 valid edges initial/error/offline → online / cross-owner reject + nil-safe ctor / restart count 反向 derive + AST scan ResolveResume 反向 + AST scan cold-start-queue 6 forbidden tokens 0 hit 锁链延伸 BPP-4+BPP-5 第 3 处) + BPP-6.3 closure (REG-BPP6-001..006 6 🟢; 总计 334→340 / active 308→314 / pending 26 不变). 立场 3 项: ① cold-start ≠ reconnect (字段集互斥) + ② state 重新 derive (Tracker.Clear + single-gate + 不重放) + ③ restart count 仅 audit (复用 runtime_crashed, COUNT 反向 derive 不另开列). 跨 milestone byte-identical 锁链: BPP-1 #304 envelope reflect lint + BPP-3 #489 PluginFrameDispatcher 复用 + BPP-5 #503 反向帧 + AL-1 #492 single-gate + REFACTOR-REASONS #496 6-dict 不扩 + AL-1 reason 锁链第 11 处 + ADM-0 §1.3 红线. 不在范围: cross-plugin restart takeover (v2) / SDK backoff / restart rate limit / state graph 不加 cold_starting transient 中间态 (跟 BPP-5 connecting deferred 同精神). server-go internal/bpp + internal/server tests 全 PASS. |
| 2026-04-30 | 战马D | CV-4 v2 整 milestone 一 PR (Phase 4+ — canvas iteration history list + timeline UI 续, **0 schema 改**): spec brief `cv-4-v2-spec.md` 49 行 (≤80) + stance `cv-4-v2-stance-checklist.md` 64 行 (≤80) + acceptance `cv-4-v2.md` 38 行 (≤50) + content-lock 不需 (server 仅加 limit query + state badge label 跟 CV-4 v1 #380 已锁). CV-4.1 v2 server (`internal/api/iterations.go::handleListIterations` — CV-4 v1 #414 endpoint 复用 + 加 `?limit=N` query clamp default 50/max 200; cv4v2ClampLimit helper + ClampCV4V2LimitForTest test-only export; 0 schema 改) + CV-4.2 client (`packages/client/src/components/IterationTimeline.tsx` — DESC list + 4-state badge byte-identical 跟 CV-4 v1 #380 + intent_text + thumbnail 复用 artifact_versions.preview_url CV-3 v2 #517 字段 + onJump callback; 4 vitest PASS — 4 态 badges + thumbnail 复用 + 空状态 + DoesNotWriteOwnCursor 反向断言) + CV-4.3 closure (REG-CV4V2-001..005 5 🟢; 总计 353→358 / active 327→332 / pending 26 不变). 立场 3 项: ① iteration history 复用既有 path (反向 grep `iteration_history_event\|artifact_iteration_log\|iteration_history_table` 0 hit) + ② thumbnail history 不存 (复用 CV-3 v2 #517 preview_url, 反向 grep `thumbnail_history\|preview_history\|version_thumbnail_snapshot` 0 hit) + ③ cursor 复用 RT-1.1 useArtifactUpdated + admin god-mode 不挂 (反向 grep `admin/iterations\|AdminListIterations\|AdminCV4` 在 admin*.go 0 hit + 反向断言 client borgee.cv4.cursor:* sessionStorage 0 hit). 跨 milestone byte-identical 锁链: CV-4 v1 #398/#414/#417 artifact_iterations 表 + CV-1 #348 commits endpoint + RT-1 #290 ArtifactUpdated frame + CV-3 v2 #517 preview_url + AL-2a/BPP-3.2/AL-1/AL-5/DM-4 owner-only 6 处 + ADM-0 §1.3 红线. 不在范围: iteration retry server queue (留 v3) / iteration-level comment (留 v3) / thumbnail 历史快照表 (永久不挂) / schema 改 (CV-4 v1 表已盖) / admin god-mode iteration history / 跨 owner cross-org (留 AP-3) / 独立 cursor 字典. server-go ./internal/api/ TestCV4V2_LimitClamp + NoSchemaChange + AdminGodModeNotMounted PASS + client vitest 4 PASS. |
| 2026-04-30 | 战马D | BPP-8 整 milestone 一 PR (PR #532) (Phase 4+ — plugin lifecycle audit log, **0 新表** 复用 admin_actions): spec brief `bpp-8-spec.md` 50 行 (≤80) + stance `bpp-8-stance-checklist.md` 62 行 (≤80) + acceptance `bpp-8.md` 39 行 (≤50) + content-lock 不需 (内部 audit log 无 DOM/UI). BPP-8.1 schema migration v=31 (`internal/migrations/bpp_8_1_admin_actions_plugin_actions.go` admin_actions CHECK 12-step rebuild 6 → 11 项加 5 条 plugin_* 字面: connect/disconnect/reconnect/cold_start/heartbeat_timeout; 跟 CV-3.1/CV-2 v2/AP-2 同模式) + BPP-8.2 server (`internal/bpp/lifecycle_audit.go` LifecycleAuditor interface + AdminActionsLifecycleAuditor 5 method, recordEvent 走 Store.InsertAdminAction with actor='system' + 复用 reasons.NetworkUnreachable/RuntimeCrashed 6-dict byte-identical, AL-1a 锁链第 13 处; `internal/api/bpp_8_lifecycle_list.go` GET /api/v1/agents/{agentId}/lifecycle owner-only ACL + limit clamp default 100/max 500 + non-plugin actions 排除; server.go 加 bpp8Handler register) + BPP-8.3 closure (REG-BPP8-001..006 6 🟢; 总计 364→370 / active 338→344 / pending 26 不变). 立场 3 项: ① 5 事件复用 admin_actions 表 (反向 grep plugin_lifecycle_events 等 0 hit, audit forward-only 锁链第 5 处) + ② reason 复用 6-dict (heartbeat_timeout=NetworkUnreachable/cold_start=RuntimeCrashed, AL-1a 锁链第 13 处) + ③ owner-only 视图 (跟 AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7 7 处同模式; admin god-mode 不挂 ADM-0 §1.3). 跨 milestone byte-identical 锁链: BPP-3 #489 dispatcher + BPP-4 #499 watchdog + BPP-5 #503 reconnect + BPP-6 #522 cold_start + BPP-7 SDK + AL-1 #492 + REFACTOR-REASONS #496 6-dict (锁链第 13 处) + ADM-2.1 #484 admin_actions audit forward-only 锁链第 5 处 + AP-2 #525 sweeper + ADM-0 §1.3 红线 + AST 锁链延伸第 5 处 (BPP-4/5/6/7/8 forbidden tokens 全 0 hit). 不在范围: metrics exporter (v3) / WS push (v3) / cross-org agg (v3) / retention policy (v3) / admin god-mode SDK / 6-dict 扩第 7 / plugin_lifecycle_events 表 / retry queue. 23 unit PASS (migrations 5 + bpp 11 + api 7). |
| 2026-04-30 | 战马D | BPP-7 整 milestone 一 PR (spec PR #529) (Phase 4+ 起步 — plugin SDK 真接入 in `packages/server-go/sdk/bpp/` 同 module 共享 envelope 0 drift): spec brief `bpp-7-spec.md` 50 行 (≤80) + stance `bpp-7-stance-checklist.md` 69 行 (≤80) + acceptance `bpp-7.md` 43 行 (≤50) + content-lock 不需 (SDK 内部无 DOM/UI). BPP-7.1 SDK Client + Connect (`sdk/bpp/client.go` — Client struct {PluginID, AgentID, conn, lastKnownCursor} + NewClient nil-safe ctor + Connect ws.Dial + ConnectFrame 5 字段 byte-identical 复用 srvbpp.ConnectFrame + AdvanceCursor monotonic 立场承袭 RT-1.3 + SendHeartbeat HeartbeatFrame 6 字段 + Close) + BPP-7.2 Reconnect/ColdStart/HeartbeatLoop/GrantRetry (`sdk/bpp/reconnect.go` — Reconnect 走 BPP-5 ReconnectHandshakeFrame 携 last_known_cursor; ColdStart 走 BPP-6 ColdStartHandshakeFrame **不**携 cursor 字段集互斥反断 + RestartReason=reasons.RuntimeCrashed byte-identical AL-1a 锁链第 12 处; HeartbeatLoop 30s ticker; GrantRetry MaxPermissionRetries=3 + RetryBackoff=30s const re-export from srvbpp byte-identical 跟 BPP-3.2.3 RequestRetryCache 同行为) + BPP-7.3 closure (REG-BPP7-001..006 6 🟢; 总计 358→364 / active 332→338 / pending 26 不变). 立场 3 项: ① SDK frame schema 跟 server byte-identical (SDK 不重定义 frame, AST + reflect 双锁) + ② SDK 同 module 复用 envelope + ws 库 `github.com/coder/websocket` 同源 (best-effort 锁链延伸第 4 处, AST scan forbidden 8 token 0 hit) + ③ BPP-3.2.3 retry + BPP-4 watchdog + BPP-5/6 reconnect/cold-start SDK side 真实施 + reason 复用 6-dict (AL-1a 锁链第 12 处). 跨 milestone byte-identical 锁链: BPP-1 #304 envelope reflect lint + BPP-3 #489 dispatcher (server-only 边界) + BPP-3.2 #498 retry cache (const 复用) + BPP-4 #499 watchdog (30s 周期同源) + BPP-5 #503 reconnect 帧 + BPP-6 #522 cold-start 帧 + AL-1 #492 + REFACTOR-REASONS #496 6-dict (锁链第 12 处) + ADM-0 §1.3 红线. 不在范围: JS/TS port (v2) / auth rotation (v2) / metrics (v3) / structured logger (v2) / plugin marketplace (Phase 7) / SDK 独立定义 frame / admin god-mode SDK / 6-dict 扩第 7 / e2e 真启 server 整链 (留 follow-up). server-go ./sdk/bpp/ 12 unit PASS. |
| 2026-04-29 | 战马D | DM-4 整 milestone 一 PR (spec PR #523) (Phase 4+ — agent message edit 多端同步): spec brief `dm-4-spec.md` 49 行 (≤80) + stance `dm-4-stance-checklist.md` 67 行 (≤80) + acceptance `dm-4.md` 38 行 (≤50) + content-lock 不需 (server-only PATCH + client hook). DM-4.1 server PATCH `/api/v1/channels/{channelId}/messages/{messageId}` (`internal/api/dm_4_message_edit.go` — DM-only ACL channel.Type != "dm" → 403 `dm.edit_only_in_dm` + owner-only ACL sender == user → 403 `dm.edit_non_owner_reject` + REG-INV-002 cross-org fail-closed + body schema {content} + events INSERT kind="message_edited" + payload op="edit" 复用 RT-3 fan-out 路径 + Hub.BroadcastEventToChannel 多端覆盖; 6 unit PASS — HappyPath / NonOwnerRejected / NonDMReject / Unauthorized401 / NotFound404 / NoThinkingPatternInBody 反向 grep 5 字面 0 hit) + DM-4.2 client `useDMEdit.ts` hook (`packages/client/src/hooks/useDMEdit.ts` PATCH wrapper via `patchDMMessage` API + optimistic update + isEditing/error 状态; **反向不写** borgee.dm4.cursor:* sessionStorage — cursor 进展全归 useDMSync DM-3 #508; 4 vitest PASS — HappyPath patchDMMessage 真调 + 空 content reject + DoesNotWriteOwnCursor + multi-device useDMSync cursor 不被破坏) + DM-4.3 closure (REG-DM4-001..005 5 🟢; 总计 340→345 post BPP-6 / active 314→319 / pending 26 不变). 立场 3 项: ① RT-3 既有 fan-out 复用 (events INSERT op="edit", 不另起 channel/frame/sequence) + ② edit 是 cursor 子集 (RT-3 fan-out 已建 + useDMSync 已订阅, 不另起 sequence) + ③ thinking 5-pattern 反约束延伸第 3 处 (RT-3 第 1 + DM-3 第 2 + DM-4 第 3, agent edit 是机械修订不暴露 reasoning). 跨 milestone byte-identical 锁链: RT-3 #488 fan-out + DM-3 #508 cursor 复用 RT-1.3 + AL-2a/BPP-3.2/AL-1/AL-5 owner-only 5 处同模式 + ADM-0 §1.3 红线 + REG-INV-002 cross-org fail-closed + RT-1.2 #292 ≤3s latency. 不在范围: edit history audit (v2) / OT-CRDT conflict (v2) / edit window 时间限制 (v2) / edit push 通知 (RT-3.2 follow-up). server-go ./internal/api/ TestDM41 5 unit PASS + client vitest 4 PASS + server.go register dm4EditHandler 真挂. |
| 2026-04-30 | 战马D | AL-7 整 milestone 一 PR (PR #536) (Phase 4+ — audit log retention + archive, audit 5 字段链终结收尾 + retention 14d default + admin override; **0 新表** 复用 admin_actions.archived_at): spec brief `al-7-spec.md` 51 行 (≤80) + stance `al-7-stance-checklist.md` 66 行 (≤80) + acceptance `al-7.md` 41 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3). AL-7.1 schema migration v=33 (`internal/migrations/al_7_1_admin_actions_archived_at.go` ALTER admin_actions ADD COLUMN archived_at INTEGER NULL 跟 AP-1.1 expires_at + AP-3.1 org_id + AP-2.1 revoked_at 跨四 milestone 同模式 + sparse idx WHERE archived_at IS NOT NULL byte-identical 跟 AP-2.1 + admin_actions CHECK 12-step rebuild 11→12 项加 'audit_retention_override' 字面跟 CV-3.1/CV-2 v2/AP-2/BPP-8 同模式; v=33 sequencing post-CV-6 v=32) + AL-7.2 server (`internal/auth/audit_retention_sweeper.go` RetentionSweeper 1h ticker + ctx-aware shutdown + RunOnce 同步入口跟 AP-2 ExpiresSweeper #525 同模式 nil-safe; UPDATE archived_at = now WHERE created_at < cutoff AND archived_at IS NULL **不真删** forward-only soft-archive; RetentionDays=14 / RetentionMinDays=1 / RetentionMaxDays=365 const 单源; SweeperReason=reasons.Unknown byte-identical AL-1a 锁链第 15 处 + `internal/api/al_7_audit_retention_override.go` POST /admin-api/v1/audit-retention/override admin-rail only 写 admin_actions audit row action='audit_retention_override' ADM-0 §1.3 红线; server.go 加 al7RetentionHandler.RegisterAdminRoutes + RetentionSweeper.Start) + AL-7.3 closure (REG-AL7-001..006 6 🟢; 总计 370→376 / active 344→350 / pending 26 不变). 立场 3 项: ① retention 走 admin_actions.archived_at 列 + sweeper 不裂表/不真删 (反向 grep `audit_archive_table\|audit_history_log\|al7_archive_log` 0 hit + `DELETE FROM admin_actions` 0 hit, audit forward-only 锁链第 6 处) + ② admin override 复用 audit + reason 复用 6-dict (AL-1a 锁链第 15 处, HB-3 v2 #14 承袭不漂) + ③ admin-rail only ADM-0 §1.3 红线. 跨 milestone byte-identical 锁链: AP-2 #525 ExpiresSweeper time.Ticker 同模式 + AP-1.1/AP-3.1/AP-2.1 ALTER ADD COLUMN nullable 跨四 milestone + ADM-2.1 #484 admin_actions audit forward-only 锁链第 6 处 + BPP-4/5/6/7/8 + HB-3 v2 audit/best-effort 锁链 + AL-1 #492 + REFACTOR-REASONS #496 6-dict (AL-1a reason 锁链第 15 处) + ADM-0 §1.3 红线 + AST 锁链延伸第 7 处 (BPP-4+5+6+7+8 + HB-3 v2 + AL-7 forbidden tokens `pendingRetentionQueue / retentionRetryQueue / deadLetterRetention` 全 0 hit) + cron 框架反向 grep `robfig/cron / gocron` 0 hit 跟 AP-2 立场承袭. 不在范围: admin dashboard UI (v3) / runtime hot-mutate retention 窗口 (v0 仅留痕) / Prometheus metrics exporter (v3) / archive 数据搬移到 cold storage (v3) / 跨 org retention policy (v3) / admin god-mode override 历史回看 (admin-api/v1/audit-log 已盖) / 另开 audit_archive 表 / retention queue. 18 unit PASS (migrations 7 + auth 8 含 AST scan + api 3 含 ClampsRetention 6 sub-case). |
| 2026-04-30 | 战马D | AL-8 整 milestone 一 PR (PR #538) (Phase 4+ audit retention 互补 — admin 看 active vs archived audit; 既有 GET /admin-api/v1/audit-log filter 加 since/until/archived/actions, **0 schema / 0 新 endpoint** 复用 AL-7 #533 archived_at + ADM-2.2 既有 path): spec brief `al-8-spec.md` 79 行 (≤80) + stance `al-8-stance-checklist.md` 69 行 (≤80) + acceptance `al-8.md` 38 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3). AL-8.1 schema 0 行 (复用 AL-7.1 archived_at + sparse idx) + AL-8.2 server filter 扩 (`internal/store/admin_actions.go::AdminActionListFilters` 加 Since/Until/ArchivedView/Actions 顺位 append 既有 3 字段 byte-identical 不动 + ListAdminActionsForAdmin Where 链按 nil-safe 加 since/until BETWEEN + archived 三态 switch + actions IN slice; `internal/api/adm_2_2_endpoints.go::handleAdminAuditLog` 加 query parse since/until int64 ms epoch + archivedView 三态 + actions 多值 r.URL.Query()["action"] Slice + 错码字面单源 audit_log.{time_range_invalid/time_range_inverted/archived_view_invalid}) + AL-8.3 closure (REG-AL8-001..006 6 🟢; 总计 376→382 / active 350→356 / pending 26 不变). 立场 3 项: ① 不另起 endpoint (additive filter 既有 GET 路径单源) + ② admin-rail only ADM-0 §1.3 + ③ archived 三态 (active 默认走 archived_at IS NULL 跟 AL-7.1 sparse idx 反向同源; archived 走 idx_admin_actions_archived_at 现网零开销; all 无 WHERE). 跨 milestone byte-identical 锁链: ADM-2.2 #484 既有 GET path + 3-filter byte-identical 不动 + AL-7 #533 archived_at 列 + sparse idx 反向同源 + ADM-0 §1.3 红线 admin-rail only + AL-1a reason 锁链 AL-8 = 第 16 处 (AL-7 #15 承袭, 复用 reasons.Unknown 跟 AL-7 SweeperReason 同源) + audit 5 字段链第 8 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8) + AST 锁链延伸第 8 处 (BPP-4+5+6+7+8 + HB-3 v2 + AL-7 + AL-8 forbidden tokens `pendingAuditQuery / auditQueryRetryQueue / deadLetterAuditQuery` 全 0 hit) + parseLimit helper default 100/max 500 复用 ADM-2.2 同源. 不在范围: admin SPA dashboard UI (留 v3) / audit export CSV/NDJSON (留 v3) / audit metadata JSON path search (留 v4) / Prometheus exporter (留 v3) / audit cross-org filter (留 v3 跟 AP-3 同期) / user-rail audit search (永久不挂 ADM-0 §1.3) / audit row write/update/delete (forward-only AL-7 锁) / audit retention runtime hot-mutate (留 v3). 14 unit PASS — TestAL81_* 3 (NoSchemaChange / NoNewEndpoint / NoUserRailAuditLog) + TestAL82_* 10 (ArchivedView 4 sub-case + TimeRange 3 + Actions 2 + RejectsUserRail + ReasonChain_NotExpanded) + TestAL83_NoAuditQueryQueue 1; ADM-2.2 既有 全套 不破. server-go ./... 全 25 packages 全绿. |
| 2026-04-30 | 战马E | CV-13 整 milestone 一 PR (Phase 4+ — artifact comment quote / reference 视觉块, **0 server production code** 复用 reply_to_id CV-8 #441; 跟 CV-9..12 / DM-5 / AP-4 / AP-5 0-server client-only 同模式): spec brief `cv-13-spec.md` 67 行 (≤80) + stance `cv-13-stance-checklist.md` 70 行 + acceptance `cv-13.md` 38 行 + content-lock `cv-13-content-lock.md` (文案 + DOM SSOT 单源). CV-13.1 server 反向断言 (git diff origin/main -- packages/server-go/ 0 production lines + 反向 grep `quote_event\|comment_quote_table\|cv13.*server` 在 internal/ 0 hit; 复用 messages.reply_to_id CV-8 #441 既有列 + 既有 fetchMessages list 返字段) + CV-13.2 client (`packages/client/src/components/QuotedCommentBlock.tsx` 新组件 props-driven 渲染 — quotedMessage Message|null + collapse toggle + 200-char truncate + missing fallback "(原消息已删除)" byte-identical; ArtifactCommentItem.tsx 加 props quotedMessage wire ≤10 行 — 父组件从 messages list 内存 cache lookup parent reply_to_id; 7 vitest PASS — HappyPath + MissingFallback + DeletedFallback + CollapseToggle + Truncate200 + DOMAttrs + NoStorageWrite) + CV-13.3 closure (REG-CV13-001..005 5 🟢; 总计 382→387 / active 356→361 / pending 26 不变). 立场 5 项: ① 0 server production code (反向断言 git diff 0 行) + ② quote 块从内存 cache 渲染 (props-driven 不 import api/fetch*) + ③ thinking 5-pattern 反约束锁链第 9 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13) + ④ 文案 byte-identical 6 字面 (> quote prefix + @ author + 展开/收起 collapse + (原消息已删除) fallback + … ellipsis + 200 chars truncate length; content-lock SSOT) + ⑤ DOM 4 data-attr (data-cv13-quoted-block + -author + -id + -collapsed). 跨 milestone byte-identical 锁链: CV-7 #535 ArtifactCommentItem 续 + CV-8 #441 reply_to_id 列 SSOT + CV-9 #539 mention preview 内存 cache 同精神 + CV-11 #543 markdown 不入 quote (纯文本截断) + thinking 5-pattern 锁链第 9 处 + ADM-0 §1.3 红线 + DM-5/AP-4/AP-5 0-server client-only 同模式. 不在范围: quote chain 递归 (留 v2) / markdown 富文本 quote (CV-11 markdown 仅对 body) / admin god-mode quote audit (永久不挂) / schema 改 / 新 endpoint / quote 跨 channel / quote 编辑历史 (forward-only 同精神) / e2e (deferred 复用 CV-7 e2e harness, 留 follow-up). vitest 47 files / 397 tests 全 PASS + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行验证. |
| 2026-04-30 | 战马D | DM-6 整 milestone 一 PR (Phase 4+ DM thread reply UI; **0 schema 改 / 0 server production code** — messages.reply_to_id 既有列 + POST /channels/{id}/messages 既有接受 reply_to_id byte-identical 不动. 跟 DM-5 #549 / CV-7 #535 / CV-9..12 一系列 0-server 模式承袭): spec brief `dm-6-spec.md` 75 行 (≤80) + stance `dm-6-stance-checklist.md` 59 行 (≤80) + acceptance `dm-6.md` 45 行 (≤50) + content-lock `dm-6-content-lock.md` 94 行. DM-6.1 server **0 production code** (`internal/api/dm_6_thread_test.go` _test.go ONLY: NoSchemaChange + NoServerProductionCode 双反向 grep + ReplyToIDColumnExists PRAGMA 反断 + DMThreadReply_HappyPath 走 既有 POST path + NoThinkingPatternInProduction 5-pattern 锁链第 9 处 + NoDMThreadQueue AST 锁链 #15) + DM-6.2 client (`packages/client/src/components/DMThread.tsx` 折叠 toggle + reply input + submit DOM byte-identical 跟 content-lock §1; toggle 文案 `▼ 隐藏 N 条回复` / `▶ 显示 N 条回复` byte-identical + N 计数动态; placeholder `回复...` 2 字 + submit `发送` 2 字; 空 thread null 不渲染; thread depth 1 层强制 — reply 行内不渲染 sub-thread toggle) + DM-6.3 closure (REG-DM6-001..006 6 🟢; §5 dup 总计 fix → 单一 405/379/26). 立场 3 项 + 3 边界: ① 0 schema / 0 server prod (跟 DM-5/CV-7/CV-9..12 同模式) + ② owner-only ACL 锁链第 18 处 (CHN-9 #17 承袭) + admin god-mode 不挂 + ③ thinking 5-pattern 锁链第 9 处 (DM-5 第 8 处承袭, RT-3+DM-3+DM-4+DM-5 反向断言延伸). 跨 milestone byte-identical 锁链: CHN-1 既有 messages.reply_to_id 列复用 (0 schema 改) + DM-3 #508 既有 channel.member ACL byte-identical 不动 + DM-5 #549 + CV-7 #535 + CV-9..12 0-server 模式承袭 + RT-3 #488 + DM-3/4/5 thinking 5-pattern 反向断言承袭 (锁链第 9 处) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 18 处一致 + audit 5 字段链第 15 处 + AST 锁链延伸第 15 处. 不在范围: thread depth >1 (永久不挂) / thread reply push (留 v3) / admin god-mode thread override (永久不挂) / thread reply audit (复用既有 messages) / cross-channel thread (永久不挂). 13 unit PASS — server-go 6 (TestDM61_NoSchemaChange + TestDM61_NoServerProductionCode + TestDM61_ReplyToIDColumnExists + TestDM61_DMThreadReply_HappyPath + TestDM61_NoThinkingPatternInProduction + TestDM63_NoDMThreadQueue) + client vitest 7 (DMThread.test.tsx: 折叠态/展开态文案 byte-identical + N 计数动态 + reply input + submit DOM byte-identical + 空 thread null + 同义词反向 + thread depth 1 层强制 + submit callback). server-go ./... 25 packages 全绿 + client ./... 47 files / 397 tests 全绿 (DM-3/DM-5/CV-7/CV-9..12 既有 unit 不破). |
| 2026-04-30 | 战马D | HB-3 v2 整 milestone 一 PR (Phase 4+ — heartbeat decay 三档 derive + cross-bucket audit + owner-only 视图, **0 schema 改**; 命名拆死跟 #507 host_grants 不交): spec brief `hb-3-v2-spec.md` 53 行 (≤80) + stance `hb-3-v2-stance-checklist.md` 66 行 (≤80) + acceptance `hb-3-v2.md` 42 行 (≤50) + content-lock 不需 (owner-only API 无 DOM/UI). HB-3 v2.1 `internal/bpp/heartbeat_decay.go` (DecayState enum 3 字面 fresh/stale/dead + DeriveDecayState 纯 fn + StaleThreshold=30s/DeadThreshold=60s const byte-identical 跟 BPP-4 watchdog + BPP-7 SDK HeartbeatInterval 同源 + IsCrossBucketTransition helper) + HB-3 v2.2 BucketAuditTrigger (HeartbeatTimeoutAuditor interface 解耦 BPP-8 PR 顺序; cross-bucket 调 RecordHeartbeatTimeout, 同档 no-op) + `internal/api/hb_3_v2_decay_list.go` GET /api/v1/agents/{agentId}/heartbeat-decay owner-only ACL (响应 `{agent_id, state, age_ms}`, 不漏 raw last_heartbeat_at; 复用 agent_runtimes.last_heartbeat_at 0 schema 改); server.go 加 hb3v2Handler register + HB-3 v2.3 closure (REG-HB3V2-001..005 5 🟢; 总计 364→369 / active 338→343 / pending 26 不变). 立场 3 项: ① decay 三档 derive 0 schema 改 (反向 grep heartbeat_decay_table 等 0 hit) + ② 复用 BPP-8 RecordHeartbeatTimeout 不另开 RecordHeartbeatStale 旁路 (AL-1a 锁链第 14 处, reasons.NetworkUnreachable 复用) + ③ owner-only 视图 (锁链第 9 处; admin god-mode 不挂 ADM-0 §1.3). 跨 milestone byte-identical 锁链: BPP-4 #499 watchdog (30s threshold) + BPP-7 SDK (HeartbeatInterval) + BPP-8 #532 (RecordHeartbeatTimeout) + AL-1 #492 + REFACTOR-REASONS #496 6-dict (锁链第 14 处) + ADM-2.1/AP-2 audit forward-only + ADM-0 §1.3 红线 + AST 锁链延伸第 6 处 (BPP-4+5+6+7+8 + HB-3 v2 forbidden tokens 全 0 hit). 不在范围: decay UI 时间轴 (v3) / metrics (v3) / owner override 阈值 (v3) / cross-org agg (v3 跟 AP-3) / admin god-mode / 6-dict 扩第 7 / 另开 heartbeat_decay 表 / RecordHeartbeatStale 旁路. 16 unit PASS (bpp 8 + api 7 + AdminGodMode 1). |
| 2026-04-30 | 战马E | AP-5 整 milestone 一 PR (Phase 4+ messages endpoints ACL audit, post-removal fail-closed gap 闭合, 跟 AP-4 #551 reactions ACL 同模式扩展; **0 schema 改 + 0 新错码 + 0 新 endpoint**): spec brief `ap-5-spec.md` 47 行 (≤80) + stance + acceptance (4件套 content-lock 不需 server-only). AP-5.1 server (`internal/api/messages.go::handleUpdateMessage` PUT + `handleDeleteMessage` DELETE + `internal/api/dm_4_message_edit.go::handleEdit` PATCH 各加 6 行 channel-member ACL gate `Store.IsChannelMember(existing.ChannelID, user.ID) && Store.CanAccessChannel(existing.ChannelID, user.ID)` 跟 AP-4 reactions 同 helper 复用 + cross-org check 仍先于 channel-member 404 守 TestCrossOrgRead403 lock byte-identical) + AP-5.2 unit (`internal/api/messages_acl_audit_test.go` 5 unit PASS — TestAP5_PutMessage_PostRemovalReject + TestAP5_DeleteMessage_PostRemovalReject + TestAP5_PatchDM_PostRemovalReject + TestAP5_NonSenderMember_403 反向 sanity + TestAP5_Member_PutDelete_OK 反向 sanity) + e2e (`packages/e2e/tests/ap-5-messages-acl-matrix.spec.ts` 4 case — PUT/DELETE/PATCH post-removal + cross-org sanity) + AP-5.3 closure (REG-AP5-001..005 5 🟢 + REG-DM5-005 / REG-AP4-005 cross-link gap 闭合三轨成对). 立场 4 项: ① 3 handler IsChannelMember + CanAccessChannel gate 复用 AP-4 同 helper + ② cross-org 403 仍先于 channel-member 404 (REG-INV-002 fail-closed) + ③ owner-only ACL 既有不破 (sender_id 403 跟 AL-2a/BPP-3.2/AL-1/AL-5 owner-only 5 处同模式) + ④ 0 schema 改 + 0 新错码 (复用 messages.go::handleCreateMessage 既有 "Channel not found" byte-identical 字符 fail-closed) + 反约束: admin god-mode 不挂 ADM-0 §1.3 红线 + audit 不另起 (复用既有 events 表 + soft-delete 路径). 跨 milestone byte-identical 锁链: AP-4 #551 reactions ACL gap 闭合 (双轨 ACL 收紧成对) + DM-5 #549 §3.3 反向断 REG-DM5-005 gap 锚 + messages.go 既有 fail-closed 字符 + AL-2a/BPP-3.2/AL-1/AL-5 owner-only 5 处 + ADM-0 §1.3 红线 + REG-INV-002 fail-closed. 不在范围: 抗 enumeration message_id (留 v2) / admin god-mode reactions visibility (永久不挂 ADM-0 §1.3) / rate-limit (留 spam Phase) / 跨 org 反向断 (复用 store.CrossOrg 既有). 副作用 — `error_branches_test.go::TestChannelsMessagesWorkspaceAdditionalBranches` update/delete-message-forbidden 翻 403 → 404 因 post-leave-public 状态 channel-member gate 先 fire (gap fix 真生效, 不是 regression — 注释解释). server-go ./... 全 25 packages 全绿 + AP-5_* 5 unit PASS. |
| 2026-04-30 | 战马D | HB-5 整 milestone 一 PR (Phase 4+ — heartbeat history retention 收尾, agent_state_log 长期累积无界增长复用 AL-7 #533 archived_at 模式; **0 新表 / 0 新 admin_actions enum** 复用 AL-7 既有 'audit_retention_override' action + metadata.target='heartbeat' 字面区分; **命名说明**: HB-4 #524 已被 release-gate 占, 顺位 HB-5): spec brief `hb-5-spec.md` 79 行 (≤80) + stance `hb-5-stance-checklist.md` 66 行 (≤80) + acceptance `hb-5.md` 41 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3). HB-5.1 schema migration v=34 (`internal/migrations/hb_5_1_agent_state_log_archived_at.go` ALTER agent_state_log ADD COLUMN archived_at INTEGER NULL 跨五 milestone 同模式 + sparse idx 跟 AL-7.1 byte-identical; 不挂 admin_actions CHECK enum 改 — AL-7.1 12 项 byte-identical 不动) + HB-5.2 server (`internal/auth/heartbeat_retention_sweeper.go` HeartbeatRetentionSweeper 跟 AL-7 RetentionSweeper #533 + AP-2 ExpiresSweeper #525 同模式 nil-safe; UPDATE agent_state_log SET archived_at = now WHERE ts < cutoff AND archived_at IS NULL 不真删; HeartbeatRetentionDays=30 / HeartbeatTargetLabel='heartbeat' / HeartbeatSweeperReason=reasons.Unknown const 单源; `internal/api/hb_5_heartbeat_retention_override.go` POST /admin-api/v1/heartbeat-retention/override admin-rail; body retention_days int 1..365 clamp 复用 auth.RetentionMinDays/MaxDays 跟 AL-7 同源; 写 admin_actions row action='audit_retention_override' 复用 AL-7 既有 ActionAuditRetentionOverride const + metadata.target='heartbeat' 字面区分; server.go 加 hb5HeartbeatRetentionHandler.RegisterAdminRoutes + HeartbeatRetentionSweeper.Start) + HB-5.3 closure (REG-HB5-001..006 6 🟢; 总计 382→388 / active 356→362 / pending 26 不变). 立场 3 项: ① retention 走 agent_state_log.archived_at + sweeper 不裂表/不真删 + ② admin override 复用 AL-7 既有 action 不挂 admin_actions CHECK 第 13 项 enum (反向 grep `'heartbeat_retention_override'` 0 hit) + ③ admin-rail only ADM-0 §1.3 红线 + reason 复用 reasons.Unknown 跟 AL-7 SweeperReason 同源 (AL-1a 锁链第 17 处). 跨 milestone byte-identical 锁链: AL-7 #533 archived_at + sparse idx 反向同源 + ALTER ADD COLUMN nullable 跨五 milestone (AP-1.1+AP-3.1+AP-2.1+AL-7.1+HB-5.1) + AL-1 #492 agent_state_log forward-only + AL-7 RetentionSweeper #533 (time.Ticker 同模式 nil-safe) + ADM-2.1 #484 admin_actions audit (5 字段链第 9 处) + AL-1a reason 锁链 HB-5 = 第 17 处 (AL-7 #15 + AL-8 #16 承袭) + ADM-0 §1.3 红线 + AST 锁链延伸第 9 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7 + AL-8 + HB-5 forbidden tokens 全 0 hit) + admin_actions CHECK 12 项 byte-identical 跟 AL-7.1 不动 + auth.RetentionMinDays/MaxDays / ActionAuditRetentionOverride const 复用 AL-7 同源. 不在范围: admin SPA dashboard heartbeat retention list 视图 (留 v3) / agent_state_log archived 行搬移到 cold storage (留 v3) / 跨 org heartbeat retention 策略 (留 v3 跟 AP-3 同期) / user-rail heartbeat retention 调控 (永久不挂 ADM-0 §1.3) / agent_state_log row write/update/delete (forward-only AL-1 锁) / heartbeat history export CSV/NDJSON (留 v3). 17 unit PASS — migrations 5 (HB51_AddsArchivedAtColumn / _HasSparseIdx / _VersionIs34 / _Idempotent / _NoAdminActionsEnumDrift) + auth 9 (HB52_RunOnceArchivesExpired / _RunOnceSoftArchiveNotRealDelete / _RunOnceIdempotent / _StartCtxShutdown / _NilSafeCtor / _SweeperReason_ByteIdentical / _NoCronFrameworkImport + HB53_NoHeartbeatRetentionQueue) + api 3 (HB52_OverrideEndpointWritesAudit / _OverrideRejectsUserRail / _OverrideClampsRetention 含 6 sub-case). server-go ./... 25 packages 全绿. |
| 2026-04-30 | 战马D | CHN-7 整 milestone 一 PR (Phase 4+ channel mute/notification 静默 — per-user mute; **0 schema 改 / 0 新 admin_actions enum** — user_channel_layout.collapsed INTEGER 列 bitmap 编码: bit 0 (=1) = 折叠 (CHN-3 既有), bit 1 (=2) = 静音 (CHN-7 新增) + MuteBit=2 const 双向锁 server+client byte-identical 跟 CHN-6 #544 PinThreshold 同模式): spec brief `chn-7-spec.md` 80 行 (≤80) + stance `chn-7-stance-checklist.md` 65 行 (≤80) + acceptance `chn-7.md` 48 行 (≤50) + content-lock `chn-7-content-lock.md` 78 行 (UI 必锁 — mute button + `已静音` indicator + 同义词反向 + MuteBit 双向锁). CHN-7.1 server (`internal/store/queries.go::SetMuteBit(userID, channelID, bitMask, set)` UPSERT collapsed bitmap toggle 不动 position; `IsMutedForUser(userID, channelID, muteBit)` 谓词单源走 collapsed & MuteBit; `internal/api/chn_7_mute.go` POST + DELETE /api/v1/channels/{channelId}/mute + MuteBit=2 const + IsMuted 谓词单源 + DM 反 layout.dm_not_grouped byte-identical + non-member 403 + 401; server.go 加 RegisterCHN7Routes user-rail authMw) + CHN-7.2 client (`packages/client/src/lib/mute.ts` MUTE_BIT=2 字面 byte-identical 跟 server + isMuted 谓词单源; `lib/api.ts::muteChannel(id, muted)` 单源 method=POST/DELETE; `components/MuteButton.tsx` toggle 文案 byte-identical `静音`/`取消静音` + data-action='mute'/'unmute' + 同义词反向 reject; `components/MutedChannelIndicator.tsx` `<span>` + `data-testid='muted-channel-indicator'` + `🔕 已静音` 文案 byte-identical + muted=false return null) + CHN-7.3 closure (REG-CHN7-001..006 6 🟢; 总计 §5 fix → 405/379/26 sum=actual rows, CHN-7 +6 净). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 collapsed INTEGER bitmap + MuteBit=2 双向锁) + ② owner-only ACL 锁链第 15 处 + admin god-mode 不挂 PATCH/POST/DELETE ADM-0 §1.3 红线 + ③ mute **不 drop messages** best-effort — CreateMessage/RT-3 fan-out/WS frame 全 byte-identical 不动, mute 仅 DL-4 push notifier skip (反向 grep `mute.*skip.*broadcast\|mute.*drop.*message` 在 internal/ws+messages*.go 0 hit). 跨 milestone byte-identical 锁链: CHN-3.1 #410 user_channel_layout 列复用 (0 schema 改 + bitmap bit 0/1 拆死) + CHN-3.2 #412 既有 PUT /me/layout endpoint byte-identical 不动 + CHN-6 #544 PinThreshold 双向锁模式承袭 (MuteBit=2 同源) + DL-4 push gateway 立场承袭 (best-effort skip) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 15 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5/CHN-6/CHN-7) + audit 5 字段链第 12 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8+HB-5+CHN-5+CHN-6+CHN-7) + AST 锁链延伸第 12 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8+HB-5+CHN-5+CHN-6+CHN-7 forbidden tokens 全 0 hit) + MuteBit=2 双向锁 (server const + client const byte-identical). 不在范围: 自动 mute 时长 muted_until 过期解锁 (留 v3 — 永久 mute v1) / 跨设备 mute 状态推送 RT-3 frame (留 v3) / admin god-mode mute override (永久不挂 ADM-0 §1.3) / DM mute (永久不挂 — DM 走 CHN-4 dm-only) / 全局静默 DnD 模式 (留 user-settings v3, 跟 mute 不同维度). 18 unit PASS — server-go 9 (TestCHN71_NoSchemaChange + TestCHN71_MuteChannel_HappyPath/NonMemberRejected/Unauthorized + TestCHN71_UnmuteChannel_HappyPath/Idempotent/PreservesCollapsedBit + TestCHN71_MuteBit_ByteIdentical + TestCHN71_NoAdminMutePath + TestCHN71_MuteDoesNotDropMessages + TestCHN73_NoChannelMuteQueue) + client vitest 9 (MuteButton.test.tsx 5: 文案 mute/unmute / click→API+onChange / 同义词反向 user-visible + MutedChannelIndicator.test.tsx 4: 已静音 文案 / muted=false null / MuteBit 双向锁 / 同义词反向). server-go ./... 25 packages 全绿 + client ./... 46 files / 389 tests 全绿 (CHN-3.2/CHN-5/CHN-6 既有 unit 不破). |
| 2026-04-30 | 战马D | DM-7 整 milestone 一 PR (Phase 4+ — DM message edit history audit, DM-4 #553 既有 PATCH 续作 — 给 edit 一份历史回放; **复用 messages 表 + ALTER ADD nullable 跨七 milestone**): spec brief `dm-7-spec.md` 80 行 (≤80) + stance `dm-7-stance-checklist.md` 64 行 (≤80) + acceptance `dm-7.md` 45 行 (≤50) + content-lock `dm-7-content-lock.md` 85 行 (EditHistoryModal `编辑历史` 4 字 + `共 N 次编辑` + RFC3339 + JSON shape `{old_content, ts, reason}` byte-identical). DM-7.1 schema migration v=34 (`internal/migrations/dm_7_1_messages_edit_history.go` ALTER messages ADD COLUMN edit_history TEXT NULL — 跟 AP-1.1+AP-3.1+AP-2.1+AL-7.1+HB-5.1+CHN-5.1 跨七 milestone ALTER ADD nullable 同模式; NULL = 无历史 / 老消息 byte-identical / 现网行为零变 + idempotent guard hasColumn pre-check) + DM-7.2 server (`internal/store/queries.go::UpdateMessage` SSOT wrap — 改 content 前 SELECT old content + edit_history + JSON marshal append `[{old_content, ts, reason='unknown'}]` + idempotent same-content skip; reason 复用 AL-1a `reasons.Unknown='unknown'` byte-identical 跟 AL-7 SweeperReason / HB-5 HeartbeatSweeperReason 同源 — AL-1a 锁链第 18 处; `internal/api/dm_7_edit_history.go` GET /api/v1/channels/{channelId}/messages/{messageId}/edit-history user-rail sender-only ACL + GET /admin-api/v1/messages/{messageId}/edit-history admin readonly admin-rail — owner-only 锁链第 19 处; admin god-mode 不挂 PATCH/DELETE/PUT ADM-0 §1.3 红线; DM-4 既有 dm_4_message_edit.go production byte-identical 不变, history append 走 store layer wrap UpdateMessage 单源调用方 unchanged) + DM-7.3 client (`packages/client/src/lib/api.ts::getEditHistory` thin wrapper + `packages/client/src/components/EditHistoryModal.tsx` — `编辑历史` 4 字 title + `共 N 次编辑` + RFC3339 时间戳 byte-identical + 同义词反向 reject `changes/revisions/revs/版本/修订/变更/回退` + 空 history null + 加载失败 `加载编辑历史失败`; 5 vitest PASS title+count + RFC3339 + 空 null + 加载失败 + 同义词反向 grep) + DM-7.4 closure (REG-DM7-001..006 6 🟢; 总计 394→400 / active 368→374 / pending 26 不变). 立场 3 项: ① schema ALTER ADD nullable 跨七 milestone 同模式 (NULL = 无历史 / 老消息 byte-identical) + ② UpdateMessage SSOT history append (反向 grep `inline.*UPDATE.*messages.*content` 0 hit production, DM-4 既有 PATCH path 不变) + ③ owner-only sender + admin readonly (admin god-mode 不挂 PATCH/DELETE/PUT 反向 grep 0 hit ADM-0 §1.3 红线). 跨 milestone byte-identical 锁链: AP-1.1+AP-3.1+AP-2.1+AL-7.1+HB-5.1+CHN-5.1+DM-7.1 ALTER ADD nullable 跨七 milestone + AL-1a reason 锁链第 18 处 (HB-5 第 17 + AL-8 第 16 + AL-7 第 15 承袭) + AL-7/HB-5 reasons.Unknown byte-identical + owner-only ACL 锁链第 19 处 + AST 锁链延伸第 16 处 forbidden 3 token (`pendingEditHistory/editHistoryQueue/deadLetterEditHistory`) 0 hit + DM-4 #553 既有 PATCH path byte-identical + ADM-0 §1.3 红线. 不在范围: edit history 全文搜 (留 v3 跟 CV-6 FTS 同期) / edit history retention sweeper (留 v3 跟 AL-7 retention 同精神) / admin god-mode edit override (永久不挂 ADM-0 §1.3) / diff syntax highlight (留 v3 client only) / delete history (永久不挂 forward-only 跟 AL-1 同精神). 13 server unit PASS (migrations 3 + api 10) + 全 server suite 25 packages 全绿 + 5 client EditHistoryModal vitest PASS + 全 client 47 file/395 case 全绿. |
| 2026-04-30 | 战马D | CHN-9 整 milestone 一 PR (Phase 4+ channel privacy 三态 — `🔒 仅创建者` / `👥 成员可见` / `🌐 组织内可见`; **0 schema 改 / 0 新 admin_actions enum** — channels.visibility TEXT 列复用 CHN-1.1 #267 既有, 加第 3 enum `creator_only` 跟既有 `private`/`public` 共三态. Visibility 三向锁 server const + client const + DB 字面 byte-identical 跟 CHN-7 #550 MuteBit 双向锁 / CHN-8 #552 NotifPref 三向锁同模式扩展): spec brief `chn-9-spec.md` 79 行 (≤80) + stance `chn-9-stance-checklist.md` 64 行 (≤80) + acceptance `chn-9.md` 48 行 (≤50) + content-lock `chn-9-content-lock.md` 80 行 (UI 必锁 — VisibilityBadge 三态 DOM + 文案 + 同义词反向 + Visibility 三向锁). CHN-9.1 server (`internal/api/chn_9_visibility.go` const VisibilityCreatorOnly/Members/OrgPublic + IsValidVisibility 谓词 + VisibilityRejectMessage 单源; `internal/api/channels.go::handleCreateChannel + handleUpdateChannel` validation 扩 IsValidVisibility 谓词调用 byte-identical 反向 reject 外值 → 400 报错文案 byte-identical) + CHN-9.2 client (`packages/client/src/lib/visibility.ts` VISIBILITY_* 字面 byte-identical 跟 server + isValidVisibility 谓词 + VISIBILITY_LABELS 三态 emoji + text 单源; `lib/api.ts::setChannelVisibility 走 updateChannel 单源 + visibility type 扩 3-tuple; `components/VisibilityBadge.tsx` `<span>` + `data-visibility` 三态 + 文案 byte-identical) + CHN-9.3 closure (REG-CHN9-001..006 6 🟢; §5 dup 总计 fix → 单一 405/379/26). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 visibility 列 + 加 creator_only enum; CHECK 在 app 层) + ② Visibility 三向锁 (server const + client const + DB byte-identical, 改一处=改三处) + ③ owner-only ACL 锁链第 17 处 + admin god-mode 不挂 PATCH visibility + creator_only **不 leak** (反向断言非 creator user GET /api/v1/channels 列表 0 行 leak; ListChannelsWithUnread `visibility = 'public'` filter byte-identical 不动). 跨 milestone byte-identical 锁链: CHN-1.1 #267 channels.visibility 列复用 (0 schema 改) + CHN-1.2 既有 PATCH + channel.manage_visibility ACL byte-identical 不动 + ListChannelsWithUnread SQL filter byte-identical (creator_only 不入 org-public preview) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 17 处一致 + audit 5 字段链第 14 处 + AST 锁链延伸第 14 处 + Visibility 三向锁 (server + client + DB byte-identical) + CHN-7 MuteBit 双向锁 + CHN-8 NotifPref 三向锁模式承袭. 不在范围: per-message visibility (永久不挂) / visibility 切换审计 (audit 5 字段链复用既有 PATCH path) / admin god-mode visibility override (永久不挂 ADM-0 §1.3) / DM visibility (永久不挂 DM 走 CHN-4 dm-only) / visibility-based 跨 org 邀请 (留 v3 跟 AP-3 同期). 13 unit PASS — server-go 8 (TestCHN91_NoSchemaChange + TestCHN91_VisibilityConsts_ByteIdentical + TestCHN91_PatchVisibility_CreatorOnly_HappyPath + TestCHN91_PatchVisibility_BackcompatPublicPrivate + TestCHN91_PatchVisibility_RejectsInvalidValue + TestCHN91_CreatorOnlyChannel_NotLeakedToOrgPeers + TestCHN91_ListChannelsFilter_ByteIdentical + TestCHN91_NoAdminVisibilityPath + TestCHN93_NoVisibilityQueue) + client vitest 5 (VisibilityBadge.test.tsx: creator_only/private/public 三态文案 byte-identical + 三向锁 + 同义词反向). server-go ./... 25 packages 全绿 + client ./... 46 files / 390 tests 全绿 (CHN-1.2/CHN-3.2/CHN-5/CHN-6/CHN-7/CHN-8 既有 unit 不破). |
| 2026-04-30 | 战马D | HB-6 整 milestone 一 PR (Phase 4+ — heartbeat lag percentile 监控, 复用 BPP-4 watchdog + HB-3 v2 decay 同源; **0 schema / 0 sweeper goroutine / 0 client UI v1**): spec brief `hb-6-spec.md` 80 行 (≤80) + stance `hb-6-stance-checklist.md` 65 行 (≤80) + acceptance `hb-6.md` 39 行 (≤50) + content-lock 不需 (admin-rail API 无 client UI v1, admin dashboard 留 v3 跟 AL-7/AL-8 同精神). HB-6.1 server lag aggregator (`internal/api/hb_6_lag.go::AggregateLag` + `SampleLagFromStore` — 30s 滚窗 SELECT lag_ms FROM agent_runtimes WHERE status='running' AND last_heartbeat_at IS NOT NULL AND last_heartbeat_at >= cutoff; 内存 sort + linear interpolation P50/P95/P99 + count + at_risk + reason_if_at_risk; WindowSeconds=30 const byte-identical 跟 BPP-4 BPP_HEARTBEAT_TIMEOUT_SECONDS 同源 + LagThresholdMs=15000 const = watchdog 周期一半) + HB-6.2 admin readonly endpoint (GET /admin-api/v1/heartbeat-lag 跟 ADM-2.2 + AL-7.2 + AL-8 admin-rail 同模式; admin readonly 不挂 PATCH/POST/DELETE ADM-0 §1.3 红线; server.go 加 hb6LagHandler.RegisterAdminRoutes) + HB-6.3 closure (REG-HB6-001..006 6 🟢; 总计 405→411 / active 379→385 / pending 26 不变). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 agent_runtimes.last_heartbeat_at AL-4.1 #398 v=16 既有列, 反向 grep migrations/hb_6_ 0 hit) + ② lag derived metric 不写表 / 不另起 admin_actions enum (反向 grep `lag_audit\|hb_lag_log\|heartbeat_lag_table` 0 hit; AST 锁链延伸第 16 处 forbidden 3 token `pendingLagSample / lagSampleQueue / deadLetterLag` 0 hit) + ③ admin-rail only (反向 grep PATCH/POST/PUT/DELETE 在 admin-api/v1/heartbeat-lag 0 hit + user-rail /api/v1/heartbeat-lag 0 hit; AL-1a reason 锁链第 19 处 — at-risk reason 字面=reasons.NetworkUnreachable byte-identical 跟 BPP-4 watchdog 同源) + ④ watchdog 节奏不动 (HB-6 是同步 GET handler 即时聚合 NOT scheduled job, 反向 grep `HeartbeatLagSweeper\|hb6.*Ticker\|lag_ticker` 0 hit) + ⑤ WindowSeconds=30 双向锁 (server const + BPP-4 BPP_HEARTBEAT_TIMEOUT_SECONDS 反向锁 — 改一处=改两处) + ⑥ 不挂 client UI v1 (反向 grep `useHeartbeatLag\|HeartbeatLagPanel` 在 client/src/ 0 hit). 跨 milestone byte-identical 锁链: AL-4.1 #398 agent_runtimes.last_heartbeat_at 列复用 (0 schema 改) + BPP-4 #499 watchdog BPP_HEARTBEAT_TIMEOUT_SECONDS=30 双向锁 (改一处 = 改两处) + HB-3 v2 #14 decay 三档承袭立场 (HB-6 read-side 不改 watchdog 节奏 / 不改 decay 阈值) + AL-1a reason 锁链 HB-6 = 第 19 处 (DM-7 #18 + HB-5 #17 + AL-8 #16 + AL-7 #15 承袭, reasons.NetworkUnreachable byte-identical 跟 BPP-4 watchdog timeout 同源) + ADM-0 §1.3 admin god-mode 不挂 PATCH/POST/DELETE + admin-rail only 跟 ADM-2.2 / AL-7.2 / AL-8 同模式 + AST 锁链延伸第 16 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 forbidden tokens 全 0 hit). 不在范围: lag 历史持久化表 (留 v3 跟 AL-7 retention 同精神) / admin SPA dashboard UI (留 v3 跟 AL-7/AL-8 同精神) / Prometheus metrics exporter (留 v3) / 主动告警 (Slack/Email留 v4) / 跨 org lag 隔离 (留 AP-3 同期) / per-agent lag 历史时序 (留 v3, v0 仅 30s 滚窗 percentile) / runtime hot-mutate WindowSeconds (留 v3, const 字面单源). 11 server unit PASS — TestHB61_NoSchemaChange + WindowSecondsByteIdentical + AggregateLag_PercentileCorrect + AggregateLag_EmptyNoRisk + WindowCutoffExcludesStale + TestHB62_AdminHappyPath + NonAdmin401 + NoUserRailPath + AtRiskReasonByteIdentical + TestHB63_NoAdminWritePath + NoLagSampleQueue + NoClientUIv1; server-go ./... 25 packages 全绿 (BPP-4 watchdog + AL-4 agent_runtimes + ADM-2.2 既有 unit 全套不破). |
| 2026-04-30 | 战马D | CHN-10 整 milestone 一 PR (Phase 4+ — channel description owner-only PUT + ChannelHeader 描述行 + DescriptionEditor modal; **0 schema 改** — 复用 channels.topic 既有列 cm-onboarding-welcome ALTER TEXT NOT NULL DEFAULT '' + GORM size:500): spec brief `chn-10-spec.md` 65 行 (≤80) + stance `chn-10-stance-checklist.md` 60 行 (≤80) + acceptance `chn-10.md` 38 行 (≤50) + content-lock `chn-10-content-lock.md` 70 行 (DescriptionEditor + ChannelHeader 文案 byte-identical + 同义词反向 reject). CHN-10.1 schema 0 行 (复用 channels.topic 既有列) + CHN-10.2 server (`internal/api/chn_10_description.go::CHN10DescriptionHandler` PUT /api/v1/channels/{channelId}/description owner-only — channel.CreatedBy == user.ID 反向断 member-level reject 403; DescriptionMaxLength=500 const byte-identical 跟 channels.topic GORM size:500 + client DESCRIPTION_MAX_LENGTH 同源 — 双向锁守门; 写入相同 channels.topic 列 store.UpdateChannel 单源; server.go 加 chn10DescHandler.RegisterUserRoutes; admin god-mode 不挂 RegisterAdminRoutes 不存在 ADM-0 §1.3 红线; 既有 PUT /topic CHN-2 #406 member-level + 250 字符 path byte-identical 不变) + CHN-10.3 client (`packages/client/src/lib/api.ts::setChannelDescription` thin wrapper + DESCRIPTION_MAX_LENGTH=500 const byte-identical; `components/DescriptionEditor.tsx` modal title `频道说明` 4 字 + save `保存` 2 字 + cancel `取消` 2 字 + counter `{n}/500` + textarea maxLength=500 + error `频道说明不能超过 500 字符` byte-identical; `components/ChannelHeader.tsx` 描述行 + edit trigger `编辑` 2 字 + 空 description return null) + CHN-10.4 closure (REG-CHN10-001..006 6 🟢; 总计 405→411 / active 379→385 / pending 26 不变). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 channels.topic 既有列, 反向 grep migrations/chn_10_ + ALTER channels.*description 0 hit) + ② owner-only ACL 锁链第 20 处 (DM-7 #19 + CHN-9 #14 承袭, channel.CreatedBy == user.ID 反向断 member-level reject 403; admin god-mode 不挂 PATCH/PUT/POST/DELETE 在 admin-api/v1/.../description ADM-0 §1.3 红线) + ③ 文案 byte-identical 锁 (`频道说明` + `保存`/`取消` + `{n}/500` counter + `编辑` trigger + 同义词反向 reject `topic/about/intro/简介/主题/关于/介绍`) + ④ 既有 /topic byte-identical 不变 (CHN-2 #406 既有 PUT /topic member-level + 250 字符 path 不动, 反向 grep `chn_10/DescriptionMaxLength//description` 在 handleSetTopic block 0 hit) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19 不延伸, 反向 grep `chn10.*reason\|description_reason` 0 hit) + ⑥ AST 锁链延伸第 17 处 forbidden 3 token 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-2 #406 既有 PUT /topic member-level + 250 字符 path byte-identical 不动 + cm-onboarding-welcome channels.topic ALTER TEXT NOT NULL DEFAULT '' + GORM size:500 byte-identical 双向锁 (server const + client const + GORM 三向锁) + DM-7 #19 owner-only ACL 锁链承袭 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 17 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7/AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 + CHN-10 forbidden tokens 全 0 hit). 不在范围: description Markdown 渲染 (留 v3 — 文本 only v0) / description 历史回放 (留 v3 — DM-7 edit history 不延伸) / description push 通知 (留 v3 跟 RT-3 fan-out 不联动) / per-language i18n description (留 v4) / description 全文搜 (留 v3 FTS 同期) / admin god-mode override (永久不挂 ADM-0 §1.3) / description retention sweeper (留 v3 forward-only). 13 unit PASS — server-go ./internal/api/ TestCHN101_NoSchemaChange + TestCHN102_PutDescription_OwnerHappyPath/NonOwnerRejected/Unauthorized401/LengthCap500 + TestCHN102_TopicPathByteIdentical + TestCHN103_NoAdminDescriptionPath/NoDescriptionQueue 8 unit + client packages/client vitest DescriptionEditor.test.tsx 5 PASS = 总 13 unit PASS. server-go ./... 25 packages 全绿 (CHN-2 既有 + cm-onboarding-welcome + 全套既有 unit 不破) + client ./... 51 files / 415 tests 全绿. |
| 2026-04-30 | 战马D | CHN-5 整 milestone 一 PR (Phase 4+ channel archive UI 闭环 — CHN-1.2/.3 #265/#288 既有 archive PATCH + 列表过滤 + badge UI 已落; 本 milestone 收尾: GET 列表 endpoint + unarchive system DM fanout 互补二式 + 客户端 ArchivedChannelsPanel 折叠区 + admin-rail readonly GET; **0 schema 改 / 0 新 admin_actions enum** — 复用 CHN-1.1 #267 既有 channels.archived_at 列): spec brief `chn-5-spec.md` 77 行 (≤80) + stance `chn-5-stance-checklist.md` 67 行 (≤80) + acceptance `chn-5.md` 44 行 (≤50) + content-lock `chn-5-content-lock.md` 60 行 (UI 必锁 — 互补二式 archive/unarchive system DM + ArchivedChannelsPanel DOM + 同义词反向). CHN-5.1 schema 0 行 (复用 CHN-1.1 既有 channels.archived_at) + CHN-5.2 server (`internal/store/queries.go::ListArchivedChannelsForUser` (cm.user_id 过滤 + cross-org 承袭 CM-3) + `ListAllArchivedChannelsForAdmin` admin 全 org readonly; `internal/api/chn_5_archived.go` handleListMyArchivedChannels owner-only + handleAdminListArchivedChannels admin readonly + fanoutUnarchiveSystemMessage 跟 fanoutArchiveSystemMessage 同模式 + 文案 byte-identical 跟 content-lock §1; `channels.go` handleUpdateChannel 加 unarchive 分支调 fanoutUnarchiveSystemMessage; server.go 加 RegisterCHN5Routes user-rail + RegisterCHN5AdminRoutes admin-rail) + CHN-5.3 client (`packages/client/src/components/ArchivedChannelsPanel.tsx` `<details>` 折叠区 + `<summary>已归档频道</summary>` + 行 data-archived="true" + badge `已归档` 跟 CHN-1.3 #288 同源 + button data-action="restore" `恢复` 1 字 + empty `暂无已归档频道`; `lib/api.ts::listArchivedChannels` 单源新增) + CHN-5.4 closure (REG-CHN5-001..006 6 🟢; 总计 381→387 / active 355→361 / pending 26 不变). 立场 3 项: ① 0 schema 改 (复用 CHN-1.1 既有列) + ② owner-only ACL 锁链 13 处一致 + admin god-mode 不挂 PATCH ADM-0 §1.3 红线 + ③ unarchive system DM 互补二式 byte-identical 跟 content-lock 同源. 跨 milestone byte-identical 锁链: CHN-1.1 #267 channels.archived_at 列复用 + CHN-1.2 #265 fanoutArchiveSystemMessage 同模式 + CHN-1.3 #288 SortableChannelItem badge byte-identical + ListChannelsWithUnread archived_at IS NULL filter byte-identical 不动 + ADM-0 §1.3 admin god-mode 不挂 PATCH + CM-3 #208 cross-org 过滤承袭 + owner-only ACL 锁链 13 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5) + audit 5 字段链第 10 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8+HB-5+CHN-5) + AST 锁链延伸第 10 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7 + AL-8 + HB-5 + CHN-5 forbidden tokens 全 0 hit). 不在范围: 自动 archive 策略 (留 v3) / age-based auto-archive cron (留 v3) / archived hard-delete (永久不挂 forward-only) / cross-org 同步 (留 v3 AP-3 同期) / admin god-mode PATCH archive (永久不挂 ADM-0 §1.3) / 历史 cursor 重置 (留 v3). 13 unit PASS — server-go internal/api TestCHN51_NoSchemaChange + TestCHN52_ListMyArchived_HappyPath/EmptyList/Unauthorized + TestCHN52_AdminListArchived_HappyPath/RejectsUserRail + TestCHN52_UnarchiveFanoutsSystemMessage + TestCHN52_NoAdminPatchPath + TestCHN53_NoChannelArchiveQueue 9 + client packages/client vitest ArchivedChannelsPanel.test.tsx 4 PASS. server-go ./... 25 packages 全绿 + client ./... 44 files / 380 tests 全绿 (含 ADM-2.2 + CHN-1.2 + 全套既有 unit 不破). |
| 2026-04-30 | 战马D | CHN-6 整 milestone 一 PR (Phase 4+ channel pin/unpin 顶置 — 用户顶置 favorite channels; **0 schema 改 / 0 新 admin_actions enum** — user_channel_layout 列复用 CHN-3.1 #410 既有, pin 状态走 `position < 0` 字面约定 + PinThreshold=0 const 双向锁 server+client byte-identical): spec brief `chn-6-spec.md` 80 行 (≤80) + stance `chn-6-stance-checklist.md` 66 行 (≤80) + acceptance `chn-6.md` 46 行 (≤50) + content-lock `chn-6-content-lock.md` 69 行 (UI 必锁 — pin button DOM + `已置顶频道` section + 同义词反向 + PinThreshold 双向锁). CHN-6.1 server (`internal/store/queries.go::PinChannelLayout` UPSERT user_channel_layout SET position = -(nowMs) ON CONFLICT DO UPDATE 跟 CHN-3.2 既有 upsert 同模式 + `UnpinChannelLayout` UPSERT position = max(positive)+1.0 跟 CHN-3.3 #415 client MIN-1.0 单调小数模式互补; `internal/api/chn_6_pin.go` POST + DELETE /api/v1/channels/{channelId}/pin endpoints + PinThreshold=0.0 const + IsPinned(position) 谓词单源 + DM 反 layout.dm_not_grouped byte-identical 跟 CHN-3.2 + non-member 403 + Unauthorized 401; server.go 加 RegisterCHN6Routes user-rail authMw 必经) + CHN-6.2 client (`packages/client/src/lib/pin.ts` POSITION_PIN_THRESHOLD=0 字面 byte-identical 跟 server PinThreshold + isPinned 谓词单源; `lib/api.ts::pinChannel(id, pinned)` 单源 method=POST/DELETE; `components/PinButton.tsx` toggle 文案 byte-identical `置顶`/`取消置顶` + data-action='pin'/'unpin' + 同义词反向 reject; `components/PinnedChannelsSection.tsx` `<section>` + `<header>已置顶频道</header>` + `data-testid='pinned-channels-section'` + 行 `data-pinned='true'` + filter `channel.position < POSITION_PIN_THRESHOLD` byte-identical + empty state 不渲染 return null) + CHN-6.3 closure (REG-CHN6-001..006 6 🟢; 总计 393→399 / active 367→373 / pending 26 不变 — 注: 修了 main §5 dup 总计行 `382/356/382/356/381/355` → 单一 405/379 总计 sum=actual rows). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 CHN-3.1 既有列 + position<0 字面约定 + PinThreshold const) + ② owner-only ACL 锁链第 14 处 + admin god-mode 不挂 PATCH/POST/DELETE ADM-0 §1.3 红线 + ③ pin 状态 client/server 双源不漂 (PinThreshold=0 双向锁 改一处=改两处). 跨 milestone byte-identical 锁链: CHN-3.1 #410 user_channel_layout 列复用 + CHN-3.2 #412 既有 layout endpoint byte-identical 不动 + CHN-3.3 #415 client MIN-1.0 单调小数模式承袭 + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 14 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5/CHN-6) + audit 5 字段链第 11 处 (ADM-2.1+AP-2+BPP-4+BPP-7+BPP-8+HB-3 v2+AL-7+AL-8+HB-5+CHN-5+CHN-6) + AST 锁链延伸第 11 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8+HB-5+CHN-5+CHN-6 forbidden tokens 全 0 hit) + PinThreshold=0 双向锁 (server PinThreshold + client POSITION_PIN_THRESHOLD byte-identical). 不在范围: pin 数量上限 / pin 排序拖拽 (留 v3 现网 ASC 自然按时间排) / pin 跨设备同步 RT-3 push (留 v3) / admin god-mode pin override (永久不挂 ADM-0 §1.3) / DM pin (永久不挂 DM 走 CHN-4 dm-only) / pin folder nested grouping (留 v3 跟 CHN-3.3 follow-up). 18 unit PASS — server-go 9 (TestCHN61_NoSchemaChange + TestCHN61_PinChannel_HappyPath/NonMemberRejected/Unauthorized + TestCHN61_UnpinChannel_HappyPath/Idempotent + TestCHN61_PinThreshold_ByteIdentical + TestCHN61_NoAdminPinPath + TestCHN63_NoChannelPinQueue) + client vitest 9 (PinButton.test.tsx 5: 文案 pin/unpin / click→API+onChange / 同义词反向 + PinnedChannelsSection.test.tsx 4: 列表 DOM / empty state null / PinThreshold 双向锁 / 同义词反向). server-go ./... 25 packages 全绿 + client ./... 45 files / 385 tests 全绿 (CHN-3.2 既有 unit 不破). |
| 2026-04-30 | 战马D | CHN-11 整 milestone 一 PR (Phase 4+ — channel member admin UI; **0 schema / 0 server production code** — POST/DELETE/GET /channels/:id/members CHN-1 #276 既有 path byte-identical 不动; 跟 DM-6 #556 / DM-5 #549 / CV-7 #535 / CV-9..12 一系列 0-server 模式承袭): spec brief `chn-11-spec.md` 70 行 (≤80) + stance `chn-11-stance-checklist.md` 50 行 (≤80) + acceptance `chn-11.md` 32 行 (≤50) + content-lock `chn-11-content-lock.md` 80 行 (MemberList + AddMemberModal + KickConfirmModal 三组件 文案 byte-identical + 同义词反向 reject). CHN-11.1 schema 0 行 (复用 channel_members) + CHN-11.2 server **0 行 production** (CHN-1 #276 既有 ABAC manage_members + agent cross-owner reject + agent_join system DM 文案锁全套 byte-identical 不动) + CHN-11.3 client (`lib/api.ts::addChannelMember/removeChannelMember/listChannelMembers` 既有 thin wrapper; `components/MemberList.tsx` title `成员` + add `添加成员` + row remove `移除` byte-identical + canManage 闸 + 空 null + data-member-user-id 行级锚; `components/AddMemberModal.tsx` title `添加成员` + placeholder `用户邮箱或 ID` + submit `添加` + cancel `取消`; `components/KickConfirmModal.tsx` title `确认移除 {user.display_name}?` byte-identical (user 占位) + confirm `确认` + cancel `取消`) + CHN-11.4 closure (REG-CHN11-001..006 6 🟢; CHN-11 +6 行). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 channel_members) + ② 0 server production code (反向 grep `chn_11/chn11/CHN11` 在 internal/api/*.go 非 _test.go 0 hit) + ③ 文案 byte-identical (`成员/添加成员/移除/确认移除 {user}?`) + 同义词反向 reject (`invite/kick/remove/expel/逐出/踢出/邀请` 在 user-visible Chinese text 0 hit) + ④ 既有 handleAddMember+handleRemoveMember block byte-identical (反向 grep chn_11 在 2 handler 2500 字符 block 0 hit) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19, 不引入新 reason; audit 5 字段链复用既有 user_joined/user_left events) + ⑥ AST 锁链延伸第 19 处 forbidden 3 token (`pendingMemberAdmin / memberAdminQueue / deadLetterMemberAdmin`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-1 #276 既有 channel_members 表 + manage_members ABAC + agent cross-owner guard + agent_join system DM 文案锁 全套不动 + DM-6 #556 / DM-5 #549 / CV-7 #535 / CV-9..12 0-server 模式承袭 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 19 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 + CHN-10 + RT-4 + CHN-11 forbidden tokens 全 0 hit). 不在范围: 角色管理 admin/moderator (留 v3) / bulk add/remove (留 v3) / member 历史回放 (留 v3) / pending invitations (留 v3 跟 INV-* 同期) / admin god-mode kick override (永久不挂 ADM-0 §1.3) / cross-org member add (CM-3 既有禁) / audit log per add/remove (留 v3 — events user_joined/user_left 既有 audit path 承袭). 10 unit PASS — server-go ./internal/api/ TestCHN111_NoSchemaChange + NoServerProductionCode + HandlersByteIdentical + TestCHN113_NoMemberAdminQueue + NoAdminMembersPath 5 unit + client packages/client vitest MemberList.test.tsx 5 PASS = 总 10 unit PASS. server-go ./... 25 packages 全绿 (CHN-1 既有 unit 全套不破) + client ./... 53 files / 423 tests 全绿. |
| 2026-04-30 | 战马D | CHN-8 整 milestone 一 PR (Phase 4+ channel notification preferences 三态 — `所有消息` / `仅@提及` / `不打扰`; **0 schema 改** — collapsed bitmap bits 2-3 跟 CHN-3 bit 0 + CHN-7 bit 1 拆死. NotifPref **三向锁** server const + client const + bitmap byte-identical 跟 CHN-7 MuteBit 双向锁 / CHN-6 PinThreshold 双向锁同模式扩展): spec brief `chn-8-spec.md` 74 行 (≤80) + stance `chn-8-stance-checklist.md` 66 行 (≤80) + acceptance `chn-8.md` 47 行 (≤50) + content-lock `chn-8-content-lock.md` 68 行 (UI 必锁 — pref dropdown DOM + 三选一文案 + 同义词反向 + NotifPref 三向锁). CHN-8.1 server (`internal/store/queries.go::SetNotifPrefBits/GetNotifPrefForUser/GetCollapsedForUser` UPSERT 不动 position; `internal/api/chn_8_notif_pref.go` PUT /api/v1/channels/{channelId}/notification-pref + NotifPrefShift=2/Mask=3/All=0/Mention=1/None=2 const + GetNotifPref 谓词单源 + DM 反 layout.dm_not_grouped + non-member 403 + 401 + spec 外值 reject 400 notification_pref.invalid_value; server.go 加 RegisterCHN8Routes user-rail authMw) + CHN-8.2 client (`packages/client/src/lib/notif_pref.ts` NOTIF_PREF_* 字面 byte-identical 跟 server + getNotifPref 谓词单源 + NOTIF_PREF_STRING_TO_INT/INT_TO_STRING 双向 map; `lib/api.ts::setNotificationPref(id, pref)` 单源 method=PUT body={pref}; `components/NotificationPrefDropdown.tsx` `<select>` + 三选一 option `所有消息`/`仅@提及`/`不打扰` 文案 byte-identical + `data-testid='notification-pref-dropdown'`) + CHN-8.3 closure (REG-CHN8-001..006 6 🟢; §5 dup 总计 fix → 单一 405/379/26 sum=actual rows). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 collapsed bitmap + NotifPref 三向锁) + ② owner-only ACL 锁链第 16 处 + admin god-mode 不挂 PUT/POST/DELETE ADM-0 §1.3 红线 + ③ 不 drop messages — CreateMessage/RT-3 fan-out/WS frame 全 byte-identical, 仅 DL-4 push notifier 三态 skip best-effort. 跨 milestone byte-identical 锁链: CHN-3.1 #410 user_channel_layout 列复用 (0 schema 改 + bitmap bit 0/1/2-3 拆死) + CHN-3.2 #412 既有 PUT /me/layout endpoint byte-identical 不动 + CHN-6 #544 PinThreshold + CHN-7 #550 MuteBit 双向锁模式承袭 (NotifPref 三向锁延伸) + DL-4 push gateway 立场承袭 (三态 skip) + ADM-0 §1.3 admin god-mode 不挂 + owner-only ACL 锁链第 16 处一致 (AL-2a/BPP-3.2/AL-1/AL-5/DM-4/CV-4 v2/BPP-7/BPP-8/CHN-5/CHN-6/CHN-7/CHN-8) + audit 5 字段链第 13 处 + AST 锁链延伸第 13 处. 不在范围: 自动 quiet hours / 时段静默 (留 v3 全局 user-settings) / per-keyword 通知 (留 v3) / admin god-mode notif pref override (永久不挂) / DM notif pref (永久不挂 DM 走 CHN-4 dm-only) / 跨设备 pref 推送 RT-3 frame (留 v3). 12 unit PASS — server-go 8 (TestCHN81_NoSchemaChange + TestCHN81_SetPref_All/Mention/None + TestCHN81_SetPref_RejectsInvalidValue + TestCHN81_SetPref_NonMemberRejected + TestCHN81_NotifPrefConsts_ByteIdentical + TestCHN81_NoAdminNotifPrefPath + TestCHN81_BitmapIsolation_PreservesOtherBits + TestCHN81_NotifPrefDoesNotDropMessages + TestCHN83_NoNotifPrefQueue) + client vitest 4 (NotificationPrefDropdown.test.tsx: 三选一 DOM byte-identical / change → API + onChange / 同义词反向 / NotifPref 三向锁含 bitmap 不互扰). server-go ./... 25 packages 全绿 + client ./... 45 files / 384 tests 全绿 (CHN-3.2/CHN-5/CHN-6/CHN-7 既有 unit 不破). |
| 2026-04-30 | 战马D | RT-4 整 milestone 一 PR (Phase 4+ — channel presence indicator (online users + 既有 typing 复用); **0 schema 改 / 0 新 WS frame** — 复用 AL-3.1 #277 presence_sessions 既有表 + IsOnline 谓词单源; 既有 RT-2 typing path byte-identical 不变): spec brief `rt-4-spec.md` 65 行 (≤80) + stance `rt-4-stance-checklist.md` 48 行 (≤80) + acceptance `rt-4.md` 33 行 (≤50) + content-lock `rt-4-content-lock.md` 50 行 (ChannelPresenceList DOM `当前在线 N 人` byte-identical + ≤5 显示 + `+M` overflow 同义词反向). RT-4.1 schema 0 行 (复用 AL-3.1 presence_sessions) + RT-4.2 server (`internal/api/rt_4_presence.go::RT4PresenceHandler` GET /api/v1/channels/{channelId}/presence — channel.members ∩ presence.IsOnline 同步取交集 即时聚合; member-only ACL IsChannelMember 反向断 non-member 403; PresenceSnapshot response shape `{online_user_ids: [...], counted_at: nowMs}`; server.go 加 rt4PresenceHandler.RegisterUserRoutes + presence.NewSessionsTracker 复用 AL-3.2 #310 既有 SessionsTracker; admin god-mode 不挂 RegisterAdminRoutes 不存在 ADM-0 §1.3 红线; 既有 RT-2 typing WS path ws/client.go::handleTyping byte-identical 不变 反向 grep `rt_4` 在 typing block 0 hit) + RT-4.3 client (`packages/client/src/lib/api.ts::getChannelPresence` thin wrapper + `components/ChannelPresenceList.tsx` `当前在线 N 人` byte-identical + ≤5 显示头像 + N>5 显示前 5 + `+M` overflow chip + `data-presence-user-id` 行级锚 + 空 onlineUserIds → return null; 既有 TypingIndicator.tsx RT-2 path byte-identical 不变) + RT-4.4 closure (REG-RT4-001..006 6 🟢; 总计 405→411 / active 379→385 / pending 26 不变). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 AL-3.1 presence_sessions, 反向 grep migrations/rt_4_*+ALTER presence_sessions 0 hit) + ② 0 新 WS frame (presence-change push 留 v3, 反向 grep `presence_changed/presenceChanged/user_online_pushed` 在 ws+api 0 hit) + ③ member-only ACL + admin god-mode 不挂 PATCH/POST/PUT/DELETE/GET 在 admin-api/v1/.../presence (ADM-0 §1.3 红线) + ④ 既有 RT-2 typing path byte-identical 不变 (反向 grep rt_4/RT4/rt4 在 ws/client.go::handleTyping block 0 hit) + ⑤ PresenceTracker.IsOnline 单源 (复用 AL-3.2 SessionsTracker, 不裂出独立 tracker) + ⑥ AST 锁链延伸第 18 处 forbidden 3 token (`pendingPresenceQuery / presenceQueueRetry / deadLetterPresence`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: AL-3.1 #277 presence_sessions 既有表 + idx_presence_sessions_user_id 复用 (0 schema 改) + AL-3.2 #310 SessionsTracker IsOnline 谓词单源 + RT-2 既有 typing WS path (ws/client.go::handleTyping + multi_device_test.go) byte-identical 不变 + 既有 client TypingIndicator.tsx (5s timeout 自动消失) byte-identical 不动 + ADM-0 §1.3 admin god-mode 不挂 + AL-1a reason 锁链不漂 (停在 HB-6 #19 — RT-4 read-only 不引入新 reason) + AST 锁链延伸第 18 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..9 + DM-6 + DM-7 + HB-6 + CHN-10 + RT-4 forbidden tokens 全 0 hit). 不在范围: presence-change WS push frame (留 v3 — RT-3.2 fan-out 同期) / per-device presence (留 v3 — multi-device 算一个 user 已 online) / typing indicator 改 (既有 RT-2 path byte-identical 不动) / presence 历史回放 (留 v3 — DM-7 edit history 不延伸) / admin god-mode override (永久不挂 ADM-0 §1.3) / cross-org presence isolation (留 AP-3 同期) / last-seen-at 列展示 (留 v3 — 当前仅 online 二态). 13 unit PASS — server-go ./internal/api/ TestRT41_NoSchemaChange + TestRT41_TypingPathByteIdentical + TestRT42_GetPresence_MemberHappyPath/NonMemberRejected/Unauthorized401 + TestRT43_NoAdminPresencePath + TestRT43_NoNewPresenceFrame + TestRT43_NoPresenceQueue 8 unit PASS + client packages/client vitest ChannelPresenceList.test.tsx 5 PASS = 总 13 unit PASS. server-go ./... 25 packages 全绿 (AL-3.1/RT-2/既有 typing path 全套不破) + client ./... 53 files / 423 tests 全绿. |
| 2026-04-30 | 战马D | CHN-12 整 milestone 一 PR (Phase 4+ — channel sort manual reorder; **0 schema / 0 server production code** — PUT /api/v1/me/layout CHN-3.2 #357 既有 batch upsert path byte-identical 不动 + 既有 SortableChannelItem 拖拽 handle byte-identical 跟 CHN-3.3 #415 同源; 跟 CHN-11 #563 / DM-6 #556 / CV-12 #545 / CV-9..12 系列 0-server 模式承袭): spec brief `chn-12-spec.md` 80 行 (≤80) + stance `chn-12-stance-checklist.md` 50 行 (≤80) + acceptance `chn-12.md` 30 行 (≤50) + content-lock `chn-12-content-lock.md` 90 行 (ChannelDragHandle DOM + ChannelList dnd-kit wiring + computeReorderPosition 单调小数算法 byte-identical 跟 CHN-3.3 #415 同源 + 同义词反向 reject + DM 反挂锁). CHN-12.1 schema 0 行 (复用 user_channel_layout.position REAL) + CHN-12.2 server **0 行 production** (CHN-3.2 #357 既有 PUT /me/layout + layout.invalid_payload + layout.dm_not_grouped + non-member 403 + layoutSaveErrorMsg 5 字面反向锚 byte-identical 不动) + CHN-12.3 client (`lib/dnd_position.ts::computeReorderPosition(prev, next)` pure fn 单调小数算法 byte-identical 跟 CHN-3.3 #415 acceptance §2.4 同源: 都 null → 1.0 / prev=null → next-1.0 / next=null → prev+1.0 / 中间 → 中点 (prev+next)/2.0; 既有 SortableChannelItem aria-label `拖拽调整顺序` + ⋮⋮ + data-sortable-handle byte-identical 不动 (CHN-3.3 #415 锚)) + CHN-12.4 closure (REG-CHN12-001..006 6 🟢; CHN-12 +6 行). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 user_channel_layout.position) + ② 0 server production code (反向 grep `chn_12/chn12/CHN12` 在 internal/api/*.go 非 _test.go 0 hit) + ③ 文案 byte-identical (跟 CHN-3.3 既有 `拖拽调整顺序` aria-label byte-identical, 不漂同义词 `移动/排序/拖动/抓握`) + ④ 既有 handlePutMyLayout block byte-identical (反向 grep chn_12 在 layout.go 3500 字符 block 0 hit + 3 既有锚 must-contain) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19, reorder per-user UI preference 不 audit, 跟 CHN-7 mute / CHN-8 notif-pref 立场 ⑥ 同精神) + ⑥ AST 锁链延伸第 20 处 forbidden 3 token (`pendingReorder / reorderQueue / deadLetterReorder`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-3.2 #357 既有 PUT /me/layout + 5 字面反向锚 (DM reject + non-member 403 + invalid_payload + 文案锁 + 字段反约束) byte-identical 不动 + CHN-3.3 #415 既有 SortableChannelItem 拖拽 handle 全套保留 + CHN-11 #563 / DM-6 #556 / CV-12 #545 0-server 模式承袭 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 20 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..11 + DM-6 + DM-7 + HB-6 + RT-4 + CHN-12 forbidden tokens 全 0 hit). 不在范围: group reorder (CHN-9 group ordering 已分别 milestone) / bulk reorder API (留 v3 — server PUT /me/layout 已 batch 单源) / pinned section drag-into-pinned (留 v3 — pinned 仅 CHN-6 toggle path) / DM reorder (永不 — DM 不参与分组立场 ④ byte-identical 跟 CHN-3.2 #357) / 跨用户共享 sort (永不 — per-user preference 立场 ②) / admin god-mode reorder override (永远不挂 ADM-0 §1.3) / audit log per reorder (留 v3). 10 unit PASS — server-go ./internal/api/ TestCHN121_NoSchemaChange + NoServerProductionCode + HandlerByteIdentical + TestCHN123_NoReorderQueue + NoAdminLayoutPath 5 unit + client packages/client vitest dnd_position.test.ts 5 PASS (5 case: 都 null fallback + prev=null + next=null + 中间中点 + 负数 position) = 总 10 unit PASS. server-go ./... 25 packages 全绿 (CHN-3.2 / CHN-3.3 既有 unit 全套不破) + client ./... 全绿. |
| 2026-04-30 | 战马D | CHN-13 整 milestone 一 PR (Phase 4+ — channel list search/filter; **0 schema / ≤15 行 server prod** — `handleListChannels` 加 `?q=` 子串 filter + `ListChannelsWithUnread(userID, q)` sig 加 q 参数; q="" 走既有 SQL byte-identical, q!="" 加 LIKE COLLATE NOCASE 大小写不敏感子串; 跟 CHN-12 #582 / CHN-11 #563 / DM-6 #556 系列 minimal-server 模式承袭): spec brief `chn-13-spec.md` 79 行 (≤80) + stance `chn-13-stance-checklist.md` 50 行 (≤80) + acceptance `chn-13.md` 35 行 (≤50) + content-lock `chn-13-content-lock.md` 95 行 (ChannelSearchInput DOM + ChannelList 空态/count + 同义词反向 reject + debounce 200ms 跟 CHN-3.3 useUserLayout PUT_DEBOUNCE_MS 同源 + server q 透传 byte-identical). CHN-13.1 schema 0 行 (复用 channels 既有表 + name 既有 index) + CHN-13.2 server (handleListChannels 加 `q := strings.TrimSpace(r.URL.Query().Get("q"))` + ListChannelsWithUnread(userID, q) sig 加 q + 空 q 路径 byte-identical 跟既有 SQL 同源, q!="" 加 `AND c.name LIKE '%' || ? || '%' COLLATE NOCASE` 子句 + 既有 callsites store_coverage_test.go + welcome_test.go 全填 q="" byte-identical) + CHN-13.3 client (`lib/api.ts::fetchChannels(q?)` thin wrapper 加 optional q + encodeURIComponent + 空 q URL byte-identical 不漂 `?q=`; `components/ChannelSearchInput.tsx` controlled input + debounce 200ms + placeholder/aria-label `搜索频道` byte-identical + 反向 grep同义词 reject) + CHN-13.4 closure (REG-CHN13-001..006 6 🟢; CHN-13 +6 行). 立场 3 项 + 3 边界: ① 0 schema 改 (复用 channels 既有表) + ② server 最小补丁 ≤15 行 production (handleListChannels 加 q 解析 + ListChannelsWithUnread sig 加 q) + ③ 文案 `搜索频道` placeholder/aria-label + `未找到匹配` 空态 + `共 N 个频道` count byte-identical + 同义词反向 reject (`find/lookup/locate/查找/检索/查询`) + ④ 既有 ListChannelsWithUnread 空 q 路径 byte-identical (反向 grep 锚: q="" 走原 SQL 不破; 既有 admin-rail /admin-api/v1/channels 不动) + ⑤ AL-1a reason 锁链不漂 (停在 HB-6 #19, search 是 read-only filter 不 audit, 跟 CHN-7 mute / CHN-8 notif-pref 立场 ⑥ 同精神) + ⑥ AST 锁链延伸第 21 处 forbidden 3 token (`pendingSearch / searchQueue / deadLetterSearch`) 在 internal/api 0 hit. 跨 milestone byte-identical 锁链: CHN-1.2 既有 ListChannelsWithUnread membership-scoped + cross-org public discovery + visibility='public' filter (CHN-9 #553 锚) byte-identical 不动 + CHN-3.3 useUserLayout debounce 200ms 同源 + ADM-0 §1.3 admin god-mode 不挂 + AST 锁链延伸第 21 处 (BPP-4/5/6/7/8 + HB-3 v2 + AL-7+AL-8 + HB-5 + CHN-5..12 + DM-6 + DM-7 + HB-6 + RT-4 forbidden tokens 全 0 hit). 不在范围: 全文搜索 (message body / artifact content) 留 v3 / 模糊匹配/pinyin 留 v3 / 跨 org 搜索 永不 (CM-3 既有禁) / admin-rail 搜索 留 v3 / 搜索历史 推荐 热门 留 v3+ / 搜索结果排序定制 v0 既有 ordering / audit log per search 永不. 10 unit PASS — server-go ./internal/api/ TestCHN131_NoSchemaChange + TestCHN132_ListChannelsWithQuery + QueryCaseInsensitive + QuerySubstringMatch + TestCHN133_NoSearchQueue + NoAdminSearchPath 5 unit (其中 ListChannelsWithQuery seed 3 channel + alpha/beta/gamma + q="alpha" 返回 1 channel + empty q 返回 ≥3) + client packages/client vitest ChannelSearchInput.test.tsx 5 PASS (placeholder + aria-label + debounce 200ms 真测 + 空 q 触发 + 同义词反向 grep) = 总 10 unit PASS. server-go ./... 25 packages 全绿 (既有 ListChannelsWithUnread callsites 全填 q="" byte-identical 不破) + client ./... 全绿. |
| 2026-04-30 | 战马E | CV-14 整 milestone 一 PR (Phase 4+ — artifact comment unread count badge, **0 server production code** 复用 useArtifactCommentAdded CV-5 #530 既有 WS hook; 跟 CV-9..13 / DM-5..6 / AP-4 / AP-5 0-server client-only 同模式): spec brief `cv-14-spec.md` 60 行 (≤80) + stance `cv-14-stance-checklist.md` 60 行 + acceptance `cv-14.md` 41 行 + content-lock `cv-14-content-lock.md` (文案 + DOM SSOT 单源). CV-14.1 server 反向断言 0 production diff (复用 ws.ArtifactCommentAddedFrame CV-5 #530 + useArtifactCommentAdded WS hook) + CV-14.2 client (`packages/client/src/components/CommentUnreadBadge.tsx` 新组件 — 订阅 useArtifactCommentAdded + filter `sender_id !== currentUserId` 不计自己 + count > 99 → "99+" overflow + click reset; 7 vitest PASS — ZeroNotRendered + OtherSenderIncrements + SelfSenderNotCounted + Overflow99Plus + ClickResets + DOMAttrs + NoStorageWrite) + CV-14.3 closure (REG-CV14-001..005 5 🟢; 总计 387→392 / active 361→366 / pending 26 不变). 立场 5 项: ① 0 server production code (反向断言 git diff 0 行) + ② 跟 CV-9 mention badge 共存 (mention 走 CV-9 既有, mention=更强 signal, mention comment 同时计入两 badge 是预期行为) + ③ thinking 5-pattern 锁链第 10 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13+CV-14) + ④ 文案 byte-identical 3 字面 (${N} 条新评论 中文模板 + 99+ overflow + zero-state null) + ⑤ DOM 2 data-attr (data-cv14-comment-unread-badge + data-cv14-unread-count). 跨 milestone byte-identical 锁链: CV-5 #530 ArtifactCommentAddedFrame WS hook + CV-9 #539 ArtifactCommentsMentionBadge 共存 + CV-13 #557 props-driven 同模式 + Sidebar.tsx unread-badge 99+ overflow 同精神 + thinking 锁链第 10 处 + ADM-0 §1.3 红线. 不在范围: persistent unread 跨 reload (留 v2 复用 channel.unread_count) / admin god-mode unread (永久不挂) / desktop notification (DL-4 路径) / schema 改 / 新 endpoint / unread per-comment highlight (留 v3). vitest 64 files / 477 tests 全 PASS + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行验证. |
| 2026-04-30 | 战马E | DM-9 整 milestone 一 PR (Phase 4+ — DM message emoji picker, **0 server production code + 0 schema 改 反 v=39 硬锁** 复用 CV-7 #535 PUT /api/v1/messages/{id}/reactions + AP-4 #551 ACL gate; 跟 CV-9..14 / DM-5..6 / AP-4..5 同模式): spec brief `dm-9-spec.md` 70 行 + stance + acceptance + content-lock (5 emoji preset + 文案 + DOM SSOT 三轨字面). DM-9.1 server 反向断言 0 行 + 反 v=39 schema 硬锁 + DM-9.2 client (`packages/client/src/components/EmojiPickerPopover.tsx` 新组件 — 5 emoji preset 顺序 byte-identical 👍 ❤️ 😄 🎉 🚀 + toggle + Escape/outside-click 关 popover + click → addReaction + onChanged callback; 8 vitest PASS — ToggleByteIdentical + DefaultClosed + OpenAndPresetOrder + EmojiClickTriggersAddReaction + EscapeCloses + OutsideClickCloses + DOMAttrs + NoStorageWrite) + DM-9.3 closure (REG-DM9-001..005 5 🟢; 总计 392→397). 立场 5 项: ① 0 server production code (反 v=39 schema 硬锁, 既有 reactions 表 + endpoint 已盖) + ② 5-emoji preset byte-identical 顺序 + ③ thinking 5-pattern 锁链第 11 处 (RT-3+DM-3+DM-4+CV-7+CV-8+CV-9+CV-11+CV-12+CV-13+CV-14+DM-9) + ④ DOM 4 data-attr (toggle + popover + 5 option + open 状态) + ⑤ 跟 DM-5 ReactionSummary 互斥共存 (picker=加新, ReactionSummary=显示+toggle, click 后 onChanged callback). 跨 milestone byte-identical 锁链: CV-7 #535 PUT reactions endpoint + AP-4 #551 ACL gate + DM-5 #549 ReactionSummary 视觉互补 + CV-7 ArtifactCommentItem 不另起 picker 反约束 (DM-9 今天补) + thinking 锁链第 11 处 + ADM-0 §1.3 红线. 不在范围: custom emoji upload (v3) / search filter / admin god-mode (永久) / schema 改 / v=39 migration (反硬锁) / skin tone (v2). vitest 70 files / 510 tests 全 PASS + typecheck 全绿 + git diff origin/main -- packages/server-go/ 0 行 + 反 v=39 grep 0 hit 验证. |
| 2026-04-30 | 战马E | ADM-3 整 milestone 一 PR (Phase 4 admin-model 收尾, BPP-8 #532 名实不符 follow-up, **元数据 RENAME 0 数据迁移 + view backward compat**): spec brief `adm-3-spec.md` 80 行 (飞马 v0 #570 起草) + stance `adm-3-stance-checklist.md` 60 行 + acceptance `adm-3.md` 38 行 + content-lock 不需 (server-only schema). ADM-3.1 schema migration v=43 (`internal/migrations/adm_3_1_audit_events_rename.go` ALTER admin_actions RENAME TO audit_events 元数据 0 数据迁移 + CREATE VIEW admin_actions AS SELECT * FROM audit_events alias backward compat + INSTEAD OF INSERT/UPDATE triggers 路由 view → table; v=43 sequencing post-cv-6 v=36 跟 team-lead 占号 reservation 一致) + ADM-3.2 backward compat 验证 (既有 ADM-2 + BPP-8 + AL-7 + AL-8 unit tests 全 PASS via alias view; AuditEventsTableExists + AdminActionsViewExists + ViewSelectRoundtrip + ViewInsertRoutedToTable + VersionIs43 + Idempotent 6 unit PASS) + ADM-3.3 closure (REG-ADM3-001..005 5 🟢; ADM-0 §1.3 红线扩展段落 ≤5 行 audit-forward-only 单表跨全 actor type 立场写进 docs/blueprint/admin-model.md 实施 PR 顺手). 立场 3 项: ① 表名改 audit_events 单表跨全 actor type (admin + plugin_system + system) + ② alias view backward compat 战马原代码 0 改 (gorm AdminAction.TableName == admin_actions 写通过 INSTEAD OF trigger 路由 audit_events table) + ③ ADM-0 §1.3 红线扩展 audit-forward-only 不可 DELETE/UPDATE + admin 仍走 /admin-api/*. 跨 milestone byte-identical 锁链: ADM-2.1 #484 起源 + BPP-8 #532 plugin lifecycle 名实不符今修 + AL-7.1 archived_at 跟随 RENAME 自动 + AL-7.2 sweeper UPDATE archived_at 走 audit_events 直接 + audit-forward-only 锁链跨 11 处. 不在范围: 字段重设计 (留 v3+) / 函数名 RENAME (留 Phase 4+) / view 删除 (留 Phase 4+ deprecation). server-go TestADM31_* 6 PASS + 全 25 packages 全绿 (+sqlite_fts5 tag) 含 ADM-2/BPP-8/AL-7 backward compat 验证. |

