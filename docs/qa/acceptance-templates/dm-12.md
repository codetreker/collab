# Acceptance Template — DM-12: DM message reaction picker composite ✅

> 0 server code (复用 CV-7 #535 + AP-4 #551 endpoint + DM-9 #585 picker + DM-5 #549 chip composite). 4件套全闭. content-lock §3 文案/preset 全 delegate 到 DM-9/DM-5 SSOT.

## 验收清单

### §1 DM-12.1 — server 反向断言 0 行

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 0 server production 行 | git diff | `git diff origin/main -- packages/server-go/` 0 production lines |
| 1.2 反向 grep `dm12.*server\|reactions_picker_table` 0 hit | grep | server-go internal/ 0 hit |
| 1.3 复用 CV-7 #535 reactions endpoint + AP-4 #551 ACL gate | inspect | 既有 |

### §2 DM-12.2 — client DMMessageReactionPicker composite

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 root + picker toggle 渲染 on mount | vitest | `DMMessageReactionPicker.test.tsx::§2.1` PASS |
| 2.2 empty initialReactions → ReactionSummary 不渲染 | vitest | `::§2.2` PASS |
| 2.3 non-empty initialReactions → ReactionSummary chip 渲染 | vitest | `::§2.3` PASS (data-dm5-reaction-chip 锁) |
| 2.4 initialReactions undefined → auto-fetch on mount | vitest | `::§2.4` PASS (getMessageReactions 真调) |
| 2.5 emoji picker click → addReaction + refetch chain | vitest | `::§2.5` PASS (addReaction + getMessageReactions 调用顺序锁) |
| 2.6 DOM data-attr 锁 (data-dm12-reaction-picker root + delegate DM-9/DM-5) | vitest | `::§2.6` PASS |
| 2.7 不写 sessionStorage / localStorage | vitest | `::§2.7` PASS |

### §3 DM-12.3 — closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 thinking 5-pattern 反向 grep 0 hit (锁链第 12 处) | grep | DMMessageReactionPicker.tsx 0 hit |
| 3.2 反向 grep `EMOJI_PRESET\|emojiPreset` 0 hit (复用 DM-9 单源) | grep | 0 hit |
| 3.3 反向 grep `👍\|❤️\|😄\|🎉\|🚀` 0 hit (preset 全 delegate DM-9) | grep | 0 hit |
| 3.4 REG-DM12-001..006 6 行 🟢 | regression-registry.md | 6 行 |
| 3.5 PROGRESS [x] 加行 | PROGRESS.md (phase-4 changelog) | 1 行 |
| 3.6 acceptance template ✅ closed | 本文件 | 关闭区块加日期 |

### §4 留账 (follow-up wire-up PR)

- MessageItem.tsx 真挂 DMMessageReactionPicker (channel.type === 'dm' filter + 替换既有 ReactionBar 重 emoji-mart 包) — 留 follow-up PR

## 边界

- DM-9 #585 EmojiPickerPopover (5-emoji preset 复用) / DM-5 #549 ReactionSummary (chip 渲染复用) / CV-7 #535 PUT/DELETE/GET reactions endpoint (SSOT) / AP-4 #551 channel-member ACL gate / thinking 5-pattern 锁链第 12 处 / ADM-0 §1.3 红线 / DM-10 #597 / DM-11 #600 client-only 同模式

## 退出条件

- §1+§2+§3 全绿
- 0 server production 代码
- vitest DMMessageReactionPicker 7 case PASS
- 文案/preset 全 delegate DM-9/DM-5 (反向 grep 0 hit)
- REG-DM12-001..006 6 行

## 关闭

✅ 2026-04-30 战马E — 0 server diff (`git diff origin/main -- packages/server-go/` 0 行) + vitest 77 files / 543 tests 全 PASS (含 DMMessageReactionPicker 7 case) + typecheck 全绿; thinking 5-pattern 锁链第 12 处 (RT-3 + DM-3 + DM-4 + CV-7 + CV-8 + CV-9 + CV-11 + CV-12 + CV-13 + CV-14 + DM-9 + DM-12); preset/文案/chip 全 delegate DM-9 #585 + DM-5 #549 (反向 grep `EMOJI_PRESET` + `👍❤️😄🎉🚀` 在 DMMessageReactionPicker.tsx 0 hit); 2 DOM 锚 (`data-dm12-reaction-picker` + `data-dm12-loading`).
