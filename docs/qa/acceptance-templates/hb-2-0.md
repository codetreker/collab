# Acceptance Template — HB-2.0 prerequisite ✅ (CI matrix + IPC primitive smoke)

> 真 infra PR (跟 INFRA-2 #195 Playwright scaffold 同模式). HB stack 4 步路径第 2 步 — HB-2 v0(C) Go daemon 实施前置. content-lock 不需 (CI infra + helper package).

## 验收清单

### §1 HB-2.0.1 — helper package single source

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 IPCPlatform 三态 const (linux-uds / darwin-uds / windows-named-pipe) byte-identical | inspect | `internal/helper/ipc.go` 30 行 |
| 1.2 IPCEndpointDefault switch 三 OS endpoint default 字面 | inspect | helper.go switch case |
| 1.3 反向 grep `IPCPlatform\|IPCEndpointDefault` 在 internal/ 除 helper/ 0 hit (单源闸) | grep | 0 hit |

### §2 HB-2.0.2 — 3 IPC unit per platform (build tag)

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 TestHB20_IPC_UDSConnect_Linux (build tag linux) PASS on Ubuntu runner | unit | ci.yml hb20-ipc-prereq matrix `ubuntu-latest` |
| 2.2 TestHB20_IPC_UDSConnect_macOS (build tag darwin) PASS on macOS runner | unit | matrix `macos-latest` |
| 2.3 TestHB20_IPC_NamedPipeConnect_Windows (build tag windows) PASS on Windows runner | unit | matrix `windows-latest` |
| 2.4 字面 endpoint default + label byte-identical 三平台对锁 | unit | 各 _test.go `if got != want` 字面比对 |

### §3 HB-2.0.3 — CI matrix job

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 .github/workflows/ci.yml 加 `hb20-ipc-prereq` job | inspect | ci.yml 新 job 18 行 |
| 3.2 strategy.matrix.os = [ubuntu-latest, macos-latest, windows-latest] + fail-fast: false | inspect | strategy block |
| 3.3 runs-on: ${{ matrix.os }} 跨平台 runner | inspect | runs-on 字面 |
| 3.4 go test ./internal/helper/... 三 OS 全运行 | CI run | matrix expansion |

### §4 HB-2.0.4 — closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 4.1 反向 grep IPC primitive 字面单源 (3 endpoint string) 在 internal/ 除 helper/ + 除 _test 0 hit | grep | 0 hit |
| 4.2 REG-HB20-001..003 3 行 🟢 | regression-registry.md | 3 行 |
| 4.3 PROGRESS [x] 加行 (phase-4.md changelog) | PROGRESS | 1 行 |
| 4.4 acceptance template ✅ closed | 本文件 | 关闭区块加日期 |

## 边界

- INFRA-2 #195 Playwright scaffold 同模式 (真 infra PR) / HB-1 #491 install-butler audit log schema 跨 OS 同精神 / HB-2 spec §1.3 红线 admin god-mode 不挂 host IPC / HB-3 host_grants schema (HB-2 v0(C) read-only consumer 接入点) / HB stack 4 步路径 — HB stack Go spec patch #599 → 本 PR HB-2.0 → HB-2 v0(C) → HB-3 / AL-1a 6-dict + HB-1 7-dict + HB-2 8-dict 三字典分立 (HB-2.0 仅 IPC primitive label, 不入 reason 字典)

## 退出条件

- §1+§2+§3+§4 全绿
- ≤30 行 production code (helper.go ipc.go) + ≤18 行 CI yml
- 3 unit per platform PASS (Linux runner CI 真验本地; macOS + Windows runner CI 真验)
- REG-HB20-001..003 3 行

## 关闭

✅ 2026-04-30 战马E — `internal/helper/ipc.go` 30 行 IPC primitive 单源 + 3 unit per platform (build-tag) + ci.yml 加 hb20-ipc-prereq matrix job (ubuntu/macos/windows runner) + 反向 grep 字面单源 0 hit; Linux runner 本地 TestHB20_IPC_UDSConnect_Linux PASS (macOS + Windows runner 真验留 CI run); REG-HB20-001..003 3 🟢. HB-2 v0(C) Go daemon (~400-500 行 packages/borgee-helper/cmd/) 留 follow-up PR 跟 HB-3 host_grants schema 同期接.
