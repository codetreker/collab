# Acceptance Template — CV-11: artifact comment markdown 渲染 ✅

> 跟 CV-9 #539 / CV-10 #541 client-only + DM-2.3 markdown 同模式. **0 server code + 0 schema 改 + 0 新 lib**. Spec + Stance + Content-lock.

## 验收清单

### §1 CV-11.1 — server 0 production code + 1 unit 反向断

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 立场 ① 0 server code — git diff packages/server-go/ excl _test.go = 0 行 | git diff | `git diff main..HEAD -- 'packages/server-go/**/*.go' ':!**/*_test.go'` 0 行 |
| 1.2 server 不解 markdown — comment body 落 messages.content raw text (跟 CV-1.2 既有 artifact body 同模式) | unit | `internal/api/cv_11_no_markdown_test.go::TestCV11_ServerStoresRawMarkdown` PASS |

### §2 CV-11.2 — client ArtifactCommentBody.tsx + DOM/sanitize 锁

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 立场 ④ DOM `data-cv11-comment-body` 锚 + 渲染基本 markdown (`**bold**`, `*italic*`, `` `code` ``, list) | vitest | `ArtifactCommentBody.test.tsx::renders_basic_markdown` |
| 2.2 立场 ④ sanitize 反向断 — `<script>alert(1)</script>` 输入后 DOM querySelector('script') == null + onerror= 等 inline event 全删 | vitest | `ArtifactCommentBody.test.tsx::sanitize_xss` (3 sub-case: script/iframe/onerror=) |
| 2.3 立场 ① 复用 renderMarkdown 既有 — `<@uuid>` mention 渲染 (复用 lib/markdown 既有 mention path) | vitest | `ArtifactCommentBody.test.tsx::mention_render` |
| 2.4 空 body fallback — 不渲染 / 渲染 placeholder | vitest | `ArtifactCommentBody.test.tsx::empty_body` |

### §3 CV-11.3 — closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 反向 grep 5 锚 4 处 0 hit + DOM ≥1 hit | CI grep | content-lock §1 + §3 |
| 3.2 REG-CV11-001..005 5 行 🟢 active | regression-registry.md | 5 行 |

## 边界

- DM-2.3 #388 (renderMarkdown 既有 mention 路径) / lib/markdown.ts (marked + DOMPurify 单源) / CV-1.2 (artifact body raw text 落库, client 渲染 — CV-11 同模式) / CV-7/CV-8 (thinking 5-pattern 第 8 处链 byte-identical 锁) / ADM-0 §1.3 admin rail 红线

## 退出条件

- §1+§2+§3 全绿
- 0 server code + 0 schema 改 + 0 新 lib
- REG-CV11-001..005 5 行
