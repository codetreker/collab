# Acceptance Template — DM-11: DM cross-DM message search ✅

> 0 schema 改 + DM-only scope JOIN c.type='dm' + channel-member ACL JOIN cm.user_id = caller (复用 AP-4 #551 + AP-5 #555 立场承袭). 4件套全闭 (content-lock §4 client UI 文案锁住但实施留 follow-up PR).

## 验收清单

### §1 DM-11.1 — store helper

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 SearchDMMessages JOIN messages × users × channels (type='dm') × channel_members | unit | `internal/store/dm_11_search_queries.go` 定义 + API 层 unit 间接验证 |
| 1.2 LIKE %query% + ORDER BY created_at DESC + clamp limit 50 | unit | API unit (HappyPath + LimitClamp) PASS |
| 1.3 maskDeletedMessages 复用 (反 deleted leak) | unit | `TestDM11_Search_DeletedMessageHidden` PASS |

### §2 DM-11.2 — server endpoint

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 happy path — owner 搜 member 在 DM 发的消息 → 1 result | unit | `TestDM11_Search_HappyPath` PASS |
| 2.2 q 缺失 → 400 `dm_search.q_required` (字面 byte-identical) | unit | `TestDM11_Search_QRequired` PASS |
| 2.3 q < 2 char → 400 `dm_search.q_too_short` | unit | `TestDM11_Search_QTooShort` PASS |
| 2.4 q > 200 char → 400 `dm_search.q_too_long` (反 DoS) | unit | `TestDM11_Search_QTooLong` PASS |
| 2.5 unauthorized → 401 | unit | `TestDM11_Search_Unauthorized` PASS |
| 2.6 no match → 200 + count=0 + 空 list | unit | `TestDM11_Search_NoMatch` PASS |
| 2.7 DM-only scope — 公开 channel 同 query 消息 NOT 出现 (立场 ②) | unit | `TestDM11_Search_DMOnly_ExcludesPublicChannel` PASS |
| 2.8 channel-member ACL — 第三方 user 搜同 query → 0 results (立场 ③) | unit | `TestDM11_Search_NonMember_NoLeak` PASS |
| 2.9 limit=999 → clamp 50 (反 cursor 滥用) | unit | `TestDM11_Search_LimitClamp` PASS |
| 2.10 deleted message NOT in result (maskDeletedMessages 守) | unit | `TestDM11_Search_DeletedMessageHidden` PASS |

### §3 DM-11.3 — closure + 反向 grep

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 0 schema 改 反向断 | git diff | `git diff origin/main -- packages/server-go/internal/migrations/` 0 production 行 |
| 3.2 admin god-mode 不挂 反向 grep 0 hit (ADM-0 §1.3) | grep | server-go internal/api/admin*.go `admin.*dm.*search` 0 hit |
| 3.3 单源 messages.content 反向 grep 0 hit (不另起 dm_search_index 表) | grep | `dm_search_index\|dm_search_table\|dm_11_search_log` 0 hit |
| 3.4 FTS5 不走 (留 v2) 反向 grep 0 hit | grep | `fts5\|MATCH.*dm_search\|VIRTUAL TABLE.*dm` 0 hit |
| 3.5 REG-DM11-001..006 6 行 🟢 | regression-registry.md | 6 行 |
| 3.6 PROGRESS [x] 加行 | PROGRESS.md | changelog 加行 |
| 3.7 acceptance template ✅ closed | 本文件 | 关闭区块加日期 |

### §4 client UI (留 follow-up PR — content-lock §4 文案锁住, 实施未做)

- DM search bar 组件 (input + result list + empty state + count)
- 文案 byte-identical (`搜索 DM 消息` / `未找到匹配` / `共 N 条消息` / `搜索失败, 请稍后重试`)

## 边界

- CV-12 #545 + CHN-13 #583 既有 search endpoint LIKE 模式 (per-channel) / DM-10 #597 DM-only path (channel.Type='dm' filter 同精神) / AP-4 #551 reactions ACL + AP-5 #555 messages ACL (channel-member helper 复用) / dm_4_message_edit.go #549 DM-only scope / ADM-0 §1.3 admin god-mode 红线 / PR #571 admin-godmode 总表 §2 / maskDeletedMessages helper (跟 SearchMessages #467 共享 — 反 deleted leak)

## 退出条件

- §1+§2+§3 全绿
- 10 unit 全 PASS
- server-go ./... 全 25 packages 全绿 (+sqlite_fts5 tag)
- 反向 grep 4 锚 0 hit
- REG-DM11-001..006 6 行

## 关闭

✅ 2026-04-30 战马E — 0 schema 改 + DM-only scope (channels.type='dm' JOIN) + channel-member ACL (channel_members JOIN cm.user_id = caller) 复用 AP-4 #551 + AP-5 #555 立场承袭 + admin god-mode 不挂 (ADM-0 §1.3) + 3 字面错码 + clamp 阈值 (q 2..200 / limit max 50); 10 unit PASS + server-go ./... 全 25 packages 全绿 (+sqlite_fts5 tag); 反向 grep 4 锚全 0 hit; REG-DM11-001..006 6 🟢; client UI (DM search bar + 4 文案 byte-identical) 留 follow-up PR.
