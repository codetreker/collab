# Acceptance Template — CV-12: artifact comment search ✅ closed

> 跟 CV-9/10/11 client-only 同模式 + CV-5 namespace 单源. **0 server code + 0 schema 改 + 0 新 endpoint + 0 新 lib**.

## 验收清单

### §1 CV-12.1 — server 0 code + 1 unit

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 0 server code (反向断 git diff) | git diff | `git diff main..HEAD -- 'packages/server-go/**/*.go' ':!**/*_test.go'` 0 行 |
| 1.2 既有 message-search endpoint 在 namespace channel 工作 byte-identical | unit | `internal/api/cv_12_search_namespace_test.go::TestCV12_SearchInArtifactNamespace` PASS |

### §2 CV-12.2 — client searchArtifactComments + ArtifactCommentSearchBox.tsx

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 立场 ④ DOM `data-cv12-search-input` 锚 + 渲染输入 | vitest | `ArtifactCommentSearchBox.test.tsx::input_anchor` |
| 2.2 立场 ④ 0 result 文案 "未找到匹配评论" byte-identical | vitest | `ArtifactCommentSearchBox.test.tsx::no_result_copy` |
| 2.3 立场 ④ 结果 list 渲染 `data-cv12-search-result-id` | vitest | `ArtifactCommentSearchBox.test.tsx::result_list` |
| 2.4 立场 ④ 空 query 不调 API (反向断) | vitest | `ArtifactCommentSearchBox.test.tsx::empty_query_no_api_call` |

### §3 CV-12.3 — e2e 3 case + closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 e2e: seed 3 comments → search "needle" → 1 hit | E2E | `cv-12-comment-search.spec.ts::needle hit` |
| 3.2 e2e: search "absent" → 0 hit | E2E | `cv-12-comment-search.spec.ts::no result` |
| 3.3 e2e: cross-channel reject — non-member 不能 search | E2E | `cv-12-comment-search.spec.ts::cross-channel reject` |
| 3.4 反向 grep 5 锚 4 处 0 hit + DOM/文案 ≥1 hit | CI grep | content-lock §3 |
| 3.5 REG-CV12-001..005 5 行 🟢 | regression-registry.md | 5 行 |

## 边界

- CV-5 #530 (artifact namespace channel 单源) / messages.go::handleSearchMessages (既有 readPerm + ACL) / lib/api 既有 search 路径 / ADM-0 §1.3 admin 红线

## 退出条件

- §1+§2+§3 全绿
- 0 server code + 0 schema 改
- REG-CV12-001..005 5 行
