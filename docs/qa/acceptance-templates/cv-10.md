# Acceptance Template — CV-10: artifact comment 草稿持久化 + restore ✅ closed

> CV-9 #539 client-only 同模式 + DM-4 既有 draft 模式承袭. **0 server production code + 0 schema 改 + 0 新 endpoint**. Spec `cv-10-spec.md` (aa743bc) + Stance + Content-lock.

## 验收清单

### §1 CV-10.1 — client useArtifactCommentDraft hook + ArtifactCommentDraftInput component

| 验收项 | 实施方式 | Owner | 实施证据 |
|---|---|---|---|
| 1.1 立场 ① localStorage 单源 + key namespace `borgee.cv10.comment-draft:<artifactId>` byte-identical | vitest (4 case) | 战马E / 烈马 | `useArtifactCommentDraft.test.ts` (load empty / save → reload restore / clear after submit / debounce 防抖) |
| 1.2 立场 ② submit 后 localStorage.removeItem(key) → 后续 load returns null | vitest | 战马E / 烈马 | `useArtifactCommentDraft.test.ts::clear_after_submit` |
| 1.3 立场 ② beforeunload 复用浏览器原生 (event.returnValue) draft 非空时 — 反约束不挂自定义 modal | vitest | 战马E / 烈马 | `ArtifactCommentDraftInput.test.tsx::beforeunload_native` |
| 1.4 立场 ④ DOM `data-cv10-draft-textarea="<artifactId>"` + `data-cv10-restore-toast` + 文案 byte-identical | vitest | 战马E / 烈马 | `ArtifactCommentDraftInput.test.tsx::dom_and_copy` (DOM data-attr + restore toast 文案 + cleared toast 文案) |

### §2 CV-10.2 — e2e

| 验收项 | 实施方式 | Owner | 实施证据 |
|---|---|---|---|
| 2.1 e2e: type → reload → draft restored 文案显示 | E2E | 战马E / 烈马 | `cv-10-comment-draft.spec.ts::reload restore` |
| 2.2 e2e: submit → localStorage cleared → reload → 输入框为空 | E2E | 战马E / 烈马 | `cv-10-comment-draft.spec.ts::submit clears draft` |
| 2.3 e2e: navigate away with non-empty draft → beforeunload 触发 | E2E | 战马E / 烈马 | `cv-10-comment-draft.spec.ts::leave warning` |

### §3 CV-10.3 — closure

| 验收项 | 实施方式 | Owner | 实施证据 |
|---|---|---|---|
| 3.1 0 server production code — git diff packages/server-go/ excl _test.go = 0 行 | git diff | 飞马 / 烈马 | `git diff main..feat/cv-10 -- 'packages/server-go/**/*.go' ':!**/*_test.go'` 0 行 |
| 3.2 0 schema 改 — git diff packages/server-go/internal/migrations/ 0 行 | git diff | 飞马 / 烈马 | git diff 0 行 |
| 3.3 反向 grep 6 锚: 4 处 0 hit + DOM/文案 ≥1 hit (cv-10-spec.md §3 字面) | CI grep | 飞马 / 烈马 | CI lint |
| 3.4 REG-CV10-001..005 5 行 🟢 active | regression-registry.md | 战马E / 烈马 | 5 行 |

## 边界

- CV-9 #539 (client-only 同模式) / DM-4 (既有 draft localStorage 模式) / CV-5 #530 + CV-7 + CV-8 (既有 comment endpoint, submit 走) / ADM-0 §1.3 admin rail 红线 / 隐私 §13 (草稿仅本地)

## 退出条件

- §1 (4) + §2 (3) + §3 (4) 全绿
- 0 schema 改 + 0 server production code
- 反向 grep 6 锚通过
- 登记 REG-CV10-001..005
