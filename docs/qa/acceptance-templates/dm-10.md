# Acceptance Template — DM-10: DM message pin ✅

> 1 列 ALTER + DM-only scope + channel-member ACL gate (复用 AP-4 #551 + AP-5 #555 同 helper). 4件套全闭. content-lock 不需 (server-only API, 文案/DOM client UI 留 follow-up PR).

## 验收清单

### §1 DM-10.1 — schema migration v=45

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 messages.pinned_at INTEGER NULL 列存在 + nullable | unit | `internal/migrations/dm_10_1_messages_pinned_at_test.go::TestDM101_AddsPinnedAtColumn` PASS |
| 1.2 sparse partial idx_messages_pinned_at WHERE pinned_at IS NOT NULL | unit | `TestDM101_HasSparseIdx` PASS (channel_id + pinned_at DESC + WHERE 锁) |
| 1.3 v=45 sequencing (team-lead 占号) | unit | `TestDM101_VersionIs45` PASS |
| 1.4 idempotent re-run no-op | unit | `TestDM101_Idempotent` PASS |

### §2 DM-10.2 — server endpoints (DM-only)

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 POST pin → 200 + pinned_at non-null + GET list shows / DELETE unpin → 200 + pinned_at null + GET list empty (happy path 全闭环) | unit | `internal/api/dm_10_pin_test.go::TestDM10_PinUnpin_HappyPath` PASS |
| 2.2 non-DM channel pin → 400 `pin.dm_only_path` (立场 ②) | unit | `TestDM10_DMOnly_NonDMRejected` PASS |
| 2.3 unauthorized → 401 | unit | `TestDM10_Unauthorized_401` PASS |
| 2.4 non-member → 404 fail-closed (立场 ③ AP-4/AP-5 同 helper) | unit | `TestDM10_NonMember_404` PASS |
| 2.5 deleted message → 404 (GetMessageByID filter deleted_at IS NULL, fail-closed before pin path) | unit | `TestDM10_DeletedMessage_404` PASS |
| 2.6 cross-channel msg ID → 404 (反 cross-channel pin) | unit | `TestDM10_CrossChannelMessage_404` PASS |
| 2.7 idempotent pin twice → 200 + 200 (last-write-wins) | unit | `TestDM10_Idempotent_PinTwice` PASS |
| 2.8 idempotent unpin unpinned → 200 + pinned=false | unit | `TestDM10_UnpinUnpinned_Idempotent` PASS |

### §3 DM-10.3 — closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 admin god-mode 不挂 反向 grep 0 hit (ADM-0 §1.3) | grep | server-go internal/api/admin*.go `admin.*pin.*messages` 0 hit |
| 3.2 单源 messages.pinned_at 反向 grep 0 hit (不另起 pinned_messages 表) | grep | `pinned_messages\|message_pin_log\|dm10_pin_table` 0 hit |
| 3.3 反向枚举 — pinned_by / pin_reason 不挂列 | grep | models.go 0 hit |
| 3.4 channel-member ACL helper 复用 (AP-4/AP-5 同源) | grep | dm_10_pin.go ≥1 hit IsChannelMember + CanAccessChannel |
| 3.5 REG-DM10-001..006 6 行 🟢 | regression-registry.md | 6 行 |
| 3.6 PROGRESS [x] 加行 | PROGRESS.md | changelog 加行 |
| 3.7 acceptance template ✅ closed | 本文件 | 关闭区块加日期 |

## 边界

- DM-7.1 #558 messages.edit_history (ALTER ADD COLUMN nullable 同模式) / AL-7.1 #533 admin_actions.archived_at (sparse partial idx WHERE 同模式) / AP-2.1 #525 / AP-1.1 #493 跨九 milestone ALTER ADD nullable / CHN-6 #544 channel pin (PinThreshold 互补) / CHN-7 mute / CHN-15 readonly — 互补二式同模式 / AP-4 #551 reactions ACL / AP-5 #555 messages ACL (channel-member ACL helper 复用) / dm_4_message_edit.go #549 DM-only path (`dm.edit_only_in_dm` 同精神) / ADM-0 §1.3 红线

## 退出条件

- §1+§2+§3 全绿
- 12 unit (4 migration + 8 API) 全 PASS
- server-go ./... 全 25 packages 全绿 (+sqlite_fts5 tag)
- 反向 grep 4 锚 0 hit
- REG-DM10-001..006 6 行

## 关闭

✅ 2026-04-30 战马E — schema v=45 messages.pinned_at NULLABLE + sparse partial idx + 3 endpoint (POST/DELETE/GET) + DM-only ACL gate + channel-member ACL gate (复用 AP-4 #551 + AP-5 #555 同 helper) + admin god-mode 不挂; 12 unit PASS (4 migration + 8 API) + server-go ./... 全 25 packages 全绿 (+sqlite_fts5 tag); 反向 grep 4 锚全 0 hit; REG-DM10-001..006 6 🟢.
