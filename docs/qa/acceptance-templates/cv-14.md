# Acceptance Template — CV-14: artifact comment unread badge ✅

> 0 server code (跟 CV-9..13 / DM-5..6 / AP-4 / AP-5 同模式) — comment unread 视觉 badge 走 useArtifactCommentAdded 既有 WS hook + frame.mentions 双 filter (CV-9 mention 互斥). 4 件套全闭.

## 验收清单

### §1 CV-14.1 — server 反向断言 0 行

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 0 server production 行 | git diff | `git diff origin/main -- packages/server-go/` 0 production lines |
| 1.2 反向 grep `comment_unread_endpoint\|cv14.*server\|cv14_unread_table` 0 hit | grep | server-go internal/ 0 hit |
| 1.3 复用 ws.ArtifactCommentAddedFrame (CV-5 #530) + frame.mentions (CV-9 #539) | inspect | 既有 |

### §2 CV-14.2 — client CommentUnreadBadge

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 sender_id != currentUserId → 计数 +1 | vitest | `CommentUnreadBadge.test.tsx::TestCV14_OtherSenderIncrements` PASS |
| 2.2 sender_id == currentUserId → 不计数 (自己发的不计) | vitest | `::TestCV14_SelfSenderNotCounted` PASS |
| 2.3 文案 `${N} 条新评论` byte-identical | vitest | `::TestCV14_LabelByteIdentical` PASS |
| 2.4 count > 99 → `99+` overflow display | vitest | `::TestCV14_Overflow99Plus` PASS |
| 2.5 count == 0 → 不渲染 (return null) | vitest | `::TestCV14_ZeroNotRendered` PASS |
| 2.6 click → reset count + 消失 | vitest | `::TestCV14_ClickResets` PASS |
| 2.7 DOM 2 data-attr 锚 byte-identical | vitest | `::TestCV14_DOMAttrs` PASS |
| 2.8 不写 sessionStorage / localStorage | grep | 0 hit |
| 2.9 不 import api / fetch | grep | 0 hit |

### §3 CV-14.3 — closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 thinking 5-pattern 反向 grep 0 hit (锁链第 10 处) | grep | CommentUnreadBadge.tsx 0 hit |
| 3.2 文案 byte-identical (`{N} 条新评论` + `99+`) | grep | content-lock §1 字面同步 |
| 3.3 REG-CV14-001..005 5 行 🟢 | regression-registry.md | 5 行 |
| 3.4 PROGRESS [x] 加行 | PROGRESS.md | changelog 加行 |
| 3.5 acceptance template ✅ closed | 本文件 | 关闭区块加日期 |

## 边界

- CV-5 #530 ArtifactCommentAddedFrame (SSOT 入口) / CV-9 #539 ArtifactCommentsMentionBadge (mention badge 互斥) / CV-13 #557 props-driven 同模式 / Sidebar.tsx unread-badge `99+` overflow 同精神 / ADM-0 §1.3 admin god-mode 红线 / thinking 5-pattern 锁链第 10 处

## 退出条件

- §1+§2+§3 全绿
- 0 server production 代码
- vitest CommentUnreadBadge 8 case PASS
- 文案 + DOM byte-identical (content-lock §1+§2)
- REG-CV14-001..005 5 行

## 关闭

✅ 2026-04-30 战马E — 0 server diff (`git diff origin/main -- packages/server-go/` 0 行) + vitest 64 files / 477 tests 全 PASS (含 CommentUnreadBadge 7 case) + typecheck 全绿; thinking 5-pattern 锁链第 10 处 (RT-3 + DM-3 + DM-4 + CV-7 + CV-8 + CV-9 + CV-11 + CV-12 + CV-13 + CV-14); 文案 byte-identical (`{N} 条新评论` + `99+` overflow + zero-state null); DOM 2 锚 (`data-cv14-comment-unread-badge` + `data-cv14-unread-count`).
