# Acceptance Template — CV-13: artifact comment quote / reference ✅

> 0 server code (跟 CV-9..12 / DM-5 / AP-4 / AP-5 同模式) — quote 视觉块从内存 cache 渲染. spec + stance + content-lock + acceptance 4 件套全闭.

## 验收清单

### §1 CV-13.1 — server 反向断言 0 行

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 1.1 0 server production 行 | git diff | `git diff origin/main -- packages/server-go/` 0 production lines |
| 1.2 反向 grep `quote_event\|comment_quote_table\|cv13.*server` 0 hit | grep | server-go internal/ 0 hit |
| 1.3 reply_to_id 列复用 (CV-8 #441 既有) | inspect | messages.go ReplyToID 入参既有 (line 199) + queries.go reply_to_id 既有 (line 50) |

### §2 CV-13.2 — client QuotedCommentBlock + ArtifactCommentItem wire

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 2.1 QuotedCommentBlock.tsx happy-path render (parent existing) | vitest | `QuotedCommentBlock.test.tsx::TestCV13_HappyPath` PASS |
| 2.2 missing parent → "(原消息已删除)" fallback 字面 | vitest | `::TestCV13_MissingFallback` PASS |
| 2.3 collapse toggle (展开/收起) byte-identical 文案 | vitest | `::TestCV13_CollapseToggle` PASS |
| 2.4 truncate 200 chars + `…` suffix (collapsed only) | vitest | `::TestCV13_Truncate200` PASS |
| 2.5 DOM data-attr 4 锚 byte-identical | vitest | `::TestCV13_DOMAttrs` PASS |
| 2.6 不写 sessionStorage / localStorage | vitest + grep | `::TestCV13_NoStorageWrite` + grep 0 hit |
| 2.7 不 import api / fetch* | grep | `git grep 'fetch\|api' QuotedCommentBlock.tsx` 0 hit |

### §3 CV-13.3 — closure

| 验收项 | 实施方式 | 实施证据 |
|---|---|---|
| 3.1 thinking 5-pattern 反向 grep 0 hit (锁链第 9 处) | grep | cv-13*.tsx production 0 hit |
| 3.2 文案 byte-identical 4 字面 (`> ` + `@` + `收起/展开` + `(原消息已删除)`) | grep | content-lock §1 字面同步 |
| 3.3 REG-CV13-001..005 5 行 🟢 | regression-registry.md | 5 行 |
| 3.4 PROGRESS [x] 第 X 项 | PROGRESS.md | changelog 加行 |
| 3.5 acceptance template ✅ closed | 本文件 | 关闭区块加日期 |

## 边界

- CV-7 #535 ArtifactCommentItem (props 加 quotedMessage 续) / CV-8 #441 reply_to_id 列 / CV-9..12 / DM-5 / AP-4 / AP-5 0-server client-only 同模式 / ADM-0 §1.3 admin god-mode 红线 / thinking 5-pattern 锁链第 9 处

## 退出条件

- §1+§2+§3 全绿
- 0 server production 代码
- vitest QuotedCommentBlock 6 case PASS
- 文案 + DOM byte-identical (content-lock §1+§2)
- REG-CV13-001..005 5 行

## 关闭

✅ 2026-04-30 战马E — 0 server diff (`git diff origin/main -- packages/server-go/` 0 行) + vitest 47 files / 397 tests 全 PASS (含 QuotedCommentBlock 7 case) + typecheck 全绿; thinking 5-pattern 锁链第 9 处 (RT-3 + DM-3 + DM-4 + CV-7 + CV-8 + CV-9 + CV-11 + CV-12 + CV-13); 文案 byte-identical (`> ` + `@` + `展开/收起` + `(原消息已删除)` + `…` + 200 chars); DOM 4 锚 (`data-cv13-quoted-block` + `-author` + `-id` + `-collapsed`).
