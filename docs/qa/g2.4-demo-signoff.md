# G2.4 demo 截屏 5 张签字 — Phase 2 退出 gate (野马)

> **状态**: 🟡 **PARTIAL** (2/5 现在可签, 3/5 待依赖 — 接 PR #199 计划锚)
> **任务**: Phase 2 退出 gate G2.4 demo, 5 张关键截屏 + 野马用户感知签字
> **范围**: 复用 cm-onboarding e2e harness + INFRA-2 stopwatch fixture; spec 落 `packages/e2e/tests/g2.4-demo-screenshots.spec.ts`; 截屏落 `docs/qa/screenshots/g2.4-{1..5}.png`
> **方法**: AI 团队无 GUI sandbox — 静态 spec 编写 + 文案立场反查; 实际 PNG 由 CI Playwright 跑后归档 (与 CM-4 闸 4 签字同模式)。

---

## 1. 签字状态 (5 张)

| # | 内容 | 立场锚 | 实施状态 | 当前签 |
|---|------|--------|---------|--------|
| 1 | Welcome 第一眼非空屏 | §1.4 + onboarding §3 步骤 2 + README §核心 11 | ✅ spec 落 `g2.4-demo-screenshots.spec.ts:#1`, CI 跑后 PNG 归档 | ✅ pass |
| 2 | 左栏团队感知 (sidebar agent + subject) | §1.2 agent=同事 + onboarding §3 步骤 5 + AL-1b | 🟡 spec `.skip` 占位 — 等 AL-1b (subject "正在熟悉环境" + 分组 header "你的团队") 落地接入 | ⏳ pending AL-1b |
| 3 | Agent invitation inbox name 渲染 | §1.1 永不暴露 UUID + bug-029 已 merge | 🟡 spec `.skip` — 等 e2e fixture 加 agent 创建 + 邀请 happy path | ⏳ pending e2e fixture 扩展 |
| 4 | Quick action 错误态 (沉默胜于假 loading) | README §核心 11 + cm-4-yema-signoff ② | 🟡 spec `.skip` — 等 `page.route()` 409 mock 模式接入 | ⏳ pending mock infra |
| 5 | System message + CTA button (步骤 2 message kind) | §1.4 + onboarding §3 步骤 2 | ✅ spec `:#5` 局部截屏 (message bubble + button), 文案 `创建 agent` 字面锁 | ✅ pass |

**总体**: **2/5 ✅** (第一眼非空屏 + CTA button 局部) → 🟡 **partial 不签 Phase 2 退出 gate**, 等 #2/#3/#4 三依赖到位后补签。

---

## 2. 立场反查 (静态)

> AI 团队无 GUI sandbox, 不录视频 — 改静态 spec 文案锁 + 触发条件反查 (与 CM-4 闸 4 同模式)。

| 反查项 | 立场锚 | spec 锁定 | 结果 |
|--------|--------|----------|------|
| #1 系统消息含 "欢迎来到 Borgee" | onboarding §3 步骤 2 success 文案锁 | `expect(.message-system-content).toContainText('欢迎来到 Borgee')` | ✅ |
| #1 反约束 §11 旧空态文案不在 | README §核心 11 反约束硬条件 | `expect(getByText('👈 选择一个频道开始聊天')).toHaveCount(0)` | ✅ |
| #5 quick action button 文案 = "创建 agent" | onboarding §3 步骤 2 button 字面锁 | `expect(button.message-system-quick-action).toHaveText('创建 agent')` | ✅ |
| #5 CTA 点击行为 = open AgentManager | onboarding §3 步骤 3 CTA 跳 AgentManager | spec 已 cover (cm-onboarding.spec.ts:102-105 同 fixture) | ✅ (cm-onboarding 重复, 不展开) |

---

## 3. 留账 (3/5 待补)

| # | 阻塞 | 解锁条件 | 预估 |
|---|------|---------|------|
| 2 | AL-1b 未启动 (Phase 4 BPP 同期) | AL-1b 落 sidebar 分组 header "你的团队" + agent default subject "正在熟悉环境" | Phase 4 BPP merge 后 |
| 3 | e2e fixture 缺 agent 创建 + 邀请 happy path | 烈马 RT-0 第一批 spec 顺手补 (PR #199 g2.4-screenshot-plan §5 已挂烈马) | RT-0 PR 后 1-2 天 |
| 4 | `page.route()` 409 mock 模式未接入 | 烈马 RT-0 第一批 spec 顺手 (同上) | RT-0 PR 后 |

---

## 4. 截屏归档 (CI Playwright 后置)

- 路径: `docs/qa/screenshots/g2.4-{1..5}.png` (#1 / #5 现在 CI 一跑就出, #2 / #3 / #4 spec `.skip` 占位待解锁)
- `blueprint-sha.txt` 同目录: 记 CI 跑时 main HEAD (execution-plan §闸 4 防漂移)
- spec 用 `await page.screenshot({ path, fullPage })` 主动调用, 不依赖 `screenshot: 'only-on-failure'`

---

## 5. 解封路径

- **当前**: 2/5 签 → Phase 2 退出 gate **暂搁** (野马签字未全)
- **解封 1**: AL-1b merged → 解 #2 → 3/5 签
- **解封 2**: 烈马 RT-0 第一批 spec 含 agent fixture + 409 mock → 解 #3/#4 → 5/5 签 → ✅ Phase 2 退出 gate G2.4 关闭
- **联动**: ADM-0.3 merged 后, 加 ADM-0 立场 demo 第 6 张 (admin 不入 channel 验证) — 跟 #205 反查 ① 联动, 一气出 (建军 R3 派活)

---

## 6. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-28 | 野马 | v0 partial: 2/5 签 (#1 + #5 spec 落地), 3/5 留账等依赖 |
