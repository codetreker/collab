# G2.4 截屏 5 张计划 — Phase 2 退出闸 4 必备

> **状态**: v0 草稿 (野马 + 烈马, 2026-04-28)
> **依赖**: INFRA-2 ✅ merged (Playwright + testutil 桥就位)
> **解封**: Phase 2 退出闸 4 (G2.4 用户感知签字, 野马硬条件)
> **关联**: `execution-plan.md` G2.4 (5 张关键截屏), `00-foundation/onboarding-journey.md` §6 (步骤 2 + 5 截屏挂钩), 14 立场 §1.4 (第一眼) + §1.2 (agent=同事) + §1.1 (UI 永不暴露 org_id) + README §核心 11 (沉默胜于假 loading)。
> **数学一致**: execution-plan G2.4 锁 5 张; onboarding-journey §6 第 5 张同盖步骤 2+5 → 本 doc 拆 5 个独立触发, 第 1 张 (welcome 第一眼) ≡ onboarding 步骤 2; 第 2 张 (左栏团队感知) ≡ onboarding 步骤 5。

---

## 1. 5 张截屏 — 文案锁 + 触发条件

| # | 内容 | 立场锚 | 触发条件 (Playwright) | 文案锁 (一字不漏) |
|---|------|--------|----------------------|-------------------|
| 1 | **Welcome 第一眼非空屏** (注册后落点) | §1.4 第一眼 + onboarding-journey §3 步骤 2 | 注册新业主 → URL `/c/<welcomeChannelId>` → DOM 含 system message + 不含 "👈 选择频道" | system message subject: "欢迎来到 Borgee — 你的协作场已就位"; CTA button: "创建你的第一个 agent" |
| 2 | **左栏团队感知** (sidebar agents + subject) | §1.2 agent=同事 + §1.4 主体验 + onboarding-journey §3 步骤 5 | 业主创建 1 agent → sidebar 左栏 DOM 含 agent name + subject 文案 | agent subject 默认: "正在熟悉环境"; 列表分组 header: "你的团队" (不是 "Agents") |
| 3 | **Agent invitation inbox** (name 渲染) | §1.1 永不暴露 UUID + §1.2 agent=同事 + bug-029 fix | 邀请 1 agent 加入 #design → inbox 渲染 → DOM `agent_name` / `channel_name` 在场, 无原始 UUID 字面 | "邀请你的 agent **助手** 加入 channel **#design**"; raw UUID 仅 `title=` hover (a11y) |
| 4 | **Quick action 错误态** (沉默胜于假 loading) | README §核心 11 + cm-4-yema-signoff.md ② | 接受邀请 → mock 409 → DOM 显示具名错误, 无空白 | "该邀请已被处理或状态已变更, 请刷新" (409); 其他 err: "操作失败: <msg>" (不沉默) |
| 5 | **System message + CTA button** (onboarding 步骤 2 message kind 扩展) | §1.4 第一眼 + onboarding-journey §3 步骤 2 + execution-plan G2.4 | 同 #1, 重点截 message bubble + button → 点击跳 AgentManager | message body 含: "你的协作场已就位"; button 文案: "创建你的第一个 agent" → click → URL `/agents/new` |

> **5 张数学**: #1 (welcome 全屏) + #5 (welcome message 局部 + button) 都是步骤 2 的截屏 — #1 验"非空屏"立场, #5 验"CTA + message kind 扩展"实施, 不重复。

---

## 2. Stopwatch ≤3s 硬条件 (RT-0 接口, 烈马 owner)

> **野马 R3 硬条件**: G2.4 demo 必含 stopwatch ≤3s 证据 (邀请发出 → 业主 inbox 收到), 不达 3s 一票否决。

| 接口点 | 实施 | Owner |
|--------|------|-------|
| Playwright fixture: `await stopwatch(async () => { ... })` 返回 ms | `packages/client/e2e/fixtures/stopwatch.ts` | 烈马 |
| 触发: `POST /api/agent-invitations` (邀请方) → SSE/WS push → inbox DOM `[data-testid=invitation-row]` count +1 | E2E spec | 烈马 (RT-0 第一批 spec) |
| 断言: `expect(elapsedMs).toBeLessThanOrEqual(3000)` | E2E spec | 烈马 |
| Trace artifact 上传: 失败时 CI 留 trace + screenshot (INFRA-2 已配) | CI workflow | 飞马 |

---

## 3. 截屏存放 + 命名

- 路径: `docs/evidence/g2.4/<n>-<slug>.png` (1-5)
- 同目录 `blueprint-sha.txt` 记当时 main HEAD (execution-plan §闸 4 防漂移)
- Playwright 配置: `screenshot: 'only-on-failure'` 不够 — 本 5 张需主动 `await page.screenshot({ path: ... })` 在 spec 内显式调用

---

## 4. 反约束 (不挡 v0)

- ❌ 多浏览器 matrix (chromium 单跑即可, 与 INFRA-2 §不在范围一致)
- ❌ Visual regression diff (人眼比对, v1 再上)
- ❌ 截屏内容真实业务数据 — fixture seed 固定 ("助手" / "#design" / "野马" 业主), 跨次稳定可复现
- ❌ stopwatch 同时验"非首次"(冷启动) — 仅验热路径, 冷启动留 RT-0 各自 spec

---

## 5. 谁干啥 (≤2h 工作量)

| 任务 | Owner | 产出 |
|------|-------|------|
| 文案锁 + 触发条件定稿 (本 doc §1) | 野马 | 本 doc v1 (本 PR) |
| stopwatch fixture 实施 | 烈马 | `e2e/fixtures/stopwatch.ts` (RT-0 第一批 spec 顺手) |
| 5 张 spec 落地 (`g2.4-*.spec.ts`) | 烈马 | 5 个 spec 跑通 + 截屏落 `docs/evidence/g2.4/` |
| demo 跑一次 + 签字 | 野马 | `docs/qa/signoffs/g2.4-yema-signoff.md` (Phase 2 闸 4) |

---

## 6. 验收挂钩

- Phase 2 退出闸 4: 5 张截屏 + stopwatch 证据 + 野马签 → `docs/evidence/g2.4/` 全在场 → 立场反查表过 → ✅ 闸 4
- 失败回路: 任一 spec fail → CI block merge; 文案漂移 → grep `docs/evidence/g2.4/blueprint-sha.txt` 反查
- CM-onboarding / RT-0 / CM-4.3b 任一未 merge → 本计划暂搁置 (依赖未到)

---

## 7. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-28 | 野马 | v0 草稿, 5 张文案锁 + stopwatch 接口挂烈马 |
