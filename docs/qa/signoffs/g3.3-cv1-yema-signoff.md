# G3.3 CV-1 用户感知签字闸 — 野马 (PM) signoff

> **状态**: ✅ **SIGNED** (野马, 2026-04-29, post-#348 e2e merged)
> **范围**: CV-1 单 milestone signoff (Phase 3 退出闸前置, 跟 G3.4 协作场骨架 demo 拆开)
> **关联**: CV-1.1 #334 schema (artifacts + artifact_versions, v=13) + CV-1.2 #342 server (commit/rollback owner-only + cross-channel 403) + CV-1.3 #346 client SPA (ArtifactPanel + kindBadge + WS push refresh) + CV-1.3 e2e #348 (`cv-1-3-canvas.spec.ts` 真 4901+5174); REG-CV1-001..017 全 🟢 (acceptance template `cv-1.md` 三段四件全闭); 文案锁 #355 (CV-2) 立场承袭 + #347 line 251 kindBadge 二元.
> **方法**: 跟 #198 CM-4 / G2.4 / G2.5 / G2.6 demo signoff 同模式 — 静态代码审 + 文案立场反查 + e2e 实施证据 + 关键截屏路径承认 (Playwright 后置补归档).

---

## 1. 验收清单 (野马 R1 锁定 5 项, CV-1 立场反查 #282/#295/#307 + acceptance #cv-1.md 同源)

| # | 验收项 | 立场锚 | 结果 | 证据 |
|---|--------|--------|------|------|
| ① | artifact 归属 channel — channel-scoped 权限继承, 非 author 主权 | CV-1 立场 ① + canvas-vision §1.3 | ✅ pass | `migrations/cv_1_1_artifacts.go` 表 `channel_id NOT NULL` + 无 `owner_id` 主权列 (反向断言); `internal/api/artifacts.go:24` 注释锚 "无 owner_id 主权列" + cross-channel 403 反断 (`TestArtifactCrossChannel403`) |
| ② | 单文档锁 30s TTL — last-writer-wins + 409 conflict | CV-1 立场 ② + canvas-vision §1.5 | ✅ pass | `artifacts` 表 `lock_holder_user_id` + `lock_acquired_at` (CV-1.1 schema); 409 conflict toast `"内容已更新, 请刷新查看"` byte-identical (`ArtifactPanel.tsx:49` CONFLICT_TOAST line 49 count==1, REG-CV1-015) |
| ③ | 版本线性 + agent 默认无删历史权 + rollback owner-only DOM gate 三条件 | CV-1 立场 ③⑦ + canvas-vision §1.5 表 | ✅ pass | `artifact_versions` 表 PK AUTOINCREMENT + UNIQUE(artifact_id, version) 线性锁; `ArtifactPanel.tsx:254` `showRollbackBtn = isOwner && !isHead && !editing` defense-in-depth (DOM omit 不 disable, REG-CV1-014); rollback 创新 version 不删 history (跟 CV-2 #355 立场 ⑥ "已解决 anchor 不移除" 同精神) |
| ④ | committer_kind 二元 🤖↔👤 byte-identical (跨 milestone 五处单测锁源头) | CV-1 立场 ⑥ + canvas-vision §1.5 | ✅ pass | `ArtifactPanel.tsx:251` `kindBadge = committer_kind === 'agent' ? '🤖' : '👤'` byte-identical 字面 — **跨 milestone 五处单测锁源头**: CV-1 #347 + CV-2 #355 ④ + DM-2 #314 ② + CV-4 #380 ④ + CHN-4 #382 ②; 改 = 改五处 |
| ⑤ | ArtifactUpdated WS frame 7 字段 byte-identical + 实时 ≤3s + cursor 单调 | CV-1 立场 ⑤ + RT-1.1 #290 envelope 锁 | ✅ pass | `ws/cursor.go::ArtifactUpdatedFrame` 7 字段 `{Type, Cursor, ArtifactID, Version, ChannelID, UpdatedAt, Kind}` byte-identical (REG-CV1-012); BPP-1 #304 envelope CI lint reflect 自动覆盖 (跟 5-frame 共序: RT-1=7 / AnchorComment=10 / MentionPushed=8 / IterationStateChanged=9 / AL-4 复用 BPP-1 既有) |

**总体**: 5/5 通过 → ✅ **签字**, G3.3 CV-1 用户感知签字闸通过, Phase 3 退出闸前置就位.

---

## 2. 关键截屏路径承认 (CI Playwright 后置补归档, 跟 G2.4 #275 同模式)

3 张截屏归档路径锁 byte-identical 跟 `docs/qa/screenshots/README.md` §0 同源:

| 截屏 | 验内容 | 立场锚 |
|------|--------|--------|
| `g3.3-cv1-markdown-render.png` | markdown artifact 渲染 + `data-artifact-id` + kindBadge 二元 (v1 owner 创建) | CV-1 立场 ④⑥ |
| `g3.3-cv1-commit-dropdown.png` | version dropdown v1/v2 + agent commit fanout `{agent_name} 更新 {artifact_name} v{n}` (artifacts.go:591 byte-identical) | CV-1 立场 ⑤ + REG-CV1-013 |
| `g3.3-cv1-rollback-flow.png` | rollback DOM gate 三条件 + 409 conflict toast + version label "v3 (rollback from v1)" 字面 | CV-1 立场 ⑦ + REG-CV1-014/015 |

**截屏机制**: CV-1.3 e2e #348 `cv-1-3-canvas.spec.ts` 已闭环真 4901+5174 路径, 后置 follow-up PR 加 `page.screenshot({path: 'docs/qa/screenshots/g3.3-cv1-*.png'})` 入 git (反 PS 修改, 跟 G2.4 #275 同模式).

---

## 3. 跨 milestone byte-identical 链 (此 signoff 强化的源头)

CV-1 是 Phase 3 协作场基础, 多个 byte-identical 锁字面**源头在此**:

- **kindBadge 二元** 🤖↔👤 — `ArtifactPanel.tsx:251` 是源头, **五处单测锁** 链 (CV-2 #355/#358 + DM-2 #314 + CV-4 #380 + CHN-4 #382 引)
- **CONFLICT_TOAST** `"内容已更新, 请刷新查看"` — `ArtifactPanel.tsx:49` 是源头 (REG-CV1-015 + 拖拽 toast `"侧栏顺序保存失败, 请重试"` 同精神三处单测锁模式参考)
- **fanout 文案** `{agent_name} 更新 {artifact_name} v{n}` — `artifacts.go:591` 是源头 (CV-4 #380 ④ + CHN-4 #382 ② + onboarding-journey #391 §7.x byte-identical 引)
- **rollback DOM gate** 三条件 (`isOwner && !isHead && !editing`) — `ArtifactPanel.tsx:254` 是源头, defense-in-depth 模式 (CV-4 #380 ⑥ iterate 按钮 + AL-4 启停按钮 + CV-2 #355 ⑤ anchor 创建按钮 byte-identical 模式承袭)
- **ArtifactUpdated frame 7 字段** — `cursor.go::ArtifactUpdatedFrame` 是 BPP-1 #304 envelope CI lint 5-frame 共序锁的第一个 (RT-1=7 头位锁, AnchorComment/MentionPushed/IterationStateChanged 全 byte-identical 跟此源)

**改 CV-1 字面 = 改跨 milestone 多源** — 任何 CV-1 字面变动需走 stance sweep 模式 (跟 #339 / #336 / #393 同模式 follow-up patch 链)。

---

## 4. 留账 (v1 后续, 不阻 G3.3 签字)

- ⏸️ G3.3 demo 截屏归档 — 待 follow-up PR Playwright `page.screenshot()` 入 git (跟 G2.4 #275 截屏后置补同模式, 不阻签字)
- ⏸️ owner grant agent 删历史权 UI — CV-1 立场 ③ "agent 默认无删历史" 已锁, owner grant UI 留 v3+ (跟 ADM-2 #266 audit_log schema 配套时机)
- ⏸️ 锚点对话挂 markdown artifact (CV-2 主线) — G3.4 退出闸 demo 路径之一, 跟 G3.3 拆开 (G3.3 仅 CV-1 single milestone)

---

## 5. 解封路径

- ✅ G3.3 签字 → Phase 3 退出闸前置 (CV-1 single milestone signoff, 5/5 验收 + 跨 milestone 锁字面源头)
- ⏸️ G3.4 协作场骨架 demo 退出闸 (跟 #382 文案锁 ⑥ + #391 §1 截屏路径锁同源, 战马 e2e + 烈马 acceptance + 野马双 tab 截屏文案锁验**三签**)
- ⏸️ Phase 3 closure announcement PR (飞马职责) — G3.3 + G3.4 双签后链入

---

## 6. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-29 | 野马 | v0 — G3.3 CV-1 用户感知签字闸 ✅ SIGNED post #348 e2e merged. 5/5 验收通过 (artifact 归属 channel / 单文档锁 30s + 409 conflict / 版本线性 + rollback DOM gate / kindBadge 二元 / ArtifactUpdated frame 7 字段) + 关键截屏 3 张路径承认 (跟 #391 §0 byte-identical 同源) + 跨 milestone byte-identical 链锁字面源头 (kindBadge 五处 / CONFLICT_TOAST / fanout / rollback gate / frame 7 字段). 跟 #198 CM-4 / G2.4 / G2.5 / G2.6 demo signoff 同模式. |
