# G4.1 ADM-1 用户感知签字闸 — 野马 (PM) signoff

> **状态**: ✅ **SIGNED** (野马, 2026-04-29, post-#459 e2e merged + #483 commits f9ff042/6bc7c04/600071f)
> **范围**: ADM-1 用户隐私承诺页 + 隐私表 (Phase 4 启动 milestone signoff, 跟 G3.3 #403 CV-1 同模式)
> **关联**: ADM-1 PrivacyPromise + SettingsPage 实施 (#483 PR open) + e2e g4.1-adm1-{privacy-promise,privacy-table}.png 双截屏 follow-up #459 / 蓝图 `admin-model.md` §0 (强权但不窥视) + §4.1 (用户侧隐私承诺页文案 R3 锁定 3 条字面) + §13 (野马硬条件); 跟 G3.3 #403 / CM-4 #198 / G2.4 / G2.5 / G2.6 demo signoff 同模式.
> **方法**: 静态代码审 + 蓝图 §4.1 字面 byte-identical 反查 + 关键截屏路径承认 + e2e PrivacyPromise.test.tsx + drift test 兜底.

---

## 1. 验收清单 (野马 R1 锁定 4 项, 蓝图 §4.1 R3 锁定 3 条字面 + §13 野马硬条件)

| # | 验收项 | 立场锚 | 结果 | 证据 |
|---|--------|--------|------|------|
| ① | **3 条隐私承诺字面 byte-identical** (一字不漏 / 顺序不变, §4.1 R3 锁定) | admin-model §4.1 + §0 强权但不窥视 | ✅ pass | `packages/client/src/components/Settings/PrivacyPromise.tsx:26-28` 三条字面 byte-identical 跟蓝图 §4.1 同源:<br>• line 26: `**Admin 是平台运维, 不是协作者** — 永不出现在 channel / DM / 团队列表里。`<br>• line 27: `**Admin 看不到消息 / 文件 / artifact 内容** — 除非你主动授权 impersonate (24h 时窗, 顶部红色横幅常驻, 可随时撤销)。`<br>• line 28: `**Admin 能看的是元数据** (用户名 / channel 名 / 条数 / 登录时间), **看不到正文**。` |
| ② | 隐私表三色锁 — `allow=gray default / deny=#d33 加粗 / impersonate=#d97706 amber` 三态 byte-identical (跟 AL-3 #305 dot 颜色锁同精神, 视觉清晰) | admin-model §0 强权但不窥视 + UX 清晰 | ✅ pass | `PrivacyPromise.tsx:33` "三色锁" 注释锚 + `PrivacyRowKind = 'allow' \| 'deny' \| 'impersonate'` 二元拆死 (line 47); `g4.1-adm1-privacy-table.png` 视觉锚 |
| ③ | impersonate 临时态 (24h 红色横幅常驻 + 可撤销) — `**impersonate**` UI 黄底显式 (`#d97706 amber`) 跟 admin 默认 deny 拆死 (反 admin god-mode 漂入) | admin-model §0 + §4.1 + ADM-0 §1.3 红线 | ✅ pass | `PrivacyPromise.tsx:44` `'授权 impersonate 后 24h 实时入站'` row + `mark: '✅ (临时)'` + `kind: 'impersonate'` byte-identical; 跟 AL-4 #387/#461 admin 元数据 only 同模式 |
| ④ | 双截屏归档 + Playwright 主动 `page.screenshot()` 入 git (反 PS 修改, 跟 G3.3 #403 / G2.4 #275 同模式) | demo signoff 联签 | ✅ pass | `docs/qa/screenshots/g4.1-adm1-privacy-promise.png` + `g4.1-adm1-privacy-table.png` 双截屏 #459 PR 已 merge + 双 e2e PrivacyPromise.test.tsx + drift test 兜底 |

**总体**: 4/4 通过 → ✅ **签字**, G4.1 ADM-1 用户感知签字闸通过, Phase 4 启动 milestone signoff 路径就位.

---

## 2. 关键截屏路径承认 (CI Playwright 主动归档, 跟 G3.3 #403 + G2.4 #275 同模式)

2 张截屏归档路径锁 byte-identical 跟 `docs/qa/screenshots/README.md` 同源:

| 截屏 | 验内容 | 立场锚 |
|------|--------|--------|
| `g4.1-adm1-privacy-promise.png` | 设置页 3 条承诺字面 byte-identical (一字不漏 / 顺序不变 §4.1 R3) | admin-model §4.1 |
| `g4.1-adm1-privacy-table.png` | 隐私表三色锁 (allow gray / deny #d33 / impersonate #d97706 amber) + admin 默认 deny 永久 | admin-model §0 + ADM-0 §1.3 |

**截屏机制**: `#459 e2e privacy promise + G4.1 双截屏 follow-up` 已闭环 (post-merge), Playwright `page.screenshot({path: 'docs/qa/screenshots/g4.1-adm1-*.png'})` 入 git (反 PS 修改, 跟 G2.4 #275 同模式).

---

## 3. 跨 milestone byte-identical 链 (此 signoff 强化)

ADM-1 是 Phase 4 启动 milestone, "强权但不窥视" 立场跟跨 milestone admin 元数据 only **四源同模式** byte-identical 同源:

- **admin god-mode 元数据 only 四源同模式**:
  - ADM-0 §1.3 红线 (admin 不入业务路径)
  - AL-3 #303 ⑦ god-mode 字段白名单
  - AL-4 #379 v2 §3 grep `last_error_reason.*admin` 0 hit
  - AL-2a #454 ④ "PATCH owner-only + admin god-mode 字段白名单不返 blob"
  - **ADM-1 (本 signoff)** 隐私表 admin 默认 deny 三态锁

- **agent silent default 多源** (跟 ADM-1 隐私承诺 ① "Admin 永不出现在 channel/DM/团队列表" 同精神):
  - 蓝图 #11 沉默胜于假活物感
  - AL-3 #305 ③ agent join silent
  - AL-2a #454 ⑦ + AL-1b #458 ⑦
  - **ADM-1 (本 signoff)** "强权但不窥视" 立场源头

- **三色锁 UX 模式** (跟 AL-1b #458 dot 颜色锁同精神 — 视觉清晰):
  - AL-3 #305 4 色 dot (online/offline/error/busy)
  - AL-1b #458 5 色 dot (busy 🟡 / idle ⚪ / online 🟢 / offline ⚫ / error 🔴)
  - **ADM-1 (本 signoff)** 3 色 (allow gray / deny #d33 / impersonate amber)

**改 ADM-1 字面 = 改跨 milestone 多源** — 任何 ADM-1 字面变动需走 stance sweep 模式 (跟 #339 / #336 / #393 / #461 / #467 同模式 follow-up patch 链).

---

## 4. 留账 (v1 后续, 不阻 G4.1 签字)

- ⏸️ impersonate 实施 (24h 时窗 + 顶部红色横幅常驻 + 撤销) — ADM-1.x 后续段, 跟 ADM-2 audit log 配套时机
- ⏸️ ADM-2 admin SPA system DM 5 模板 (#484 待战马D 实施 .2/.3 后翻牌)
- ⏸️ admin 写动作 audit_log (跟 ADM-2 #266 schema 同源)
- ⏸️ G4.audit 飞马 v1 fill (跟 G3.audit 同模式收口)

---

## 5. 解封路径

- ✅ G4.1 签字 → Phase 4 起步 milestone signoff (ADM-1 用户感知签字闸 4/4 通过 + 跨 milestone 立场承袭 byte-identical)
- ⏸️ G4.audit 飞马收口 (Phase 4 退出闸前置, 跟 G3.audit #443 skeleton 同模式)
- ⏸️ Phase 4 closure announcement PR (飞马职责) — G4.x signoff + audit 双签后链入

---

## 6. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-29 | 野马 | v0 — G4.1 ADM-1 用户感知签字闸 ✅ SIGNED post-#459 e2e merged + #483 commits f9ff042/6bc7c04/600071f. 4/4 验收通过 (3 条隐私承诺字面 byte-identical 跟蓝图 §4.1 R3 同源 / 隐私表三色锁 / impersonate 临时态 amber + admin 默认 deny / 双截屏归档 Playwright `page.screenshot()` 入 git) + 关键截屏 2 张路径承认 + 跨 milestone byte-identical 链锁字面 (admin god-mode 元数据 only 四源 + agent silent default 多源 + 三色锁 UX 模式 跟 AL-1b dot 颜色锁同精神). 跟 G3.3 #403 / CM-4 #198 / G2.4 / G2.5 / G2.6 demo signoff 同模式. |
