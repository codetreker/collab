# G3.2 CV-2 锚点对话 E2E — 烈马 (QA) signoff

> **状态**: ✅ **SIGNED** (烈马, 2026-04-29, post-#359/#360/#404/#421 全 merged)
> **范围**: G3.2 闸 — CV-2 锚点对话三段四件全闭 + 4 文案字面 byte-identical 跟 #355 同源
> **关联**: CV-2.1 #359 (artifact_anchors + anchor_comments schema v=14) + CV-2.2 #360 (server REST 4 endpoints + AnchorCommentAdded WS frame 10 字段) + CV-2.3 #404 (client SPA 选区→锚点 entry + thread side panel + WS push 实时刷) + REG-CV2 add 5🟢 #421; evidence bundle #442 §2; 蓝图 canvas-vision §1.6 (锚点 = 人审 agent 产物); 文案锁 #355 (4 字面 byte-identical 跟 G3.4 demo 截屏锚 #391 §1 同源)
> **方法**: 跟 #403 G3.3 + g3.1 signoff 同模式 — 真 e2e 实施证据 + 立场反查 + 反约束三连 (agent 不能开 thread / cross-anchor 同 403 / version-pin immutable)

---

## 1. 验收清单 (烈马 acceptance 视角 5 项, 跟 #442 §2 evidence bundle byte-identical)

| # | 验收项 | 立场锚 | 结果 | 实施证据 (PR/SHA + 测试名 byte-identical) |
|---|--------|--------|------|------|
| ① | artifact_anchors / anchor_comments schema 双表 + range CHECK end>=start + immutable artifact_version_id (立场 ② version-pin) | CV-2.1 #359 立场 ②③ + canvas-vision §1.6 | ✅ pass | `internal/migrations/cv_2_1_anchor_comments_test.go` 8 PASS (REG-CV2-001): `TestCV21_RejectsInvalidEndOffsetRange` + `TestCV21_RejectsInvalidAuthorKind` (CHECK ('agent','human')) + `TestCV21_AnchorsAcrossVersionsCoexist` (跨版本不自动迁移); 反约束 anchors 表无 kind/cursor 列 + anchor_comments 无 committer_kind 列 (author_kind 命名拆 commit 之 committer_kind, anchor 是评论作者非提交者) |
| ② | server 4 endpoints — POST 创锚 owner-only / GET list active / POST comments reply / POST resolve owner-or-creator + cross-channel 403 + agent kind=='agent' POST → 403 `anchor.create_owner_only` | CV-2.2 #360 立场 ①⑦ + #355 反约束三连 | ✅ pass | `internal/api/cv_2_2_anchors_test.go` 8 PASS (REG-CV2-002): `TestCV22_AgentCannotCreateAnchor` (反断 403 错码 byte-identical) + `TestCV22_AnchorPinnedToVersion_Immutable` + `TestCV22_CrossChannel403` + `TestCV22_ResolveOwnerOrCreator` (idempotent re-resolve) + `TestCV22_AgentCanReplyAfterHumanCreate` (反约束 agent-only thread 反, 蓝图 §1.6 字面禁 AI 自循环) |
| ③ | AnchorCommentAdded WS frame 10 字段 byte-identical + cursor 共序 (跟 RT-1 7 / DM-2 8 / CV-4 9 同 hub.cursors 单调 sequence) | CV-2.2 立场 ③ + RT-1.1 #290 envelope 锁 + BPP-1 #304 lint | ✅ pass | `internal/ws/anchor_comment_frame_test.go::TestAnchorCommentAddedFrameFieldOrder` (REG-CV2-003) — JSON byte-equality pin 10 字段 `{type, cursor, anchor_id, comment_id, artifact_id, artifact_version_id, channel_id, author_id, author_kind, created_at}`; spec drift 修 #368 v3 (烈马 #360 review 抓 9→10 字段 envelope 修); BPP-1 #304 envelope CI lint reflect 自动覆盖 |
| ④ | client 4 文案字面 byte-identical 跟 #355 文案锁 ①②③④ 同源 | CV-2.3 #404 立场 + #355 文案锁 | ✅ pass | `packages/e2e/tests/cv-2-3-anchor-client.spec.ts::§3.1` 选区→锚点 entry button + tooltip `"评论此段"` byte-identical (#355 ①); `§3.2` thread panel literals 4 字面: `"段落讨论"` (#355 ② header) + `"针对此段写下你的 review…"` (#355 ③ placeholder) + `"标为已解决"` (#355 ⑥ resolve button) + `🤖 reply` author_kind="agent" (#355 ④ kindBadge byte-identical 跟 CV-1 #347 line 251 同源 — 跨 milestone 五处单测锁链) |
| ⑤ | client WS push 实时 ≤3s + 反约束 agent 视角 DOM 无入口 (count==0, defense-in-depth) | CV-2.3 #404 立场 + #355 反约束三连 | ✅ pass | `cv-2-3-anchor-client.spec.ts::§3.5` WS push 实时 ≤3s 真 server-go(4901)+vite(5174) PASS; `§3.6` 反约束 agent 视角 DOM 无 entry hover button (count==0 反查) — defense-in-depth 模式跟 CV-1 #347 line 254 showRollbackBtn DOM gate 三条件同精神 (server #360 anchor.create_owner_only 兜底 + client DOM omit 双层) |

**总体**: 5/5 通过 → ✅ **签字**, G3.2 CV-2 锚点对话 E2E 闸通过.

---

## 2. 反约束三连验 (跟 #355 文案锁 ⑤ byte-identical 同源)

CV-2 立场 ⑤ 反约束三连永久锁:

- (a) **client DOM agent 视角无 ① hover 入口** ✅ — `cv-2-3-anchor-client.spec.ts::§3.6` agent 视角 DOM `[data-anchor-id]` count==0
- (b) **server agent role POST `/api/v1/artifacts/:id/anchors` → 403 + 错码 `anchor.create_owner_only`** ✅ — `TestCV22_AgentCannotCreateAnchor` (REG-CV2-002)
- (c) **cross-anchor agent→agent 同 403** ✅ — agent 必须落在 thread 已含 author_kind='human' 的锚点; agent-only thread 接龙 → 同 403 (蓝图 §1.6 字面禁 AI 自循环)

跟 CV-1 #347 立场 ③ "agent 默认无删历史权" 同精神, 锚点也是 review 历史保留 (resolved 锚点 DOM `data-resolved="true"` 不从 DOM 移除).

---

## 3. AnchorCommentAdded 10 字段 byte-identical drift 修

烈马 #360 review 抓 envelope 9 vs 10 字段 spec ↔ 实施 drift, 飞马 #368 v3 spec 修齐 — 当前 spec ↔ schema (#359) ↔ frame (#360) 三源 byte-identical 闭环:

| 源 | 字面 |
|---|---|
| spec v3 立场 ③ #368 | 10 字段 ✅ |
| #359 schema | artifact_anchors.artifact_version_id INTEGER NOT NULL ✅ |
| #360 frame | AnchorCommentAddedFrame.ArtifactVersionID int64 ✅ |

`artifact_version_id` 选 schema FK 列名 (跟 anchor 钉死 PK 立场 ② 字面) 而非 ArtifactUpdated 的 `version` 用户号 — 反约束跟 commit committer_kind 拆开同精神.

---

## 4. 留账 (v1 后续, 不阻 G3.2 签字)

- ⏸️ G3.2 demo 截屏归档 — 走 G3.4 demo bundle (跟 #391 §1 路径锁 byte-identical, 5 张 g3.4-cv2-* 待 page.screenshot 入 git)
- ⏸️ 锚点跨版本自动迁移 — CV-2 反约束 v1 不做 (留 v3+, 真做要解决 diff/范围漂移); 当前 stale 标签 byte-identical `"锚点指向 v{N}, 文档已更新到 v{M}"` 跟 #364 patch + #358 §3.4 同源
- ⏸️ code/image artifact 加锚 — CV-2 §4 反约束 v3+, server 前置 `anchor.unsupported_artifact_kind` 403 已落 (CV-3 #363 立场 ① + #396 schema 协作)

---

## 5. 解封路径

- ✅ G3.2 签字 → Phase 3 退出闸第二闸闭 (CV-2 实施 100% 闭环 + 文案锁 4 字面 byte-identical + 反约束三连永久锁)
- ⏸️ G3.4 协作场骨架 demo 退出闸 (跟 G3.2 demo 截屏路径联动, 5 张 g3.4-cv2-* 截屏入 git 后 G3.4 三签可补野马签)
- ⏸️ G3.audit 飞马留账行 (软 gate)

---

## 6. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-29 | 烈马 | v0 — G3.2 CV-2 锚点对话 E2E 闸 ✅ SIGNED post #359/#360/#404/#421 全 merged. 5/5 验收通过 (schema 双表 + range CHECK + version-pin immutable / server 4 endpoints + 反约束三连 / AnchorCommentAdded 10 字段 byte-identical / client 4 文案字面 byte-identical 跟 #355 / WS push ≤3s + DOM defense-in-depth). 跟 #403 G3.3 + g3.1 signoff 同模式 + #442 §2 evidence bundle byte-identical. 反约束三连永久锁 + spec ↔ schema ↔ frame 三源 byte-identical 闭环. |
