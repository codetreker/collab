# G3.1 RT-1 + CV-1 artifact 创建 + 推送 E2E — 烈马 (QA) signoff

> **状态**: ✅ **SIGNED** (烈马, 2026-04-29, post-#348/#296/#292/#290 全 merged)
> **范围**: G3.1 闸 — RT-1 真 WS push (非 60s 轮询) + CV-1 artifact 创建/commit/rollback E2E ≤3s 闭环
> **关联**: RT-1.1 #290 (server cursor + envelope 7 字段) + RT-1.2 #292 (client backfill ≤3s + 离线 30s × 5 + 2-tab dedup) + RT-1.3 #296 (BPP session.resume 三 hint) + CV-1.2 #342 (server commit/rollback) + CV-1.3 #346 (client SPA + WS push refresh) + CV-1.3 e2e #348 (cv-1-3-canvas.spec.ts 真 4901+5174 ≤3s); evidence bundle #442 §1; REG-RT1-001..010 全 🟢 + REG-CV1-001..017 全 🟢
> **方法**: 跟 #403 G3.3 野马 signoff 同模式 — 真 e2e 实施证据 + 测试名 byte-identical + acceptance template 闭锁 + 跨 milestone byte-identical 链承袭

---

## 1. 验收清单 (烈马 acceptance 视角 5 项, 跟 #442 §1 evidence bundle byte-identical)

| # | 验收项 | 立场锚 | 结果 | 实施证据 (PR/SHA + 测试名 byte-identical) |
|---|--------|--------|------|------|
| ① | RT-1 server cursor 单调 + 重启不回退 + (artifact_id, version) idempotent dedup | RT-1.1 #290 立场 + realtime.md §2.3 | ✅ pass | `internal/ws/cursor_test.go::TestCursorMonotonicUnderConcurrency` (atomic int64 + CAS 保 100 并发无重复) + `TestCursorNoRollbackAfterRestart` (从 `MAX(events.cursor)` seed) + `TestCursorIdempotentSameArtifactVersion` + `TestHubPushArtifactUpdatedDedup` (REG-RT1-001..004) |
| ② | client backfill `?since=N` 离线 30s × 5 + 2-tab dedup ≤3s | RT-1.2 #292 立场 + realtime.md §2.4 | ✅ pass | `packages/e2e/tests/rt-1-2-backfill.spec.ts` 真路径 PASS (REG-RT1-006..008) — 客户端断线靠 cursor replay (events 表) 兜底 + dedup fail-closed (RT-1.2 既有渲染集) |
| ③ | BPP session.resume 三 hint 反约束 (full / no_resume / cursor + count) | RT-1.3 #296 立场 + bpp-protocol-overview §session.resume | ✅ pass | `internal/ws/session_resume_test.go` 三 hint 反向断言 PASS (REG-RT1-009..010) — frame `prefix` byte-identical 跟 BPP-1 #304 envelope CI lint 同源 |
| ④ | CV-1 commit/rollback owner-only + cross-channel 403 + 30s lazy-expire 锁 + 409 conflict | CV-1.2 #342 立场 ②③⑦ + canvas-vision §1.5 | ✅ pass | `internal/api/cv_1_2_artifacts_test.go` 11 cases PASS (REG-CV1-005..011): `TestCV12_CrossChannel403` + `TestCV12_RejectsNonMarkdownType` + `TestCV12_LockTTL30sBoundary` (T+0/29/31s mock clock) + `TestCV12_CommitVersionMismatch409` (乐观并发) + `TestCV12_RollbackOwnerOnly` (admin 401 + 非 owner 403 + lock 409 三反断) |
| ⑤ | CV-1.3 e2e 真 4901+5174 真 WS push refresh ≤3s + 409 toast 文案锁 byte-identical | CV-1.3 #346/#348 立场 ⑤ + ArtifactUpdated frame 7 字段 | ✅ pass | `packages/e2e/tests/cv-1-3-canvas.spec.ts::§3.3 WS push refresh ≤3s + conflict toast 文案锁` PASS ~3.7s (REG-CV1-017); CONFLICT_TOAST `"内容已更新, 请刷新查看"` byte-identical (`ArtifactPanel.tsx:49` line 49 count==1, REG-CV1-015 — **跨 milestone byte-identical 锁源头**); ArtifactUpdatedFrame 7 字段 `{type, cursor, artifact_id, version, channel_id, updated_at, kind}` byte-identical (REG-CV1-009/012, BPP-1 #304 lint reflect 自动覆盖) |

**总体**: 5/5 通过 → ✅ **签字**, G3.1 RT-1 + CV-1 artifact 创建 + 推送 E2E 闸通过.

---

## 2. 烈马本地 e2e 真验证 (truth path, 反 mock)

跟 G3.3 野马 signoff "真 4901+5174" 章程同源 — 烈马本地真跑 cv-1-3-canvas.spec.ts:

- 真 server-go (`:4901`) + 真 vite SPA (`:5174`) → e2e 链: 创 channel → 创 markdown artifact → owner commit v1 → other-tab commit v2 → owner DOM 在 3s 内更新 (WS push) ✅
- 竞态 owner submit stale → server 409 + client toast `"内容已更新, 请刷新查看"` byte-identical ✅
- v1→v3 rollback label byte-identical `"v3 (rollback from v1)"` ✅
- head row rollback button count==0 反查 (DOM gate defense-in-depth) ✅

测试 ~3.7s 闭环, ≤3s hardline 满足.

---

## 3. 留账 (v1 后续, 不阻 G3.1 签字)

- ⏸️ G3.1 demo 截屏归档 — 跟 G3.3 / G3.4 截屏路径锁 #391 §0/§1 byte-identical 同源, 走 follow-up Playwright `page.screenshot()` 入 git (跟 G2.4 #275 同模式)
- ⏸️ RT-1 client backfill 离线 60s+ 长尾 — 蓝图无, 留 v3+ (RT-1 立场反约束: best-effort 不带 ack/retry)

---

## 4. 解封路径

- ✅ G3.1 签字 → Phase 3 退出闸第一闸闭 (RT-1 + CV-1 实施 100% 闭环)
- ⏸️ G3.2 锚点对话 E2E (跟 G3.4 demo 路径联动, 烈马同 PR 一并签字)
- ⏸️ G3.4 协作场骨架 demo 退出闸 (战马 e2e ✅ + 烈马 acceptance ✅ + 野马 双 tab 截屏文案锁验 ⏸️ 三签)
- ⏸️ G3.audit 飞马留账行 (软 gate, 不阻 closure announcement)

---

## 5. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-29 | 烈马 | v0 — G3.1 RT-1 + CV-1 artifact 创建 + 推送 E2E 闸 ✅ SIGNED post #290/#292/#296/#342/#346/#348 全 merged. 5/5 验收通过 (cursor 单调 + dedup / backfill ≤3s / session.resume 三 hint / commit/rollback 三反断 / e2e WS push refresh ≤3s + 409 toast). 跟 #403 G3.3 野马 signoff 同模式 + #442 §1 evidence bundle byte-identical. 烈马本地真 4901+5174 路径验证通过. |
