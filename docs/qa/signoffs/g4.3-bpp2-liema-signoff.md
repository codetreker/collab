# G4.3 BPP-2 协议抽象语义层 (dispatch + task lifecycle + agent_config_update) — 烈马 (QA acceptance) signoff

> **状态**: ✅ **SIGNED** (烈马 acceptance, 2026-04-29, post-#485 merged fe0913c)
> **范围**: G4.3 闸 — BPP-2 plugin 协议抽象语义层三段全闭 (BPP-2.1 dispatch + BPP-2.2 task lifecycle + BPP-2.3 agent_config_update), 新协议一 milestone 一 PR 整闭 (跟 G4.1 ADM-1 #459 + G4.2 ADM-2 #484 同模式)
> **关联**: BPP-2 #485 整 milestone — bppEnvelopeWhitelist 9→12 扩 (AL-2b ack +1 + task_started/task_finished +2) + 7 v1 op 白名单 + ActionHandler interface seam + TaskStartedFrame 6 字段 + TaskFinishedFrame 7 字段 + AgentConfigUpdateFrame 复用 BPP-1 envelope; 战马E PM 客串 4 件套 v0 #460 land + 实施 PR #485 三段一次合; REG-BPP2-001..017 17🟢 全 active; 累计 26 unit test (10 dispatcher + 8 task lifecycle + 8 agent_config_update) + 反向 grep 7 项 count==0
> **方法**: 跟 #403 G3.3 + #449 G3.1/G3.2/G3.4 + #459 G4.1 + G4.2 烈马代签机制承袭 — 真单测实施证据 + 立场反查 + acceptance template 闭锁 + 跨 milestone byte-identical 链承袭 (reason 6 处 → 8 处单测锁链 post-BPP-2 task_failed + AL-2b ack 第 8 处加入)

---

## 1. 验收清单 (烈马 acceptance 视角 5 项, 跟 acceptance bpp-2.md §1+§2+§3 byte-identical 17 项 ✅)

| # | 验收项 | 立场锚 | 结果 | 实施证据 (PR/SHA + 测试名 byte-identical) |
|---|--------|--------|------|------|
| ① | bppEnvelopeWhitelist 9→12 扩 (BPP-1 9 + AL-2b agent_config_ack +1 + BPP-2.2 task_started + task_finished +2 = 12) + control/data 方向锁 (control=6 / data=6) — BPP-1 #304 reflect lint 自动覆盖, 改字段 = lint fail PR 卡 | bpp-2.md §1.1 + plugin-protocol.md §1.5 frame whitelist + BPP-1 #304 envelope CI lint | ✅ pass | `internal/bpp/frame_schemas_test.go::TestBPPEnvelopeFrameWhitelist` (envelope 数 12) + `TestBPPEnvelopeDirectionLock` (data 5→6 字面同步) PASS — REG-BPP2-001 |
| ② | 7 v1 op 白名单 byte-identical 跟蓝图 §1.3 v1 列表字面 (`create_artifact`/`update_artifact`/`reply_in_thread`/`mention_user`/`request_agent_join`/`read_channel_history`/`read_artifact`) + 12 反约束值全 reject (`list_users`/`delete_org`/大小写漂/空字符串/v2+ ops) + sentinel `errSemanticOpUnknown` + ActionHandler interface seam 跟 ArtifactPusher / IterationStatePusher 同模式 (bpp 包不 import internal/api 依赖反转) | bpp-2.md §1.2-§1.5 + content-lock §1 ① + §2 ⑦ + 立场 ① "Borgee 不带 runtime" | ✅ pass | `internal/bpp/dispatcher_test.go::TestBPP21_OpWhitelist` (7 op 字面对齐 + 长度 ==7) + `TestBPP21_RejectsUnknownOp` (12 反约束) + `TestBPP21_DispatchRoutesToHandler` + `TestBPP21_DispatchAllSevenOps` + `TestBPP21_RegisterHandlerRejectsUnknownOp` + `TestBPP21_RegisterHandlerIdempotent` + `TestBPP21_NoHandlerRegistered` + `TestBPP21_DispatchErrorCodeLiteral` + `TestBPP21_HandlerErrorPropagated` PASS — REG-BPP2-002..005 (5 行) |
| ③ | TaskStartedFrame 6 字段 byte-identical (type/task_id/agent_id/channel_id/subject/started_at) + TaskFinishedFrame 7 字段 byte-identical (type/task_id/agent_id/channel_id/outcome/reason/finished_at) + 3 outcome enum 严闭 + subject 必带非空 (反默认值 fallback, 蓝图 §11 文案守野马硬条件) + outcome 中间态 reject + 4 错误码字面 byte-identical (`bpp.task_subject_empty` / `bpp.task_outcome_unknown` / `bpp.task_reason_unknown` / `bpp.task_finished_no_reason`) | bpp-2.md §2.1+§2.2+§2.5 + content-lock §1 ③⑤⑥ + §2 ⑧ | ✅ pass | `task_lifecycle_test.go::TestBPP22_TaskStartedFrameFieldOrder` + `TaskFinishedFrameFieldOrder` + `ValidateTaskStarted_RejectsEmpty` (5 反约束) + `ValidateTaskFinished_AcceptsThreeOutcomes` + `ValidateTaskFinished_RejectsMiddleStates` (10 反约束) + `ErrorCodeLiteralsByteIdentical` PASS — REG-BPP2-006..009 + 011 (5 行) |
| ④ | task_failed reason 复用 AL-1a #249 6 reason byte-identical (改 = 改八处单测锁: AL-1a #249 + AL-3 #305 + CV-4 #380 + AL-2a #454 + AL-1b #458 + AL-4 #387/#461 + BPP-2 task_failed + AL-2b ack — 第 7+8 处 post-#481/#485 加入) + completed/cancelled 时 reason 必空 (反字典污染) | bpp-2.md §2.2+§2.5 + content-lock §1 ④ + 跨 milestone reason 八处单测锁链 (#748ad89 sync) | ✅ pass | `task_lifecycle_test.go::TestBPP22_ValidateTaskFinished_FailedRequiresAL1aReason` (6 reason 全过 + 4 字典外 reject + 空 reason 反断) + `_CompletedRejectsReason` (反字典污染) PASS — REG-BPP2-010 |
| ⑤ | AgentConfigUpdateFrame 4 字段 byte-identical 复用 BPP-1 #304 既有 envelope (不裂 namespace) + 6 fields 白名单 byte-identical 跟蓝图 §1.4 表字面 (name/avatar/prompt/model/capabilities/enabled) + runtime 调优字段全 reject (8 反约束值, 蓝图 §1.4 右列字面分界, 立场 ① "Borgee 不带 runtime") + ConfigRevTracker (agent_id, config_rev) 单调幂等 reload (蓝图 §1.5 字面 "幂等 reload") + config 单源 server→plugin (plugin 不上行 config) + payload 必为 JSON object + 2 错误码字面 byte-identical (`bpp.config_field_disallowed` / `bpp.config_payload_malformed`) | bpp-2.md §3.1-§3.5 + content-lock §1 ② + §2 ③⑤ + 蓝图 §1.4 SSOT + §1.5 幂等 | ✅ pass | `agent_config_update_test.go::TestBPP23_ValidConfigFieldsWhitelist` + `ValidatePayload_AcceptsWhitelistedFields` + `ValidatePayload_RejectsRuntimeFields` (8 反约束) + `ConfigRevTracker_IdempotentReload` + `ConfigRevTracker_NegativeRev` + `ValidatePayload_RejectsMalformedJSON` (5 反约束) + `ErrorCodeLiteralsByteIdentical` PASS + BPP-1 #304 reflect lint 自动覆盖 — REG-BPP2-012..017 (6 行) |

**总体**: 5/5 通过 (覆盖 acceptance 17 项 ✅) → ✅ **SIGNED**, G4.3 BPP-2 协议抽象语义层闸通过.

---

## 2. 跨 milestone byte-identical 链验 (BPP-2 是 plugin 协议主线锚, 多 milestone 锚承袭)

BPP-2 兑现/承袭多源 byte-identical:

- **reason 6 处 → 8 处单测锁链** post-#481 AL-2b ack + #485 BPP-2 task_failed 加入 (#748ad89 stance sync 5 篇): AL-1a #249 源头 + AL-3 #305 + CV-4 #380 + AL-2a #454 + AL-1b #458 + AL-4 #387/#461 + **BPP-2 task_failed (此)** + **AL-2b ack 第 8 处** — 改 reason = 改八处, 任一处漂 = lint fail
- **5-frame → 12-frame envelope 共序 byte-identical**: type/cursor 头位 + hub.cursors atomic int64 单调 + BPP-1 #304 reflect lint 自动覆盖; control 6 (RT-0/RT-1.1/RT-1.3/AnchorComment/MentionPushed/IterationStateChanged/AgentConfigUpdate) + data 6 (artifact/iteration/anchor/runtime/task_started/task_finished + ack) — 改 frame = lint fail PR 卡
- **BPP-1 #304 envelope CI lint 自动扩**: whitelist 9→12 (AL-2b ack +1 + BPP-2.2 task +2), TestBPPEnvelopeFrameWhitelist count + TestBPPEnvelopeDirectionLock data 字面同步
- **AL-2b #481 协同**: AgentConfigUpdateFrame 复用 (不裂 namespace) + AL-2a #480 server PATCH→hub.PushAgentConfigUpdate→AL-2b BPP frame fanout→plugin 接收 整链跨 PR byte-identical 锚
- **AL-1b #482 task_started/task_finished UPSERT 真接管**: AL-1b REG-AL1B-003 stub UPSERT 路径 → BPP-2.2 真 frame dispatcher 落地 (AL-1b deferred e2e 1 项 ⏸️ 等 BPP-2 真 frame 后翻, 本 milestone 落地解封 AL-1b 翻牌路径)
- **ADM-0 §1.3 admin god-mode 红线承袭**: BPP-2 dispatch 不挂 admin path (反向 grep `admin.*BPPDispatch\|admin.*RegisterHandler` count==0) 跟 AL-3 #303 ⑦ + AL-4 #379 v2 + AL-2b #471 §2.4 + ADM-2 #484 同模式
- **ActionHandler interface seam 依赖反转**: bpp 包不 import internal/api (跟 ArtifactPusher / IterationStatePusher / AgentConfigPusher 同模式) — 防 dispatcher 下穿 raw REST

---

## 3. 留账 (G4.3 闭闸不阻, 留 follow-up)

- ⏸️ **plugin 上行 frame dispatcher 真接管** — BPP-3 (#489 in-flight, AL-2b ack ingress 真接管 + plugin_frame_dispatcher_test 6🟢) 同 worktree 已落, 跟 G4.audit 同期收口
- ⏸️ **BPP-2.4 plugin SDK 起手** — protocol 抽象稳后 plugin SDK (TS / Go / Python 三端) 留 v2 (蓝图 §1.6 字面 "v1 不裂 SDK")
- ⏸️ **task lifecycle e2e 真路径** — AL-1b deferred e2e 1 项 ⏸️ 待 BPP-3 #489 ack ingress merge 后翻 (跟 G3.4 野马 5 张截屏 #450 同模式 deferred ⏸️)

---

## 4. 解封路径 (Phase 4 退出闸)

- ✅ **G4.1 ADM-1**: 野马 ✅ #459 SIGNED
- ✅ **G4.2 ADM-2**: 烈马 acceptance ✅ #484 6cf5240 SIGNED
- ✅ **G4.3 BPP-2**: 烈马 acceptance ✅ 本 signoff (5/5 验收 + REG-BPP2-001..017 17🟢 + 26 unit test + 反向 grep 7 项 count==0)
- ⏳ **G4.4 CM-5** (#476 merged) — 同 batch 跟进
- ⏳ **G4.5 AL-2a + AL-2b + AL-4 联签** (3 milestone 全 merged) — 同 batch 跟进
- ⏸️ **G4.audit** Phase 4 代码债 audit (软 gate 飞马职责)
- ⏸️ **Phase 4 closure announcement** (G4.1-G4.5 全签 + G4.audit 后链入)

---

## 5. 更新日志

| 日期 | 作者 | 变化 |
|------|------|------|
| 2026-04-29 | 烈马 | v0 — G4.3 BPP-2 协议抽象语义层闸 ✅ SIGNED post-#485 merged fe0913c. 5/5 验收通过 (envelope whitelist 9→12 + 7 v1 op + ActionHandler seam + TaskStarted/Finished 6+7 字段 + reason 八处单测锁链 + AgentConfigUpdate 复用 + 6 fields 白名单 + ConfigRevTracker 幂等 + 4 task + 2 config 错误码字面). 跟 #403 G3.3 / #449 G3.1+G3.2+G3.4 / #459 G4.1 / G4.2 烈马代签机制同模式 — 真单测实施证据 + 立场反查 + acceptance template 闭锁. REG-BPP2-001..017 17🟢 + 26 unit test (10 dispatcher + 8 task lifecycle + 8 agent_config_update) + 反向 grep 7 项 count==0. 跨 milestone byte-identical 链全锚 (reason 8 处单测锁 #748ad89 sync 5 篇 + 12-frame envelope 共序 + BPP-1 #304 reflect lint 自动扩 + AL-2b/AL-2a/AL-1b 协同链 + ADM-0 §1.3 红线 + ActionHandler 依赖反转). 留账 3 项 ⏸️ deferred (BPP-3 plugin 上行 dispatcher + BPP-2.4 SDK + task lifecycle e2e), 跟 CV-1 -017 / CHN-1 -007 / G4.2 同模式不阻闭闸. G4.4/G4.5 烈马同 batch 接续不 idle. |
